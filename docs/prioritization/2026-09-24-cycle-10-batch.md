# Prioritization batch 2026-09-24 — repo lineage cycle 10

Provenance: conductor run 449ed1a5f72c4bce8c31e038ffeffcea, prioritize attempt 5201e21d1cbb40a78b0935ef34187c70.

Lineage numbering (disclosed per convention): the engine labels this run cycle 1; repo lineage's last
prioritization doc is `2026-09-21-cycle-5-batch.md` while ROADMAP notes reference a cycle-9 compound
(2026-09-24, no batch doc), and this run's roadmap extension already self-describes as **cycle 10**
(`ROADMAP.md` cycle-10 extension comment, items rm-160..rm-170). Doc takes repo lineage cycle 10.

## Live frame re-measurement (2026-09-24)

- `origin/main` tip `82312f7` ("merge: autonomously integrate run f4622d7", 2026-09-24T04:13Z).
  **Main CI is RED at tip** (run 35954752299): exactly one deterministic failure —
  `test/fork-exclusion-guard.test.ts > no .conductor/ engine state is tracked in git` — because the
  landing merge re-tracked **13** `.conductor/progress/*.ndjson` files (verified
  `git ls-tree -r origin/main --name-only | grep -c '^.conductor'` => 13). This is the known recurring
  landing-merge hazard (constraints memory; latest same-day precedent: integrate 3ec29c38 cured by
  `git rm -r --cached .conductor` riding the merge). CodeQL/Scorecard/visual green at tip.
- Worktree `7809df6`: stale diverged sibling, 18 behind main, nothing unique (adjudicated by main's
  ROADMAP cycle-9 note and this run's assess baseline decision). ROADMAP.md in this worktree is already
  main content + the cycle-10 extension (rm-160..rm-170) from this run's roadmap phase.

Inputs used, not redone: assess 73943980 (11 verified findings F1–F11, two codex cross-model passes),
research f3942a23 (upstream v0.115.0 delta, toolchain majors, standards), roadmap 0dc9d8b9 (cycle-10 items).

## Gated scoring (five axes: impact, risk-to-skip, risk-to-do, effort, dependencies)

Gates: (G1) restore/lock a currently broken invariant? (G2) completable end-to-end this cycle offline
(no live gateway, no push authority, no external owner)? (G3) depends on unlanded or out-of-repo work?

| item | impact | risk-to-skip | risk-to-do | effort | deps | gated |
| --- | --- | --- | --- | --- | --- | --- |
| B1 untrack .conductor (live CI red) | 10 — Main red at tip blocks every landing | 10 — every integrate re-reds until cured | 1 — `git rm -r --cached`, rides integrate merge | XS | none | PASS |
| rm-160 deadlines + cycle-bounded refresh (92.0) | 9 — snapshot freezes invisibly; codex-confirmed high | 9 — wedge class has no other owner | 2 — deadline plumbing + guard clearing, tests exist | M | none | PASS |
| rm-112 remainder (75.0) | 8 — silent-degradation visibility arc left half-done | 7 — named halves since cycle-9 | 2 — absence entries, warm-empty banner, MonitoringDto signal | S-M | none | PASS |
| rm-161 denylist telemetry (58.0) | 7 — warning can never clear on modern node_ids | 6 | 2 — pure-function + tests | S | none | PASS |
| rm-165 gateway-session allowlist (36.0) | 5 — invariant widening risk | 5 | 1 — one branch + test | XS-S | none | PASS |
| rm-166 push-index read cache + no-store (30.0) | 4 | 3 | 1 — memoized read + header + test | XS-S | none | PASS |
| rm-157 pin bump + runbook (72.0) | 7 | 5 | 3 | M | live gateway | DEFER — G2 (cycle-9 G2 precedent) |
| rm-103 absorb (85.0) | 6 | 4 | 3 | M | agent pin sequenced behind rm-157; chore half rides rm-102's ~2026-10-03 dependabot window | DEFER — G2/G3 |
| rm-167 vitest 5 wave (55.0) | 5 | 3 | 3 — suite-wide | M | majors deliberately frozen by rm-133's ignore block (re-eval 2026-10-21) | DEFER — time-gated by policy |
| rm-168 TS 7 probe (40.0) | 4 | 2 | 3 | M | same freeze; rm-108 already documents typescript-eslint needs 6.x side-by-side | DEFER — peer evidence |
| rm-162 API budget / ETag (60.0) | 6 | 4 | 2 | M | none | DEFER — next batch head (perf wave) |
| rm-170 octokit hygiene (28.0) | 4 | 3 | 2 | S-M | none | DEFER — pairs with rm-162 |
| rm-163 push lifecycle (54.0) | 5 | 4 | 2 | S-M | none | DEFER — web-side wave, keep src batch focused |
| rm-164 SSE watchdog + resume decision (50.0) | 6 | 4 | 3 — design decision first | M | resume may need gateway support | DEFER — decision required |
| rm-104 render hardening (98.0) | 7 | 6 | 3 | M | tool lives in fleet repo | HOLD — out-of-repo scope |
| rm-116 branch protection (58.0) | 6 | 5 | 3 | M | owner/account action | DEFER — G2 |
| rm-105 SBOM/provenance (80.0) | 6 | 4 | 3 | M | Release surface wave | DEFER |
| rm-107 status-surface composition (65.0) | 6 | 4 | 3 | M | B2's rm-112 remainder delivers its degradation-signal slice | DEFER — partially advanced by B2 |
| rm-127 gateway redirect topology (56.0) | 5 | 4 | 3 | M | decision | DEFER |
| rm-119 gate-state snapshot (52.0) | 5 | 4 | 2 | M | REST expansion | DEFER |
| rm-117 alert counts (44.0) / rm-118 per-repo tokens (42.0) | 5/6 | 4 | 3/4 | M/L | snapshot-surface expansion | DEFER |
| rm-144 fuzz suites (50.0) | 5 | 4 | 3 | M | none | DEFER |
| rm-141 concurrency (47.0) | 5 | 3 | 3 | M | none | DEFER |
| rm-129 trusted-proxy (40.0) | 4 | 3 | 2 | S-M | rides rm-157's absorb-verification runbook | DEFER — G3 |
| rm-120 drift watch (36.0) | 3 | 3 | 2 | S | workflow surface; first delta content already produced by this run's research | DEFER |
| rm-138 PWA decision (30.0) | 3 | 2 | 2 | S | decision required | DEFER |
| rm-140 pnpm 12 review (22.0) | 2 | 2 | 1 | S | wants its own dated changelog review | DEFER |
| rm-139 / rm-102 / rm-169 | — | — | — | — | time-gated (~Oct 2026 / 2026-10-03 / LTS promo) | TIME-GATED |
| rm-106 / rm-147 / rm-159 | — | — | — | — | blocked-external / push authority | BLOCKED |

## Selected batch — cycle 10

**B1 (invariant restore, XS — must land first):** untrack the 13 `.conductor/progress/*.ndjson` re-introduced
by landing merge 82312f7. Implementation per the recorded cure: `git rm -r --cached .conductor` (disk copies
preserved untracked; back up before any reset), staged deletions ride this run's integrate/landing merge;
acceptance = fork-exclusion-guard green at the landed tip (Main run green). Trap recorded: the
candidate-delta exclude-pathspec drops staged deletions — the untrack must ride the merge that lands.

**B2 (refresh-path reliability wave — the cycle's value payload):** rm-160 + rm-161 + rm-112-remainder +
rm-165 + rm-166. One coherent src cluster (src/github/aggregator.ts, src/server.ts, plus client factories),
all offline-testable in `pnpm test`, no schema/workflow/web-build surface. Completes rm-112's arc, closes
this run's top codex-confirmed finding (rm-160), and keeps every edit read-side (invariant-safe).

Sequencing note for implement: B1 is staging-only; B2 edits tracked src files; neither touches `.conductor`
content — and `git add` discipline must exclude `.conductor/` (the exact red mechanism B1 cures).

## Pre-review validation outcome (recorded at compound, 2026-09-24)

B1 + B2 implemented in-tree (implement eae30057, recovering from the 429-killed bd1adae7):
B1's 13 staged deletions intact (`git ls-files .conductor` empty); B2 = rm-160/161/112-remainder/
165/166 across src/github/{aggregator,app-client,installations,metadata}.ts, src/server.ts,
src/routes/api.ts + 15 new it()-level test cases.

- Local, this exact tree (fingerprint aefa27d2…): `pnpm test` exit 0 — server 2063/2063 (32 files) +
  web 1078/1078 (28 files); `pnpm lint` 0; `pnpm check-types` 0; fork-exclusion-guard 15/15 green
  (B1 active in the index).
- Ephemeral-PR cloud route (github_ci_validate, runs 35964569879 / 35965269959 / 35966401894 — three
  independent pushes): every surface-reflecting check green (Lint, Check Types, Design, Check Workflows,
  Test Scripts Load, Dependency Review, CodeQL). Test job 31/32 files each time; the sole red is
  fork-exclusion-guard rm-131 on base-lineage `.conductor` breadcrumbs — the documented
  exclude-pathspec artifact (docs/solutions/workflow-issues/validation-clone-exclude-pathspec-drops-staged-breadcrumb-deletion-2026-09-24.md),
  unfixable pre-landing and self-curing when B1 rides the merge.

Landing checklist carried to merge-release: preserve B1's 13 staged deletions through the integrate
merge; after landing verify `git ls-files .conductor` empty and the guard green on Main at the pushed
sha (flips main's standing red, run 35954752299). Engine-side formality recorded for future full_tests
attempts: `validation_evidence.command` must be byte-exact to the dispatch `full_command` — supporting
gates go in evidence_refs, never appended to the command string.

Deferred candidates left queued for cycle 11+: rm-162 (API-budget memoization), rm-163 (push abort
windows), rm-164 (stream watchdog + Last-Event-ID decision) — the natural next reliability wave;
rm-167 vitest 5 / rm-168 TS 7 / rm-169 Node 26 behind the rm-133 freeze (re-eval 2026-10-21) and LTS
timing; rm-157 agent v0.115.0 absorb still needs the live gateway.
