# Prioritization batch 2026-09-24 — repo lineage cycle 10

Provenance: conductor run a4a97e30f3a24b75a7bc6f3f0c123dcd, prioritize attempt
eb0e7e41416449adb58720a83493abb8, repository-maintenance lineage c56a9cd9 cycle:1 (engine label).

Lineage numbering (disclosed per convention): engine labels this run cycle-1 of a new lineage, but repo docs
carry landed batches cycle 1–7 plus the cycle-9 doc (docs/prioritization at origin/main 82312f7, verified by
ls-tree 2026-09-24), PR #23's title burned "cycle-8" without landing a doc, and PR #45 additionally claims the
cycle-9 label (noted in the cycle-9 doc's own post-hoc). Taking lineage **cycle 10** avoids every collision;
the run id `a4a97e30…` is the disambiguator, as the ROADMAP compound header records for cycle 9.
This doc is authoritative for this run's implement phase.

## Live frame re-measurement (stale-base rule)

- `git fetch origin` 2026-09-24 (prioritize): worktree HEAD `7809df6`; `origin/main` = `82312f7` —
  **main advanced DURING this run**. The roadmap phase (f8230f54) based its edit on `763c1fa`; since then
  `6b6f4e7` landed the cycle-9 batch (release-trigger completeness rm-154; fail-visible aggregator rm-112
  halves; test/release-trigger-paths.test.ts +229) and `82312f7` integrated run f4622d7 (+928/−48 over 27
  files across the two commits).
- Consequences, disclosed for the implement/integrate phases (not re-done here — roadmap phase is complete):
  - The worktree ROADMAP's cycle-9 statuses (rm-154/rm-155/rm-158 "pending landing", rm-112 fix-halves) are
    now LANDED at main. Integrate reconciles by content (statuses → completed with `6b6f4e7` evidence); no
    renumbering is needed — main's id space ends at rm-159 and this run's additions start at rm-164 with the
    reservation note already in the file header.
  - Landing merge `82312f7` re-tracked `.conductor/progress/*.ndjson` on the OURS side (recurring pattern);
    integrate must let `git rm -r --cached .conductor` ride the landing merge and verify
    `git ls-files .conductor` ends empty.
  - All four batch selections below were re-verified against `82312f7` directly (evidence per item).
- Landed rm-112 surface that B1 composes with (src/github/aggregator.ts at main): snapshot-level
  `staleBanner: boolean` (:108) with `refreshedAt` (:115) and per-repo `stale` (:76); fail-closed
  last-good-cache docstring (:13). B1 mirrors this additive-snapshot-field pattern for the enumeration gap.

Inputs used, not redone: assess 036d520b (6 findings at 7809df6, de-duplicated vs main; codex cross-model
blocked by CLIProxyAPI policy — single-model, disclosed), research 66ae4073 (upstream drift 4 dep-only
commits at 9908bcf; base-drift.yaml digest-only scope; ecosystem watchlist unchanged), roadmap f8230f54
(ROADMAP.md at 483 lines on 763c1fa content: rm-164..167 added, 10 verified landings relocated to
Completed, lint green).

## Gated scoring (impact, risk-to-skip, risk-to-do, effort, dependencies)

Gates: (G1) restores/locks a broken invariant? (G2) completable end-to-end this cycle with hosted-runner CI
evidence? (G3) depends on unlanded or out-of-repo work?

| item | impact | risk-to-skip | risk-to-do | effort | deps | gated |
| --- | --- | --- | --- | --- | --- | --- |
| rm-164 enumeration-gap visibility (66.0) | 8 — a monitoring dashboard whose working set can silently shrink during a partial GitHub App outage, while the snapshot claims freshness | 7 — the exact failure class the product exists to surface; verified live at 82312f7 (installations.ts mint catch + list-repos catch → `continue`, enumerate still Ok) | 3 — additive snapshot field + failure contract + tests; composes on the just-landed `staleBanner` precedent, no field removal | M | none (rm-107's full panel stays deferred; this is the signal layer) | PASS |
| rm-167 drift-watch extension to dep/action pins (44.0) | 7 — 100% of the measured live upstream delta (4/4 dep-pin commits at 9908bcf) is invisible to the only drift detector; dependabot window closed until ~2026-10-03 | 6 — the covered class (base digests) had already self-resolved to parity while the uncovered class IS the entire delta | 2 — read-only workflow job in an already-landed file; strictly never-bumps by design (rm-102 owns bumps) | S-M | none | PASS |
| rm-165 dead gateway-client surfaces + header truth (34.0) | 5 — the contract header actively misleads every fresh session; dead `launchRun`/`getRunSnapshot` carry the rm-130 CSRF bug class into any future caller | 5 — verified at 82312f7: src/ + web/src/ grep finds the names only in src/gateway/operator-client.ts itself (tests aside) | 1 — deletion + header rewrite + test updates; zero callers to migrate | S | none | PASS |
| rm-166 docs-truth micro-batch 3 (30.0) | 5 — binding README claims a Workbox stack that was removed at f334fb2 (rm-128); vite.config precache comments describe caching the kill-switch discards; wiki-writer prose residue | 4 — binding-docs consistency invariant + every fresh agent session greps into the residue | 1 — docs/comments/wording only | S | none (independent of rm-138's architecture decision by design) | PASS |
| rm-105 SBOM/provenance (80.0) | 7 | 4 — attestations already land via buildx | 3 | M | Release-pipeline scope beyond this batch's theme | DEFER |
| rm-107 composed status panel (65.0) | 6 | 3 | 3 — UX design surface | M | **enabled by this batch's B1** (signal layer first) | DEFER |
| rm-103 absorb cadence (85.0) | 6 | 3 | 3 | M | nothing functional to absorb: 4 dep-only commits at 9908bcf | DEFER |
| rm-116 branch protection (58.0) | 6 | 5 — three recorded red landings | 3 | S | account-level owner action | DEFER — raise out-of-band |
| rm-129 trusted-proxy limiter (40.0) | 6 | 5 — proxy-collapse lockout documented on the gateway precedent | 3 | M | rm-127 topology decision | DEFER |
| rm-144 property-based SSE suites (50.0) | 5 | 3 | 2 | M | none | DEFER — next-cycle pairing with rm-114 |
| rm-104 render hardening (98.0) | 7 | 6 | 3 | M | hermes-roadmap lives in the fleet repo | HOLD — fleet scope |
| rm-102 first-PR proof / rm-146 auto-merge | — | — | — | — | dependabot window ~2026-10-03; rm-146 sequenced on rm-116 | NOT selected (time/sequence blocked) |
| rm-106 send surface / rm-115 persistent-query relay | — | — | — | — | blocked-external (upstream gateway contract) | NOT selected |
| rm-141 setup-action promotion / rm-117 metadata count / rm-118 TTL knob | 3–4 | 2 | 1 | S each | none | NOT selected — off-theme slack riders if implement finishes early; do not force |
| rm-150/152/154/155/157/158/159 (pending-landing) | — | — | — | — | landing is the integrate phase's job, not implement | NOT selected |

## Selected batch — theme: make the remaining silent gaps loud

All four items close gaps THIS run measured fresh (assess 036d520b F2/F3/F4–F6; research 66ae4073 C1); none
needs push authority, infra pairing, or unlanded contracts; risk profiles are additive-field, read-only
workflow, and docs/dead-code; the full suite plus the actionlint container gate are the nets.

- **B1 — rm-164: enumeration-gap visibility (flagship).** Scope tight to the signal layer: (a)
  `enumerateRepos` (src/github/installations.ts:221-236 at main — mint catch 222-227, list-repos catch
  231-236) returns per-installation failure detail (installation id + error class) instead of only
  warn-and-`continue`; (b) the snapshot gains an additive, fail-visible field mirroring the landed
  `staleBanner` precedent — e.g. `skippedInstallations` count (plus a boolean or null-shape consistent with
  the repo's existing additive semantics) — so a reduced working set can never render as fresh; (c) driftCount
  semantics documented against the reduced set (either excludes nothing silently or the skip is visible next
  to it); (d) tests cover the mixed fixture (one healthy + one failing installation) end-to-end. NOT in scope:
  the full composed operator panel (rm-107) and any web UX beyond surfacing the count where `staleBanner`
  already renders. AC per rm-164: partial failure renders on the same surface family as `staleBanner`; no
  public field removed (web client consumes these shapes); suite green including the new mixed-fixture tests.
- **B2 — rm-167: extend the drift watch from base digests to dependency/action pins.** One new read-only job
  (or job extension) in .github/workflows/base-drift.yaml, reusing its landed tracking-issue discipline:
  diff the fork's `packageManager` pin, workflow action pins, and key npm pins (package.json/in-lockfile) against
  upstream's values (fro-bot/dashboard raw files) and npm latest; report divergence in the same single-issue
  form. Strictly never-bumps (dependabot owns bumps per rm-102/rm-146 — the file's own design rule).
  AC: actionlint container gate exit 0; workflow_dispatch dry-run output logged showing digest + pin rows;
  tracking issue opens only on divergence; zero secret requirements (GITHUB_TOKEN only).
- **B3 — hygiene pair: rm-165 + rm-166 (clears the remaining assess findings).** rm-165: rewrite the
  src/gateway/operator-client.ts contract header to name the live surfaces (auth `getCurrentSession` + the
  browser runtime's decide/push) and the frozen ones; delete `launchRun` (:227 interface, :546 impl) and
  `getRunSnapshot` (:228, :576) with their tests — or land a dated keep-note if a caller is planned, and if
  kept, apply the rm-130 CSRF-refresh retry discipline to `launchCmd` in the same change. **Boundary: do not
  touch public/operator-launch.{js,d.ts}** — that browser module owns its own client (its `launchRun` is a
  definition, not a call into the gateway class) and is a separate lineage (rm-114 adjacency). rm-166: truth
  the README stack list (Workbox claim at README.md:31-32 at main; describes the kill-switch SW), state the
  kill-switch reality in web/vite.config.ts:31-32,40-46 comments (independent of rm-138's architecture
  decision), and clear the wiki-writer prose residue in docs/ideation/ (three files at main: the 2026-09-20
  research pair plus 2026-09-21-repository-extensions-research.md) either by read-only rewording or by adding
  the ideation tree to the documented exclusion set with the canonical grep form updated in the same change.
  AC: `grep -i workbox README.md` → 0; canonical sweep (no extension/path filters) returns only intended
  historical hits; suite + `pnpm lint` green.

Order: **B2 → B3 → B1** (mechanical/read-only riders green first; the one semantic change lands last with
the full suite as its net — the same discipline cycle 9 used). Landing rider for integrate: reconcile the
ROADMAP cycle-9 statuses against `6b6f4e7` and let the `.conductor` untrack ride the merge (live-frame
section above).
