---
date: 2026-09-20
topic: dashboard maintenance cycle 2 — scoring and batch selection (post-916783f re-scope)
mode: delegated-conductor
run: 779e72711650493589a3b2e6cb1903a7
phase: prioritize
attempt: 618e9d1c570a4e4b80ab9505b9eb9a8d
skill: ce-plan (scoring/batch selection — no dedicated ce-prioritize skill in the installed router; fleet precedent cycle 1 used ce-plan identically). Deviations declared: no subagent surface in this session, frames run in-process and disclosed (convention #3768). Label disclosure per #4323: the engine cycle label is 2 and this is the SECOND batch selected under that label — run 270220e7's cycle-2 batch already landed as 916783f (04:56Z today) WITHOUT a batch doc; this document re-scopes against that landing.
---

# Dashboard maintenance — cycle 2 batch, take 2 (2026-09-20)

## Live frame facts (re-measured this phase, not carried stale)

- Fork tip moved DURING this run's roadmap phase: `origin/main` = `916783f` (2026-09-20T04:54Z, run 270220e7's cycle-2 batch: "restore main gates, absorb upstream drift, fix checkSuites undercount"). This worktree sits at `2f3a884` (1 behind, no local commits; uncommitted: restored ROADMAP.md lineage + research doc).
- Landed at `916783f`: ROADMAP rebuild (their lineage `rm-100..rm-115`), fro-bot.yaml job-env `HAS_FRO_BOT_PAT` gate (the #3273 pattern — Main Check-Workflows should pass), codeql.yaml deps-install via `./.github/actions/setup`, Dockerfile pins → `0e0ff40` with in-image libpcre2 patch retired, hono 4.13.8, `checkSuites(first: 100)` + 12-suite fixture (suite 3018 local), checkout pin unified v7.0.1.
- STILL RED at `916783f`: CodeQL (run 35490406856, failed 04:57Z) — `Cannot find module 'typescript'` → `TypeScript parser wrapper crashed` → fatal from `autobuild.sh` under `trace-command --use-build-mode`. The landed deps-install DID run (`pnpm install --frozen-lockfile`, "Done in 19.6s", 04:57:33Z) yet the wrapper still failed 14s later: the "missing deps" theory from runs 333ad19e/270220e7 is insufficient. Decisive counter-evidence: upstream `fro-bot/dashboard` runs CodeQL GREEN (run 35489816584, 57s, 2026-09-20T04:42Z) with NO install step at all and the same `build-mode: none` — upstream's workflow differs only in `runs-on: ubuntu-latest` and checkout v6.1.0. Anomaly: with `build-mode: none` no autobuild should run at all; the self-hosted run traces `autobuild.sh` anyway — prime suspect is the runner's cached CodeQL toolchain (2.27.0) / toolcache interplay, not the repo's dependency tree.
- Main queued (35490406915, 7m45s+ queued behind Release on the single self-hosted runner — serialization per #3264/#3391); Release in_progress (35490406800, 7m46s; warm runs ~7 min). Scorecard green at `916783f`. Both Main and Release outcomes were PENDING at scoring time — the implement phase MUST re-verify, and if Release Trivy reports HIGH findings on the new base, absorbing that becomes batch contingency (rm-111's outstanding acceptance).
- Two divergent curated ROADMAP lineages now exist: landed `916783f` (`rm-112` aggregator-staleness, `rm-113` CI pin hygiene, `rm-114` SSE single-sourcing, `rm-115` CSRF) vs this run's restored f4622d7e lineage + extensions (`rm-112` merge-hygiene, `rm-113` security panel, `rm-114` per-repo tokens, `rm-115` gate-health roll-up, `rm-116..rm-121`). The ids `rm-112..rm-115` COLLIDE across different meanings.
- `rm-117`-concept still outstanding on main: binding docs + metadata.ts doc comment still say `fro-bot/.github` (AGENTS.md:18, README.md:65, src/github/metadata.ts:4 at `916783f`) while code reads `codeo1io/.github` since 4aa5d07.
- Upstream drift: 6 commits (fetched this run; `876a02a` security #481 merged — fork lacks `app.onError` entirely; `81d6c20` #493 clonedeps removal must be SKIPPED: fork keeps its clonedep artifacts deliberately as the v0.78.0 reference source). No open PRs on fork or upstream.

## Five-axis scoring (1–5; Effort 5 = cheapest; Dep-free 5 = no external landing dependency)

| Ref | Impact | Delay | Effort | Dep-free | Strategic | Total | Standing |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CodeQL round 2 (rm-100 rider) | 5 | 5 | 3 | 5 | 5 | 23 | SELECTED B1 |
| Port upstream #481 chokepoint (rm-122 new) | 5 | 4 | 4 | 5 | 5 | 23 | SELECTED B2 |
| ROADMAP lineage reconciliation (cross-cutting) | 4 | 5 | 4 | 5 | 3 | 21 | SELECTED B3 |
| Binding-docs metadata sync (rm-121 new) | 2 | 3 | 5 | 5 | 2 | 17 | SELECTED B4 |
| Server/docs hygiene batch (rm-124 new) | 3 | 3 | 4 | 5 | 3 | 18 | SELECTED B5 (droppable last) |
| Merge-hygiene baseline (rm-116 new) | 4 | 4 | 3 | 4 | 4 | 19 | DEFER (process decision) |
| Digest-drift visibility (rm-123 new) | 3 | 2 | 4 | 3 | 4 | 16 | DEFER |
| SBOM + provenance (rm-105) | 4 | 2 | 2 | 4 | 4 | 16 | DEFER |
| Operator status panel (rm-107) | 3 | 3 | 2 | 5 | 4 | 17 | DEFER |
| Absorb cadence + drift gate (rm-103) | 3 | 2 | 2 | 4 | 3 | 14 | DEFER |
| Aggregator staleness semantics (rm-112 landed-id) | 3 | 2 | 2 | 5 | 3 | 15 | DEFER |
| Gate-health roll-up (rm-119 new) | 3 | 3 | 2 | 5 | 4 | 17 | DEFER |
| Dependabot first-PR proof (rm-102) | 3 | 1 | 3 | 1 | 3 | 11 | DEFER (external clock ~2026-10-03) |
| Listener digest push (rm-106) | 3 | 2 | 2 | 2 | 3 | 12 | DEFER (privacy policy first) |
| Security-posture panel (rm-117 new) / per-repo tokens (rm-118 new) / gateway watch (rm-120 new) | 3 | 2 | 2 | 4 | 4 | 15 | DEFER (feature track) |
| SSE single-sourcing (rm-114 landed-id) / CSRF (rm-115 landed-id) | 2 | 2 | 3 | 5 | 3 | 15 | DEFER (hardening track) |

## Selected batch — "finish the gates, close the redaction gap"

Five work units, no file overlaps, all locally verifiable except the final CI proof.

### B1. CodeQL round 2 (rm-100 rider) — the cycle's mandate

Diagnosis-first: with `build-mode: none`, `autobuild.sh` should never run; upstream is green with no install step. Inspect the failed run's init-step logs for how build mode resolved on the self-hosted runner (stale toolcache 2.27.0 vs action v4's bundled toolchain is the lead hypothesis), then fix along the options ladder, recording why each rejected option failed: (a) force the action's own toolchain (init `tools: latest` / explicit version) to bypass the runner cache; (b) drop the now-proven-insufficient deps-install step if the toolchain fix alone greens it (keeps the workflow closest to upstream's proven shape); (c) only if a build genuinely must be traced, provide an explicit minimal build command instead of autobuild.

- acceptance: root cause identified from run logs and written to `docs/solutions/workflow-issues/`; fixed codeql.yaml passes the actionlint container gate locally; CodeQL green at the pushed sha; upstream-contrast documented (green run 35489816584 cited).
- evidence: `docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:1.7.12 -no-color` exit 0; gh run view of the new green CodeQL run; solution doc exists.

### B2. Port upstream security #481 — app.onError redaction chokepoint (new id rm-122)

Hand-port `876a02a` intent (fork server.ts has diverged; line-merge will not apply): register `app.onError` in `buildDashboardApp` — `logger.error` with `sanitizeErrorMessage` + generic 500. Fork currently has NO onError anywhere (grep-verified this run): an unhandled throw bypasses the logger.ts single-chokepoint redaction and hits Hono's raw `console.error`.

- acceptance: onError registered; a secret-shaped throw never reaches the response body nor the log raw (upstream's two test intents ported, adapted to the fork's fixture harness); existing handler behavior unchanged.
- evidence: `pnpm test` green (3018+N) including the new redaction tests; grep shows onError registered in src/server.ts; `git show 876a02a` cross-referenced.

### B3. ROADMAP lineage reconciliation (cross-cutting, mandatory)

Rebuild ROADMAP.md taking `916783f`'s file as BASE (landed ids are now authoritative), absorbing the unlanded f4622d7e curation and this run's items under renumbered, collision-free ids. Fixed mapping, applied verbatim: `rm-116` merge-hygiene baseline, `rm-117` security-posture panel, `rm-118` per-repo scoped tokens, `rm-119` gate-health roll-up, `rm-120` gateway-contract drift watch, `rm-121` binding-docs metadata sync, `rm-122` port #481 chokepoint (=B2), `rm-123` digest-drift visibility, `rm-124` server/docs hygiene (=B5); f4622d7e's aggregator-hygiene content folds into landed `rm-112`; all landed-id meanings (rm-112..rm-115 at 916783f) unchanged. Preserve implement-outcome/review-note history from this run's restored lineage; refresh statuses against `916783f` + fresh run outcomes.

- acceptance: exactly one meaning per id; statuses current; `pnpm lint` exit 0; nothing from either lineage silently dropped (explicit supersession notes instead).
- evidence: id-mapping table above executed; eslint ROADMAP.md exit 0; diff vs 916783f reviewed against this table.

### B4. Binding-docs metadata-source sync (new id rm-121)

Four textual fixes: AGENTS.md:18, README.md:65, copilot-instructions.md:17, src/github/metadata.ts:4 doc comment — `fro-bot/.github` → `codeo1io/.github` (code is correct; docs misstate production since 4aa5d07). Also aligns metadata.ts doc comment with its own tests.

- acceptance: grep for the `fro-bot/.github` metadata claim across binding docs returns nothing; known non-residue left untouched (`.github/renovate.json5` shared-config semantics; fro-bot.yaml agent prompt text; test/server.test.ts titles already deferred by prior review note — fold them here if trivial).
- evidence: grep output; `pnpm lint` exit 0; `pnpm test` green.

### B5. Server and docs hygiene batch (new id rm-124) — stretch rider, droppable without breaking coherence

Five locally-testable fixes from this run's assess: `devAutoLogin` allowlist matches its comment (unset NODE_ENV no longer passes, mirroring the fixture guard); metadata.ts malformed-entry count logged as promised; OAuth state compare timing-safe (session.ts discipline); trivy solution doc's false "Renovate already keeps this pin current" sentence corrected; Dockerfile:41 "staged" → landed.

- acceptance: all five fixed; new tests for the guard allowlist and skip-count; `pnpm lint` / `pnpm check-types` / `pnpm test` green.
- evidence: git diff of the two doc lines; suite green.

## Deferred (one-line rationales)

- Merge-hygiene baseline (rm-116): needs a PR-flow-vs-direct-push process decision — stewardship phase should own it; not a maintenance rider.
- Digest-drift visibility (rm-123), absorb cadence (rm-103), SBOM+provenance (rm-105): next-cycle additive-workflow/Release work; batch B1 already churns the CI surface.
- Status/roll-up/security panels + tokens + gateway watch (rm-107/rm-117/r-118/r-119/r-120): feature track, sized beyond this batch.
- Aggregator staleness semantics (rm-112 landed-id): medium feature; depends on none, but gates first.
- Dependabot first-PR proof (rm-102): external clock (~2026-10-03); no action possible.
- Listener digest push (rm-106): privacy policy prerequisite (upstream #238-class decision).
- SSE single-sourcing (rm-114) / CSRF tokens (rm-115 landed-id): hardening track, low severity.

## Disclosures

- This batch was scored AFTER 916783f landed mid-run; the assess/research/roadmap artifacts feeding it predate that landing — every item above was re-verified against `916783f` (grep/git show) before selection; nothing selected on stale evidence.
- Main (35490406915) and Release (35490406800) were still queued/in_progress at scoring time; if either fails, B1's scope extends to that failure and B5 drops first (dropping order: B5, then B4).
- Engine label "cycle 2" retained per RUN REQUIREMENT; repo lineage already consumed "cycle 2" for 916783f — this document is its scoped successor, disclosed per #4323.
