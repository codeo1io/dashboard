# Dashboard maintenance — cycle-17 batch, run 71991cb31f2041ee9932217a420ceb22 (2026-09-25)

Repository-maintenance cycle:1 (parallel run; the chronological cycle-17 slot was
taken by the numbering sequence, hence the run-suffixed filename). Selected by
the prioritize phase under two live-parallel-lineage constraints (cycle-16 run
040f1ae0 owns auth.ts/server.ts/canary/id-guard/lockfile-rider; the stranded
PR #166 owns aggregator/monitoring-refresh), then grounded surface-by-surface by
the stewardship phase.

## B0 — base + ledger (rm-165 cure)

Worktree re-based from the stale 7809df6 dispatch frame onto origin/main
**20ade64** before any edit (freshness gate re-verified this phase: main
unchanged, cycle-16 unlanded, so the rm-207..209 mints stand; landing-order
renumbering against cycle-16's rm-209/210/212 remains the loser's move).
ROADMAP.md carries this run's roadmap-phase apply plus the batch's status
updates below.

## U1 — container-CVE census response (rm-205 extended, rm-117 rider)

**Verdict flipped by live evidence.** The batch planned in-image apt patching
per the triage convention; the live check (`docker run node:24-slim@sha256:0e0ff40…
apt-cache policy …`) shows **every census package at Installed == Candidate** —
zero fixed versions published (perl-base's bookworm-security offer is the OLDER
u2; zlib is no-dsa; util-linux/libacl/ncurses/gzip/tar/systemd all current).
Nothing to patch to. Implementation is the census table + re-check procedure +
dismissal notes in
`docs/solutions/best-practices/container-cve-census-2026-09-25-no-fixed-versions.md`,
with the watch already wired (base-drift weekly digest compare + release-time
scans). Alert dismissals deferred to final_validation (default-branch scan must
see the fix first; bulk dismissal before that would misstate the census).

## U2 — rm-186: dropped (already landed)

Stewardship-time correction: `.dockerignore` landed at 5c9c5d2 (cycle-13
'environmental determinism', integrated 4d8ec12) with the guard assertions at
`test/dockerfile-context.test.ts:117-131` already live. Only the stale
`candidate` status string is corrected in ROADMAP.md.

## U3 — pin the base-drift upstream fetch (rm-209)

`.github/workflows/base-drift.yaml:172`: the `curl … package.json | node -p`
pipe (CodeQL PinnedDependencies, created 2026-09-25T04:01Z) replaced with an
in-node `fetch()` — the downloaded bytes are parsed as JSON, never executed.
Report-only semantics preserved (lookup failure still emits the warning +
'upstream lookup failed' summary line). Signals-line correction recorded in
ROADMAP.md: the flagged pipe is the package.json read; `scripts/read-pnpm-version.js`
does not exist in this repo.

## U4 — visual gate paths coverage (rm-207)

`.github/workflows/visual.yaml` pull_request paths gained `public/**`,
`web/index.html`, and `web/vite.config.ts` (the last was in the original filter
and had been dropped in the 45-commit window). New guard
`test/visual-gate-paths.test.ts` parses the workflow with `yaml` and pins the
coverage set + rejects negation entries, so a future filter edit cannot
silently re-open the escape hatch.

## Rider — fro-bot/agent digest (rm-157)

`.github/workflows/fro-bot.yaml:340`: `8e6494ed…# v0.115.0` →
`930ffc9f…# v0.115.1` (tag verified lightweight → commit). Single line; the
read-only prose block in the same file is untouched. The item's
gateway-verification acceptance stays deferred (G2, unverifiable without the
live gateway).

## Verification

- `pnpm vitest run test/visual-gate-paths.test.ts test/dockerfile-context.test.ts`
  (focused: the new guard + the adjacent context-seal guard)
- `docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest -no-color`
  over the two changed workflows
- `pnpm lint` (yaml yml/quotes + TS) and `pnpm check-types` on the touched
  surfaces
- Evidence for the census verdict: the `apt-cache policy` loop in the solution
  doc, runnable against the pinned digest at any time

## Next-cycle candidates (compounded context, 2026-09-25)

Queued with rationale for whichever lineage picks up the next cycle:

- **rm-208 (operator-experience, p32)** — auth-expired badge state + stale
  indicator with last-good timestamp + poll-loop mocks in web/src/App.tsx:45-56.
  The cycle-17 batch deliberately left it out (surface-collision discipline:
  this run owned workflows/census/docs only). The SPA is the only surface that
  hides expiry; the operator runtime it feeds already surfaces auth-required
  states.
- **rm-157's open core (reliability, p72)** — the v0.115.1 digest rider landed
  with this batch, but the gateway-verified OPERATOR_CONTRACT_VERSION 1.7.0
  surface (checkoutProvenance plumbing, failureKinds, runbook) remains, gated
  on a live-gateway change per the item's own acceptance.
- **CVE re-check on first base-digest move** — mechanical: base-drift weekly
  will observe the next node:24-slim rebuild; on that move re-run the census
  doc's re-check procedure before considering in-image patching (rm-205).
- **Strand reconvergence audit** — the live landing surface still spans PRs
  155, 136, 113, and 166 (six cycle labels; PR 166 carries the monitoring gate +
  bounded refresh at a 45-behind base). The next integrate should re-run the
  convergence check before minting, per the mint-collision protocol in
  docs/solutions/workflow-issues/stale-dispatch-base-and-parallel-lineage-mint-collision-2026-09-25.md.
- **Parallel-lineage watch** — cycle-16 (run 040f1ae0: auth.ts/server.ts/canary/
  id-guard/lockfile rider) was still unlanded at this compound. If it lands
  first, its rm-209/210/212 meanings own those ids and this batch's rm-209
  (PinnedDependencies) renumbers at the integrate — landed meanings own ids.
