# Dashboard maintenance — cycle-18 batch (2026-09-26)

Run `0c6f62d5295047178893326f2c52b07b` (repository-maintenance cycle 1 of this
work order), implement attempt `dec912be8321496ab52505e969b01fc5`. Base: origin/main
`e407953` (fetched fresh 2026-09-26; the dispatch frame `7809df6` is 57 commits
stale and was NOT used — rm-165 cure; the worktree was reset to the live tip
before any edit).

## B1 (rm-228, STRIP shape) — registration truth

**Decision: STRIP.** Recorded reasons (decided at prioritize, confirmed here):

1. The push send path does not exist — rm-106 is blocked-external on the gateway
   contract, so an implemented client ships a subscription UI that can never
   receive anything.
2. `dfa3f80` ("stop SW registration") is the recorded product intent; this batch
   COMPLETES that decision rather than reversing it. The un-overridden
   `injectRegister: 'auto'` default was silently undoing it on every page load.
3. rm-138's strip payoff rides the same `web/vite.config.ts` edit — one batch,
   one vite-plugin-pwa posture.

### Changes

- `web/vite.config.ts` — `injectRegister: false` (stops the auto-injected
  `vite-plugin-pwa:register-sw` script tag in built `index.html`); injectManifest
  globbing emptied (`globPatterns: []`) so the build emits no precache manifest
  (`precache 0 entries (0.00 KiB)` in build output) while `sw.ts` keeps compiling
  as the kill-switch; comments rewritten to the post-strip truth.
- `web/src/push/capability.ts` — new `isPushClientSupported()`: false unless a
  page-controlled worker exists (`navigator.serviceWorker?.controller != null`).
  Default-false is the strip truth; it flips true automatically the day a real
  worker registers, so it doubles as the re-introduction seam.
- `web/src/views/Notifications.tsx` — `runSweep` short-circuits to the new
  `unavailable` state before any capability probe or
  `navigator.serviceWorker.ready` await (the ready promise never settles when
  nothing registers the worker — that was the permanent `sw-not-ready` hang);
  `handleEnable`/`handleDisable` carry the same guard (unreachable while
  unavailable; kept as the re-introduction seam).
- `web/src/views/notifications-copy.ts` — new `unavailable` state (9 states
  total): "Alerts Not Available" + copy explaining this build has no
  push-capable background worker; no CTA. Copy-module security patterns honored
  (no endpoints/paths/keys/protocol strings).
- `web/privacy.html` — availability note under "Push is optional" + date bump;
  the design-behavior sections stay as the contract for when a subscribing build
  ships.
- `web/src/privacy/claims.ts` — `PUSH_AVAILABILITY_NOTE` export with the same
  wording; the machine-verified claim set is thereby scope-annotated
  send-path-only.
- `src/server.ts` — comment-only truth fix on the `/sw.js` route block
  (`/registerSW.js` is no longer emitted; the route stays for
  forward-compatibility and now 404s; `/sw.js` STAYS served so clients that
  registered the old precaching worker can fetch the kill-switch and uninstall).

## B2 (rm-138) — precache strip

Same vite.config.ts edit as B1: empty glob, no manifest transforms. Build
output: `precache 0 entries (0.00 KiB)`; `web/dist/` no longer contains
`registerSW.js`; the emitted `sw.js` is the pure kill-switch (install →
skipWaiting; activate → purge caches → unregister → navigate clients away).
The vite-plugin-pwa plugin itself is RETAINED as the injectManifest compiler
for `sw.ts` (hand-written SW needs a bundler step; removing the plugin would
require replacing that step — recorded as the evaluated-and-declined option).

## B3 — test truth (closes the CI-masking gap)

- `tests/visual/dashboard.spec.ts` — the `/sw.js` + `/registerSW.js` stubs that
  MASKED the auto-injection regression are removed; the operator-view test now
  asserts `navigator.serviceWorker.getRegistrations()` is EMPTY after load.
- `test/static-assets.test.ts` — new `rm-228` describe: built `index.html`
  carries no `vite-plugin-pwa:register-sw` tag / no `registerSW.js` reference /
  no `/sw.js` script tag; `web/dist/registerSW.js` absent (ENOENT) while
  `web/dist/sw.js` stays emitted; the SERVED `/` shell carries no loader tag.
  Stale registerSW note rewritten to post-strip reality.

## rm-106 re-scope rider

ROADMAP rm-106's acceptance clause "one live verified delivery to the
operator's subscription" is re-scoped to digest-only (gateway-log-verified
dispatch) until a push-capable client ships; the claim set stays annotated
send-path-only. Recorded as a dated signal on rm-106.

## Verification (this phase, focused budget)

- `pnpm build:web` — `precache 0 entries (0.00 KiB)`; generated `dist/sw.js`
  only; grep proofs: `vite-plugin-pwa:register-sw` 0×, `registerSW` 0× in
  built `index.html`; `registerSW.js` absent from `web/dist`.
- `npx vitest run --config web/vitest.config.ts` (4 touched suites:
  capability, notifications-copy, Notifications view, claims) — **509 passed**.
- `npx vitest run test/static-assets.test.ts test/operator-ui.test.ts` —
  **171 passed** (includes the new rm-228 regression describe).
- `npx eslint` on all changed non-ignored files — **0 errors** (the two
  intentionally-ignored files surface only pre-existing style under
  `--no-ignore`).
- `pnpm check-types` (server + web + .opencode tsconfigs) — **EXIT=0**.
- Visual suite itself requires the pinned Playwright container; the
  registration assertion is code-reviewed here and the equivalent
  built-artifact proof is machine-enforced by the static-assets regression
  (which fails the suite if the tag ever returns).

## ROADMAP

rm-228 and rm-138 → `status: implemented 2026-09-26 … pending landing`
(this batch); rm-106 gains the re-scope dated signal. Roadmap patch
`a033a4bfb…-roadmap.patch` applied on top of tip `e407953` first.
