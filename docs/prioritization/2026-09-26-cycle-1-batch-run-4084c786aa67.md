<!-- managed by conductor prioritize attempt 5032cda0e801410aa92fd3d0d33c562f (run 4084c786aa67426ba0ce3402cf4d8c05) — cycle-6-frame batch selected 2026-09-26 against live main e407953 (see Frame-vs-main audit below) -->

# Dashboard maintenance — cycle-1 (fleet run 4084c786) batch (2026-09-26)

Selected at prioritize attempt `5032cda0e801410aa92fd3d0d33c562f`. Theme: **client-facing
truth + fail-closed riders + drift alerting** — everything below is verified open on live
`origin/main` (e407953) AND implementable end-to-end in this run's worktree (base 7809df6)
with local evidence (vitest / actionlint / `pnpm lint`).

## Frame-vs-main audit (selection methodology)

This run's frame (7809df6, 2026-09-22) is **57 commits behind** live main (e407953, 2026-09-25)
— cycles 6..17 of the parallel fleet landed while this run's assess/research/roadmap phases ran.
Per the live-tip re-audit convention (id-collision recurrence class), every roadmap-phase
candidate was re-verified against `origin/main` BEFORE batch selection:

| roadmap-phase item (frame id) | live-main status | disposition |
|---|---|---|
| absorb pure-dep drift (rm-134@frame) | LANDED — main pins agent@v0.115.1, codeql@2892aa5, pnpm@11.27.1, eslint 10.11.0 | excluded |
| visual.yaml digest pins (rm-135@frame) | LANDED — main visual.yaml: checkout v7.0.1/setup-node v7.0.0/upload-artifact v7.0.1 all digested (rm-137-pins-half) | excluded |
| visual baseline stability (rm-136@frame) | LANDED — visual runs ubuntu-24.04 + playwright:v1.63.0-noble container (rm-142, rm-148) | excluded |
| public posture review (rm-137@frame) | LANDED — docs/runbooks/security-posture.md (rm-143) | excluded |
| fetch deadline/concurrency (rm-139@frame) | LANDED — aggregator "Outbound deadline machinery" + stall watchdog (rm-203) | excluded |
| gateway session memoization (rm-138@frame, half) | IN-FLIGHT — PR #196 (OPEN) implements the 15s-TTL single-flight session cache (rm-222) | excluded (double-work) |
| listener-poll resilience (rm-138@frame, other half) | **OPEN** — main web/src/App.tsx:43-48 still swallows `!res.ok` silently | **B1** |
| riders: random-fallback/cache-eviction/header-truth/validation-parity (rm-140@frame) | **OPEN** (three verified live; one re-verify at implement) | **B2** |
| rm-127 (standing, 56.0) | **candidate** | **B3** |
| rm-103 (standing, 85.0) | **candidate** — no upstream-drift workflow exists (only base-drift.yaml/canary.yaml) | **B4** |

NOTE for the fold: the roadmap-phase additions `rm-134..rm-140` were authored in the frame's
id-space and COLLIDE with main's landed meanings of those ids (main's rm-134..rm-140 are the
2026-09-22 cycle items). At fold time they renumber per the standing integrate convention
(docs unit only); this doc references items by TITLE + frame-id to stay unambiguous.

## Batch

### B1 — Listener-poll resilience (web surface)
- item: "Gateway session memoization + listener-poll resilience" — POLL HALF ONLY (frame rm-138;
  the session-memoization half is PR #196's, do not touch)
- verified open: `web/src/App.tsx:43-48` (live main) — `pollUnreadCount` does `if (res.ok)
  {setUnreadCount(...)}` and nothing else; every failure class maps to silence;
  `web/src/api/listener.ts` folds `!res.ok` into a generic 'network' error; the 30s interval
  (App.tsx:53) keeps firing regardless of response class or view state
- work: distinguish 401 (surface auth-expiry / trigger re-auth flow), 404 (listener surface
  unmounted server-side — stop polling), and transient failures (capped backoff, keep-alive);
  stop polling when the listener view is unmounted; tests in `web/src/App.test.tsx` covering
  all three response classes + poll stop + backoff ceiling
- acceptance: no response class is silently swallowed; a 404 stops the interval; a 401 is
  surfaced to the operator (not folded into 'network'); transient failures back off with a cap;
  `pnpm test` green with the new class-coverage tests
- collision risk: LOW — web/ listener surface untouched by in-flight PR #196 (auth-path latency)

### B2 — Fail-closed riders batch (server/web small defects)
- item: "Server and web hygiene batch 4" (frame rm-140), all verified live on main:
  - `web/src/push/subscribe.ts:238-243` — `mintIdempotencyKey` still falls back to
    `Date.now + Math.random` (predictable); make it fail closed (throw in the fallback case)
    with a test pinning the failure mode
  - `src/github/installations.ts:153-176` — `tokenCache` still an unbounded Map; entries expire
    on re-access only and departed installations are never evicted; add a bounded sweep
    (TTL expiry + absence cleanup) with tests
  - `scripts/should-release.ts` — code hard-releases `web/` and `public/` (:165-172 live) but
    the header comment's decision-logic list still omits them; sync the header (docs-only)
  - `src/github/metadata.ts` — public-entry empty-string parity with the redacted branch's
    `length > 0` discipline; RE-VERIFY against the live tree at implement time (main reworked
    the deny-key path 2026-09-24; fix only if still asymmetric)
- acceptance: each rider landed with its pinning test; `pnpm lint`/`pnpm check-types`/
  `pnpm test` green
- collision risk: LOW-MEDIUM (installations.ts/metadata.ts saw recent cycles — re-derive at
  implement; converge by content)

### B3 — rm-127: gateway-redirect topology made explicit
- item: standing rm-127 (reliability, 56.0, candidate since 2026-09-20)
- verified open: `src/server.ts` gateway mode redirects unauthenticated non-public paths to the
  relative `GATEWAY_LOGIN_REDIRECT` the dashboard never serves itself; the same-origin-proxy
  assumption is silent — misdeployment yields a browser-terminating redirect loop with no
  server-side diagnostic
- work: startup config validation of the documented gateway-origin/proxy topology (preferred —
  fail-fast with a log line) or an absolute redirect built from the configured gateway origin;
  a gateway-mode unauthenticated test; a seeded misconfigured-startup test
- acceptance: per the roadmap item's acceptance — assumption explicit, one config shape's
  unauthenticated flow tested, misconfigured startup fail-fasts visibly
- collision risk: LOW (candidate since 2026-09-20, untouched by in-flight work)

### B4 — rm-103 first half: upstream-drift alert workflow (alert-only)
- item: standing rm-103 (reliability, 85.0) — the drift ALERT half only; automerge stays
  sequenced behind rm-116 (rm-146's standing reason: automerge on unprotected main automates
  the three recorded red landings)
- rationale (lived pain): THIS run just measured the failure mode — a frame 57 commits behind
  main and an absorb that had already landed elsewhere; nothing alerts on upstream drift today
  (main has base-drift.yaml for the docker digest and canary.yaml for the GraphQL query —
  nothing watches autonomy-upstream)
- work: scheduled workflow comparing HEAD to `autonomy-upstream/main` (read-only checkout of
  the public upstream + `git rev-list --count` + on-drift alert/issue, digest-pinned actions,
  single-quote YAML per repo lint); actionlint container-form validation; a workflow-statics
  guard test if the class warrants one
- acceptance: actionlint exit 0; alert-only (no write path to main); first scheduled fire is
  post-landing evidence (time-boxed watch, canary.yaml precedent)
- collision risk: LOW (no such workflow on main)

## Deferred (explicit, with reasons)

- rm-226 availability half — adjacent to in-flight PR #196's gateway auth path; sequence after
  its landing
- rm-107 monitoring-consumer — product decision gate; web/src/App.test.tsx:73-85 regression-
  guards against a monitoring view until that decision
- rm-116 (branch protection) → rm-146 (automerge) — repo-admin sequencing decision, not a code
  batch; fourth probe: protection object exists but required checks EMPTY (one call from core)
- rm-112 view consumption — rides the rm-107 decision
- rm-141 / rm-187 / rm-183 / rm-117 / rm-118 / rm-114 — medium items, next cycles' core
- rm-104 — fleet-render hardening (external repo)
- rm-102 — dependabot first-PR watch (external timing, ~2026-10-03)
- 2026-09-28 scheduled first fires (canary 05:23Z, base-drift 04:13Z) — watch, not work
