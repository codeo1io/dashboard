# Prioritization batch 2026-09-24 — repo lineage cycle 10

Provenance: conductor run 3538ec96ece446b18ca438b9e485928f, prioritize attempt
6cf9420ddb834e7d829c6adcc66ed4d8, repository-maintenance campaign 32d16713 cycle:1 (engine label).

Landing-merge note (2026-09-24, run c2ae28ab integration, conflict case 3a504ba7): renamed from
`2026-09-24-cycle-10-batch.md` — a second parallel cycle-10 run (c2ae28ab) landed its own batch doc
under the canonical name, and its in-tree rm-164/rm-165/rm-166 registry ids (cross-referenced in its
AGENTS.md/src/session.ts/tests) win per the landed-meanings-win rule. This run's colliding ids
renumber with the doc: rm-164→rm-167, rm-165→rm-168 (see the ROADMAP header manual-revision).

Lineage numbering (disclosed per convention): engine labels this run cycle-1 of campaign 32d16713, but repo
docs carry landed batches cycle 1–9 at origin/main 82312f7, the filename
`docs/prioritization/2026-09-24-cycle-1-batch.md` is already claimed by open PR #43 (b0fc297, parallel
campaign run 6cbc2a1d, merge-base d73fbe7 — its ROADMAP also claims rm-155..rm-163, see B0), and the
cycle-9 label itself collides with PR #45's title per cycle-9's own post-hoc note. Taking lineage
**cycle 10** avoids both collisions at authoring time; the run id `3538ec96…` is the disambiguator.
This doc is authoritative for this run's implement phase.

## Live frame re-measurement (stale-base rule)

- `git fetch origin main` this phase (2026-09-24): `origin/main` = `82312f7`, unchanged since the roadmap
  phase; worktree HEAD `7809df6` (18 behind). All of this run's inputs were already currency-checked at
  82312f7 item-by-item (assess `git show 82312f7:` per finding; assess F7 records the three candidates
  already fixed there).
- **Self-hosted prose re-measured LIVE at 82312f7** (relevant to B2): trio `vitest.config.ts:10`,
  `web/vitest.config.ts:13`, `test/dockerfile-context.test.ts:12`, plus one hit each in
  `.github/workflows/main.yaml`, `.github/workflows/release.yaml`, `AGENTS.md` (codeql.yaml clean — part of
  rm-148's quartet landed via the 82312f7 wave). Cycle-9's batch had measured the trio absent at `763c1fa`;
  the landing wave re-introduced them. Rule: re-measure at implement time before sweeping.
- **In-flight payloads that gate sequencing (de-dup + order):**
  - PR #43 (b0fc297): listener routes ±94, `src/server.ts` +55 (security.txt, **CSRF ack tokens**, pnpm
    11.27.1 in packageManager + Dockerfile corepack, visual.yaml `self-hosted`→`ubuntu-latest` flip,
    pnpm-workspace guard solution doc, ROADMAP rm-155..163).
  - PR #45 (943d3ed): `src/github/app-client.ts` + `src/github/aggregator.ts` (transport timeouts + cache
    removal = rm-112's fix-half), client polling hardening, badge, ROADMAP.
  - PRs #13/#14/#15 (cycle-7 payloads): rm-135's 18-file superset incl. rm-148's quartet rewrites.

## Inputs used, not redone

assess 1a9c85ac (gates green at base: types/lint/2037+1078 tests; F1 limiter, F3 dead monitoring surface,
F4 cache, F6 prose, F8 stale-frame evidence), research 501c5ddc (ideas 1–8 with ecosystem dates: TS 7.0.2,
vitest 5.0.1, Node 26 LTS 2026-10-28), roadmap 1c078a48 (rm-167/rm-168 minted + 7 signal appends; patch at
/tmp/roadmap-update/roadmap.patch, lint-gated).

## Gated scoring (impact, risk-to-skip, risk-to-do, effort, dependencies)

Gates: (G1) restores/locks a broken invariant? (G2) completable end-to-end this cycle with hosted-runner CI
evidence? (G3) depends on unlanded or out-of-repo work?

| item | impact | risk-to-skip | risk-to-do | effort | deps | gated |
| --- | --- | --- | --- | --- | --- | --- |
| rm-129 limiter fairness (security 40.0; assess F1 live at main) | 8 — one global 60/min bucket keyed on the proxy's address is shared by /api/healthz, /auth/*, operator and ingest: a flooder on the public class starves the single operator | 7 — the outage mode is operator-visible denial on a single-operator product | 2 — opt-in trusted-proxy keying + path-class budgets; default unset keeps current behavior | S-M | none (PR #43's server.ts hunks are security.txt/CSRF — merge-order touch, not logic overlap) | PASS |
| rm-167 residue guard + live-site sweep (reliability 48.0; assess F6 live at main) | 7 — locks the twice-failed sweep class into CI; prose sites measured live at 82312f7 | 8 — two recorded scope-filter misses (#3255 lessons); recurrence is guaranteed without a guard | 1 — one test + six comment rewrites | S | none; quartet wording mirrors the PR #13–15 payloads (content-equal on merge) | PASS |
| rm-144 property-based SSE/ingest suites (reliability 50.0) | 6 — the ingest contract and SSE parsers are the product's data spine and are covered only by example-based tests today | 5 — parser edge cases are exactly where silent data loss hides | 2 — fast-check is already transitively resolved in pnpm-lock.yaml | M | none | PASS |
| rm-115 CSRF ack tokens (security 30.0) | 7 | — | — | — | **in flight: PR #43 implements ackCsrf with hourly rotation** | NOT selected (de-duped against PR #43) |
| rm-141 bounded fleet refresh (performance 47.0) | 6 | — | — | — | PR #45 owns `src/github/aggregator.ts` + `app-client.ts` | NOT selected (sequence-blocked until #45 lands) |
| rm-112 remainder (reliability 75.0) + rm-107 panel (65.0) + rm-119 gate-health (52.0) | 8 | — | — | M-L | rm-112's cache removal rides PR #45; panel sequenced on rm-112 semantics (cycle-9 table) | DEFER — next-cycle headline once #45 lands |
| rm-103 absorb cadence (85.0) | 6 | — | — | M | wants rm-116; the pnpm rider is already in PR #43; bfra-me v4.32.0 is a single-site micro-rider (B0) | DEFER |
| rm-105 SBOM/provenance (80.0) | 7 | — | — | M | Release-pipeline scope beyond this batch's theme | DEFER |
| rm-157 absorb agent v0.115.0 (72.0) | 7 | — | — | M | G2 fail — unverifiable without the live gateway | DEFER |
| rm-116 branch protection (58.0) | 6 | — | — | S | account-level owner action | DEFER — raise out-of-band |
| rm-127 gateway login-redirect assumption (56.0) | 4 | — | — | S | none — theme-adjacent rider if the B-set shrinks | DEFER |
| rm-108 matrix / rm-139 Node 26 (60.0/24.0) | decision docs | — | — | S | time-gated on the 2026-10-28 LTS promotion | DEFER |
| rm-118 (42.0) / rm-120 (36.0) | 3–4 | — | — | S-M | value gated on gateway work resuming | DEFER |
| rm-138 / rm-140 / rm-146 / rm-147 / rm-159 / rm-168 | — | — | — | — | decision-required / time-gated / sequenced on rm-116 / blocked-external / push-authority / fleet-side | NOT selected |

## Selected batch — theme: fair throttle, true prose, hardened parsers (operator-surface trust)

All units are unblocked, in-repo, test-backed, and untouched by the open PR payloads. Implement order B0→B1→B2→B3.

- **B0 — landing reconciliation + micro-riders (XS).** Apply /tmp/roadmap-update/roadmap.patch reconciled
  by content with PR #43's ROADMAP (its rm-155..163 meanings vs main's landed rm-157/rm-159 — renumber the
  PR-side ids at merge, landed meanings win; this run's rm-167/rm-168 stand). Place the research doc at
  `docs/ideation/2026-09-24-repository-extensions-ideation.md` and this batch doc at
  `docs/prioritization/2026-09-24-cycle-10-batch-run-3538ec96.md`. Optional micro-rider: bump the single bfra-me
  workflow pin to v4.32.0 (rm-103 signal). Gate: `pnpm lint` on all landed md.
- **B1 — rm-129: trusted-proxy re-key + path-class budgets.** `src/server.ts` limiter: (a) opt-in
  trusted-proxy mode (env/config) that keys the bucket on the X-Forwarded-For first hop **only** when the
  direct peer is the configured proxy — never trusts XFF otherwise (assess F1's spoofing note); (b)
  always-on per-class budget split — public (`/api/healthz`, `/`, `/auth/*`), operator, ingest — so a
  flooder on the public class cannot consume the operator's bucket even in standalone mode. Tests: flood
  the public class → operator route still 200s; XFF ignored when peer untrusted; default-unset behavior
  byte-identical to today. Acceptance and evidence per rm-129 in ROADMAP.md. Effort S-M.
- **B2 — rm-167: filter-free prose-residue guard + live-site sweep.** New
  `test/prose-residue-guard.test.ts` mirroring `test/fork-exclusion-guard.test.ts`: sweeps the repo tree
  with NO file-extension filter and NO path-scope filter; the only exclusions an explicit,
  comment-justified allowlist (`docs/solutions/`, `docs/prioritization/`, `docs/ideation/`, `.agents/`,
  `.slim/`, `node_modules/`, `web/dist/`, `.git/`, `.conductor/`, `pnpm-lock.yaml`); terms target live
  claims about THIS repo's runners/tooling (self-hosted, wiki-writer, renovate-ownership) with external
  topologies (the gateway runbook) named as legitimate; one exported term-constant array; negative
  verification documented in-file (inject a violation in a scratch copy → guard reds). Same payload fixes
  the sites live at implement-time re-measure — authoring-time set: `vitest.config.ts:10`,
  `web/vitest.config.ts:13`, `test/dockerfile-context.test.ts:12` (rationale becomes runner-agnostic:
  tight under CI load), `main.yaml:132`, `release.yaml:438`, `AGENTS.md:47` (wording mirrors rm-148's
  pending PR #13–15 payloads, content-equal on merge). Acceptance per rm-167. Effort S.
- **B3 — rm-144: property-based suites for the data spine.** Add `fast-check` as a devDependency (already
  transitively resolved in the lockfile). Three suites: (a) listener ingest contract decode — malformed and
  adversarial frames always land in the Result-shaped error path, never throw, never mutate the store;
  (b) SSE stream parsers (operator + listener) — arbitrary chunk boundaries and interleaved events parse
  without loss or reorder; (c) aggregator delta invariants — add-only within a cycle and the driftCount
  repo-identity lock from rm-126 holds under generated sequences. Acceptance per rm-144; effort M — the
  batch's largest unit, scoped to two parser surfaces plus one invariant suite.

**Confidence (ce-plan pipeline mode):** high on B1/B2 (direct live evidence, small well-precedented
surfaces); medium-high on B3 (new devDep + property-test authoring, scoped to stay M). In-thread
substitution disclosed per the ce-plan fence: pi exposes no subagent tool, so no independent scorer or
reviewer context ran — scoring and the doc-review pass below are this one context's work, not independent
corroboration.

**Non-interactive doc review (in lieu of ce-doc-review):** re-read found no absolute paths in doc body
references, rm-ids consistent with /tmp/roadmap-update/ROADMAP.updated.md (rm-167/rm-168 minted above the
all-lineage max rm-163), dates 2026, and the deferred table covers every open roadmap candidate plus the
PR-de-duped pair.
