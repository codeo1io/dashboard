# Prioritization batch 2026-09-24 — repo lineage cycle 12

Provenance: conductor run 6fe2697291c74e1e8a184110c804a0f5, prioritize attempt
d78791e055ab42f9b77434f7ee20875d, repository-maintenance campaign 83e4b501 cycle:1 (engine label).

Lineage numbering (disclosed per convention): engine labels this run cycle-1 of campaign 83e4b501, but repo
docs carry landed batches cycle 1–10 at origin/main 539c632, and the `2026-09-24-cycle-11-batch.md` filename
is already claimed in-flight (branch conductor/run-b4aac9ba84e6, id-max rm-176). Taking lineage
**cycle 12** avoids that collision at authoring time; the run id `6fe26972…` is the disambiguator.
This doc is authoritative for this run's implement phase.

## Live frame re-measurement (stale-base rule)

- `git fetch origin main` this phase (2026-09-24): `origin/main` = `539c632`, unchanged since this run's
  assess and roadmap phases; worktree HEAD `7809df6` (18 behind), carrying exactly ` M ROADMAP.md`
  (this run's roadmap deliverable: rm-177..rm-180 + signal appends, built on the 539c632 base) plus
  untracked `.conductor/`. All line evidence below derives from the origin/main tree per the
  truth-scoping rule.
- Both of this run's new criticals re-verified un-owned at authoring time by direct blob inspection of the
  open lineages, not by assumption: `git show origin/conductor/run-a4a97e30f3a2:src/github/aggregator.ts`
  carries both `// GraphQL max page size` comments (at its shifted :139/:177) and its
  `.github/workflows/base-drift.yaml:48` keeps `docker manifest inspect | awk '/^Digest:/'`;
  `origin/conductor/run-c2ae28ab9962:src/github/aggregator.ts` likewise carries both comments.
- **In-flight payloads that gate sequencing (de-dup + order):**
  - PR #43 (b0fc297, oldest): server.ts security.txt/CSRF, pnpm 11.27.1, visual flip, ROADMAP rm-155..163.
  - PR #45 (943d3ed): `src/github/app-client.ts` + `src/github/aggregator.ts` transport timeouts + cache
    removal (rm-112 fix-half) — **sequence-blocks rm-141 and the rm-112/rm-107/rm-119 family, not this
    batch** (B1's aggregator edits are two comment lines + one param, content-reconcilable with #45).
  - PR #97 (3538ec96 payload, cycle-10 B1–B5): rate limiter, prose guard, property suites — landed-era.
  - PR #82 (c2ae28ab9962, cycle-11): `.github/workflows/base-drift.yaml` toml-override (rm-171) +
    fail-visible enumeration (rm-172) — **B2 shares that file; reconcile by content on landing** (the
    extraction fix and the toml rider touch different lines; both keep intent).
  - PR #104 (a4a97e30f3a2, cycle-10 second wave): rewrites base-drift.yaml (pin-drift job, rm-167) +
    aggregator visibility (its rm-164) — **retains both defects this batch fixes**; on landing, this
    batch's fix wins by content (it is the strictly-correct semantics for the same lines).

## Inputs used, not redone

assess 06462e0a (F1 GraphQL critical, F2 drift-extraction, F4/F7/F8 micro; five workflows green at
539c632; branch protection still vacuous), research fa89b529 (C1 canary with three sourced approaches;
C2/C3 absorbed as rm-157/rm-133 signal deltas; npm/actions surface at latest except two minors),
roadmap 368f26dd (rm-177..rm-180 minted above the verified all-lineage max rm-176; enumeration-gap
deliberately not re-minted — doubly in-flight as PR #82 rm-172 + PR #104 rm-164).

## Gated scoring (impact, risk-to-skip, risk-to-do, effort, dependencies)

Gates: (G1) restores/locks a broken invariant? (G2) completable end-to-end this cycle with hosted-runner CI
evidence? (G3) depends on unlanded or out-of-repo work?

| item | impact | risk-to-skip | risk-to-do | effort | deps | gated |
| --- | --- | --- | --- | --- | --- | --- |
| rm-177 GraphQL query syntax (reliability 97.0; assess F1, live-proven at main) | 9 — the product's core data path fails on every per-repo fetch: whole board serves stale/unknown with zeroed counts since 2026-09-20 while CI is green | 9 — silent by construction (faked-transport tests); every hour unfixed is an hour the operator reads a lie | 1 — two comments become `#`-form + one additive guard test; no behavior change to valid code | S | none (PR #45's aggregator hunks are transport/cache — comment lines only overlap; PR #104 carries the bug) | PASS |
| rm-178 base-drift extraction (reliability 91.0; assess F2, live-proven at main) | 7 — the only base-drift detector has never once passed; every red is a false positive | 8 — first cron fire Mon 2026-09-28 04:13Z is guaranteed false-red and will keep firing weekly as noise, training ignore-the-gate | 2 — workflow-only change; extraction swap + empty→extraction-error; actionlint-gated | S | none (PR #82/#104 share the file; content-reconcile on landing, this fix is the correct semantics for those lines) | PASS |
| rm-179 reality canary (reliability 71.0; research C1) | 7 — locks the green-CI-broken-reality class (rm-177 + rm-178 are its two instances) out of recurrence | 6 — without it, the next transport-contract break ships exactly as silently as this one did | 2 — GITHUB_TOKEN-only read-only scheduled query; structural guard (its (c) half) rides B1's acceptance | S-M | (a)-approach needs a public repo coordinate — this repo itself satisfies it | PASS |
| rm-180 code-truth micro (docs 23.0; assess F7/F8) | 3 — dead param with response-masking precedence; env-parse lenience vs documented contract | 3 | 1 — same files as B1 (aggregator.ts) + one server.ts line; tests ride B1's suite | S | none | PASS (rider on B1) |
| rm-104 render hardening (reliability 98.0) | 7 | — | — | M | G3 fail — the fix lives in the fleet's hermes-roadmap generator, out-of-repo; the in-repo guard-test half landed cycle-10 | NOT selected |
| rm-102 dependabot first-PR (90.0) | 6 | — | — | — | in-progress by design — window opens ~2026-10-03 | NOT selected (waiting on evidence, nothing to build) |
| rm-112 remainder (75.0) + rm-107 panel (65.0) + rm-119 (52.0) + rm-141 (47.0) | 6–8 | — | — | M-L | PR #45 owns aggregator/app-client | NOT selected (sequence-blocked until #45 lands) |
| rm-126 drift-surface decision (73.0) | 5 | — | — | M | decision-heavy (operator-visible semantics + `/api/monitoring` consumer) — next-cycle candidate with B3's canary as fresh evidence | DEFER |
| rm-157 agent v0.115.0 absorb (72.0) | 7 | — | — | M | G3 fail — needs the live gateway; signals already carry the #1656 provenance payload | DEFER |
| rm-116 branch protection (58.0) | 6 | — | — | S | account-level owner action; mid-fleet enablement with 5 open PRs risks wedging every landing — revisit when the PR queue drains | DEFER (raise out-of-band) |
| rm-127 gateway redirect (56.0) | 4 | — | — | S | theme-adjacent; batch is full | DEFER |
| rm-139 Node 26 (24.0) / rm-140 pnpm 12 / rm-108 matrix | decision docs | — | — | S | time-gated (2026-10-28 LTS promotion) | DEFER |
| enumeration-gap visibility | 6 | — | — | M | **in flight: PR #82 rm-172 + PR #104 rm-164** | NOT selected (de-duped) |
| rm-168/rm-169–style riders (PR #97/#104 leftovers), rm-150+ visual-fragility family | 3–4 | — | — | S-M | gated on rolling-image decision | NOT selected |

## Selected batch — theme: restore the true signal (make the dashboard's reality checkable)

All units are unblocked, in-repo, test-backed or dispatch-verified, and none duplicate an open PR's
payload. Implement order B0→B1→B2→B3. The shared thread: today the dashboard reports calm while its data
path is broken (B1), its drift gate cries wolf (B2), and nothing would catch either class next time (B3).

- **B0 — landing reconciliation rider (XS).** Land this run's already-staged `ROADMAP.md` (rm-177..rm-180,
  signal appends, header note) reconciled by content with whatever of PR #43/#82/#104 has landed by
  implement time — landed meanings win ids, in-flight claims renumber at merge (roadmap phase verified
  the all-lineage max is rm-176). Place this batch doc. Research evidence lives in the research
  PhaseResult JSON + ROADMAP signal appends (no separate ideation doc this cycle — C2/C3 were absorbed
  as deltas, C1 became rm-179). Gate: `pnpm lint` on all landed md; ROADMAP item-ids unique.
- **B1 — rm-177: GraphQL query syntax fix + structural guard (+ rm-180 rider).** Convert the two
  `// GraphQL max page size … (rm-110)` comments at `src/github/aggregator.ts:131/:169` (origin/main
  numbering; `#`-style inside the template or hoisted outside — the rm-110 ceiling note survives in
  valid form, one truth). Add `test/query-shape-guard.test.ts`: imports every exported query template
  and asserts none contains a `//`-prefixed line (negative-verified: inject a violation in a scratch
  copy → guard reds; documented in-file). Rider rm-180: remove parseRepoResponse's dead third param
  (both call sites pass null) or pin its intended precedence with a test — removal preferred; and
  settle envIntOrDefault semantics (tighten to whole-string parse per the documented invalid-fallback
  contract, adding the `12abc` test case). Acceptance/evidence per rm-177 + rm-180 in ROADMAP.md.
  Effort S.
- **B2 — rm-178: base-drift digest extraction that can pass and can fail honestly.** Replace the
  `docker manifest inspect | awk '/^Digest:/'` extraction (`.github/workflows/base-drift.yaml:39`,
  PR #104's :48) with the repo's proven convention — `docker buildx imagetools inspect node:24-slim`
  default pretty-dump `Digest:` line (release.yaml:451 form, per the 2026-09-19 digest-parse solution
  doc) — or a token-authed registry HEAD probe reading Docker-Content-Digest (both were proven correct
  from this runner/registry on 2026-09-24). Empty-or-missing extraction exits as `extraction error`,
  never a drift verdict. Re-baseline rm-123's first-fire expectation: green-at-parity (pin 0e0ff40 ==
  live, verified under both Accept headers). Validate with actionlint (container form). A manual
  dispatch's green run is the ci-phase gate evidence, not implement's. Acceptance per rm-178. Effort S.
- **B3 — rm-179: reality canary for the GitHub data path.** The (c) structural guard already rode B1.
  Land the (a) half: `canary.yaml` (or a job in main.yaml) — weekly schedule + workflow_dispatch,
  GITHUB_TOKEN-only, read-only, single step that imports/echoes a hash of each exported query template
  and executes ONE real query (REPO_STATUS_QUERY against `codeo1io/dashboard`'s own coordinates via
  `gh api graphql` or the app client) asserting a 200-class response with no errors array; red means
  "the real API would reject our query today". Log the query-text hash so the evidence channel can
  diff template drift. The (b) offline `graphql validate()` variant stays a recorded alternative, not
  this batch. actionlint-gated; first-fire green is ci-phase evidence. Acceptance per rm-179. Effort S-M.

Out of scope this cycle, explicitly: rm-116 branch protection (owner action; revisit when the PR queue
drains), rm-126 (decision-heavy; B3's canary feeds it), any aggregator/app-client semantics beyond the
two comment lines and the dead param (PR #45's territory), and the 2026-09-28 cron red (expected and
meaningless until B2 lands — treat as confirmation, per the roadmap stewardship note).
