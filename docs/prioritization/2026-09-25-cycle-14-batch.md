# Prioritization batch 2026-09-25 — repo lineage cycle 14

Provenance: conductor run 7cf68feaa1b9458299cdbda8235cf2e6, prioritize attempt
f420abfb82e84bc393cc932e08e199ee, implement attempt c3e0acfc76b24f978bf5ecdb42cf2fbb,
compound attempt 63045a096f47427ba9f32590978fdc36, repository-maintenance campaign
ebc911b1 cycle:1 (engine label).

Lineage numbering (disclosed per convention): the engine labels this run cycle:1
of campaign ebc911b1 — the SAME campaign label carried by the already-landed
run 4e7c674 batch (`2026-09-25-cycle-1-batch.md`, landed at 6fd7d04 behind
be2ad13 + integrate). Repo docs carry landed lineage batches cycle 1–13 (cycle 13
in two parallel landed waves: `2026-09-25-cycle-13-batch.md` + `-batch-2.md`).
Taking lineage **cycle 14** avoids collision with both; the run id `7cf68fea…`
is the disambiguator. This doc is authoritative for this run's batch.

Status at authoring: IMPLEMENTED + PRE-REVIEW VALIDATED. Review and shipping
outcomes are deliberately NOT recorded here — the next cycle's assessment
carries them forward.

## Batch theme: make the red checks and the listener tell the truth

| Unit | Id | Surface | Outcome |
|------|----|---------|---------|
| U1 (lead) | rm-192 | failingChecks drill-down | implemented: GraphQL selections + extractFailingCheckDetails + /api/monitoring DTO + Monitoring.tsx view (Runs nav) |
| U2 | rm-197 | .dockerignore artifact seal | implemented: test-results/playwright-report/.pnpm-store + any-level guard matcher |
| U3 | rm-198 | listener parse-drift visibility | implemented: droppedCount + drift notice |
| U4 | rm-199 | listener prune-overflow visibility | implemented: prunedCount through DTO + aged-out notice |
| U5 | ledger | ROADMAP re-derivation | landed in-worktree: rm-197..199 minted above tip ceiling rm-196; rm-192/rm-186 flipped implemented; rm-190 relocated out of Completed |

Net: 20 modified + 3 new files, +630/−35 at base 6fd7d04, left uncommitted per
the fleet implement convention.

## Pre-review validation (consumed by compound, not re-run)

- Targeted (attempt a8e86e55): the work-order targeted command self-escalated
  (package.json in the changed set → shared-build-config rule) to the
  authoritative fallback — ephemeral PR #149, commit b60b840a: ALL 10 cloud
  checks SUCCESS.
- Full (attempt 1de6bee6): full_command verbatim — ephemeral PR #151, commit
  e0bf52a: ALL 10 cloud checks SUCCESS, independent of #149 (fresh snapshot
  base + fresh run/job IDs).
- Zero fixes needed in either validation phase; tree fingerprint 16ab2da0a0a77519
  identical across implement → targeted → full (no executable drift).
- Local gates at the same fingerprint (implement phase): server 2180 + web 1088
  tests green, check-types clean, lint clean, graphql canary live-green
  (query sha256 732438f0…, exit 0).

## Cycle lessons (compounded)

1. **F1 false positive (assess → prioritize correction).** The assess P2 claim
   "root-anchored node_modules misses web/node_modules" was disproven: the
   entry is UNANCHORED, so under dockerignore any-level semantics the nested
   path was already excluded. Prevention rule + any-level guard matcher lesson:
   docs/solutions/best-practices/dockerignore-unanchored-pattern-false-positive-2026-09-25.md
   (new this cycle). The surviving true half (artifact paths) became rm-197.
2. **Mid-run tip movement invalidates spool-minted ids.** The roadmap phase's
   spool artifact minted rm-191..193 above the then-ceiling rm-190; PR #140's
   landing mid-run took rm-191..196 with different meanings. Implement
   re-derived the ledger at the live tip (ceiling rm-196) and minted rm-197+.
   Rule for every future implement phase: re-verify the all-lineage id ceiling
   at implement time; never apply spool-artifact ids unverified (the ROADMAP
   mint-notes in rm-190/rm-197 record this pattern).
3. **Date-token id traps persist.** The apparent all-lineage ceiling rm-202 was
   the word-boundary false positive `platform-2026` (documented in rm-190's
   mint note, re-encountered by this run's roadmap phase). Always grep
   `rm-\d+` with strict token boundaries before minting.

## Integrate collision map (for the fold)

Open PRs carrying divergent versions of batch surfaces: #136 (.dockerignore +
dockerfile-context + canary + query-shape-guard + aggregator — much larger
.dockerignore rewrite), #113 (listener store), PRs #104 + #45 (aggregator family; #45 ships the
createBoundedFetch/AbortSignal.timeout transport, the dedupe target for the
data-plane deadline lineage). Reconcile by content, per house convention.

## Next-cycle candidates and context (concrete)

- **rm-194** (approvals aging/ordering) — natural follow-on; the U1 pattern
  (Monitoring view + DTO + pinned inline snapshot test) is established for reuse.
- **rm-190** (fixture completeness) — relocated back into the open queue by U5;
  the corrective incident is citable from run 11d3a522 breadcrumbs.
- **rm-196 rider** — hono 4.13.9 + vite 8.3.1 maturity timestamps passed;
  `pnpm update hono vite` + vite pin 8.3.0→8.3.1 is a ready micro-batch.
- **rm-157 absorb guidance** — take fro-bot/agent v0.115.1 (one patch above the
  payload's v0.115.0); upstream codeql-action main digest fa8392b is newer than
  the pending 2892aa5 rider.
- **rm-139 window** — node 24 support ends 2026-10-20; node 26 LTS starts
  2026-10-28: an 8-day pinch where no supported-LTS overlap exists. Plan the
  toolchain move before the window.
- **rm-133 re-eval** stands at 2026-10-21 (typescript-eslint peer range still
  <6.1.0; TS 7 still blocked).
