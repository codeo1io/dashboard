# Dashboard maintenance — cycle-18 batch, run 743f2e47fc3b4f6d992986d5a59c3338 (2026-09-26)

Repository-maintenance cycle:1 for this campaign (run 743f2e47); the
chronological cycle-18 slot follows 2026-09-25's cycle-17 compound, hence the
run-suffixed filename. Selected by the prioritize phase from the live open
ledger at origin/main **e407953** (re-verified unmoved this phase), this run's
assess (447aedf6) and research (ba318003) outputs, and the roadmap phase's four
new items (rm-228..231), under three constraints: the 2026-09-28 05:23Z canary
first-fire deadline, surface-collision discipline against the one fresh live
landing lineage (PR #196), and end-to-end completability (nothing that needs an
external secret, a gateway, or an org-side webhook subscription).

## Selection table

| candidate | impact | risk if skipped | effort | external deps | verdict |
|---|---|---|---|---|---|
| rm-192 failingChecks drill-down (p68) | high — a red repo names the failing workflow + attempt instead of a count | the staged-lead convention erodes if skipped a second time | medium | none | IN (B1) |
| rm-228 canary credential parity (p67) | high — the canary's first scheduled fire (2026-09-28 05:23Z) is predicted scope-class red | the weekly drift signal is born indistinguishable from the break it watches for | small | none for the code half; dispatch-proof belongs to the CI gate | IN (B2) |
| rm-229 fleet Scorecard columns (p34) | medium — fleet security posture at zero auth (live-proven 7.8 / 8.1 on 2026-09-26) | posture stays invisible; the API is public and inert to adopt | medium | none | IN (B3) |
| rm-231a dead `UPDATE_BASELINES` lever (p25 half) | small — an operator-flippable env that does nothing, in the visual gate | dead-lever trap persists | trivial | none | IN (B4) |
| rm-208 unread badge freeze (p32, queued by cycle-17) | high operator value | badge keeps freezing on auth expiry | medium | none | OUT — surface collision: PR #196 owns App.tsx / Listener.tsx / api/listener |
| rm-107 composed status panel (p65, five recurrences) | high | sixth recurrence | large | none | OUT — sequenced after rm-192's DTO/Monitoring groundwork; next-cycle lead |
| rm-227 license/release columns (p30, sibling of B3) | medium | columns stay absent | small | minted installation-token probe | OUT — its acceptance carries a probe no in-repo credential can run |
| rm-230 webhook-poked refresh (p43) | medium-high red-detection latency win | poll latency stands | medium | WEBHOOK_SECRET + org webhook subscription | OUT — not completable end-to-end this cycle |
| rm-199 C3 feed / C5 trend substrate | decision-gated by the item's own acceptance | — | small | a decision, not a dep | OUT — decision-gated |
| rm-157 open core (p72) | gateway contract surface | — | medium | live gateway | OUT — gateway-gated |
| rm-108 / 133 / 139 / 140 majors | toolchain majors | — | large | peer-toolchain gates (typescript-eslint peer still blocks TS7) | OUT — calendar-gated (2026-10-21 / 2026-10-28 windows) |
| strand reconvergence + PR triage | operational truth | strands keep misleading the mint census | small | — | recorded in B0 and next-cycle, not batch code |

## B0 — base + ledger + convergence verdict (rm-165 discipline)

Worktree re-aligned from the stale 7809df6 dispatch frame onto origin/main
e407953 by this run's roadmap phase (ancestor-safe reset, `.conductor/`
preserved; tip re-verified unmoved at prioritize time). rm-192's recorded
convergence precondition is RESOLVED: PR #166 (run fc4bb6a491a2,
`DASHBOARD_MONITORING_REFRESH` gate + bounded refresh) MERGED
2026-09-25T11:18:36Z and its content is at the tip (README rows 95/122) — the
staged lead implements clean on e407953, with no live parallel lineage on its
surfaces (aggregator / query-shape-guard / canary / Monitoring). The canonical
checkout /work/projects/dashboard is still parked on cloud/visual @7809df6 with
one untracked doc; the implement phase resets it to origin/main (preserving the
untracked zz-conflict-lint-check.md) BEFORE batch work, per the recorded
protocol.

## B1 — rm-192: failingChecks drill-down (staged lead honored)

Additive GraphQL only: `checkSuites.nodes.workflowRun { displayTitle runAttempt }`
plus `checkRuns` node `name`/`detailsUrl`; strictly read-only, zero mutations.
RepoCiStatus DTO gains the fields with mapper tests; the SPA renders workflow
title/attempt for red repos with a pinned snapshot test; the exported-query
seam (rm-177) and both registry templates extend to the new selection;
`test/query-shape-guard.test.ts` negative-verified against field removal.
Rationale: p68, staged by run 4e7c674 prioritize 1be0e527 as the next-cycle
lead; its convergence watch (PR #166) is satisfied per B0.

## B2 — rm-228: canary credential parity (time-boxed)

`scripts/graphql-canary.ts:84-86` requires `GITHUB_TOKEN`; the workflow's
permissions block (canary.yaml:18-31) carries no security-events family while
template #1 selects `vulnerabilityAlerts` — and zero canary runs exist to
settle it empirically (`gh run list --workflow canary.yaml` is empty). Fix
shape: scope-aware template selection (template #2 NO_ALERTS under a token
lacking the family, mirroring the aggregator's own production fallback,
rm-170) or the schema-valid permission addition; the stale `checkWorkflowRuns`
comment in the permissions block corrected to name fields a shipped template
actually selects. The dispatch-proof (one `workflow_dispatch`, both templates
green at parity) is deferred to the CI/final gates — dispatching is prohibited
before them. Deadline: first cron 2026-09-28 05:23Z.

## B3 — rm-229: fleet OSSF-Scorecard posture columns

First column-family extension on the repo DTO (rm-227's license/release columns
verified still candidate at the tip — no licenseInfo/latestRelease code in
src/github/), so this batch defines the absent-tolerant column pattern. Source:
public zero-auth `api.securityscorecards.dev` (live-proven 2026-09-26:
codeo1io/dashboard 7.8 analyzed 2026-09-25T13:14:10Z — re-analyzed at that
morning's push; fro-bot/dashboard 8.1). Deadline-bounded fetch in the
aggregator's existing pool; denylisted/private repos fail-soft to no column
(redaction invariant preserved: query only repos already in the public working
set — denylist-before-query discipline); no new runtime deps, no new GitHub
scopes; acceptance includes the public/scored, denylisted/no-column, and
API-down/fail-soft test matrix.

## B4 — rm-231a: dead `UPDATE_BASELINES` lever (truth rider)

visual.yaml:85 sets `UPDATE_BASELINES`; no reader exists anywhere on the tip
(`git grep UPDATE_BASELINES` — that single site; line 83 already passes
`--update-snapshots`). Remove the dead lever (removal is the truth-preserving
default; wiring real behavior would need a README row per rm-214 and has no
wanting operator). The item's Listener.tsx AbortController half (rm-231b) is
DEFERRED: the file is owned by live PR #196, and wiring `abort()` here would
collide at its landing.

## Verification (implement-phase plan)

- focused vitest: aggregator + query-shape-guard + canary-registry +
  Monitoring/dashboard snapshot suites (`pnpm build:web` first — web/dist is
  absent in fresh worktrees)
- `node scripts/graphql-canary.ts` exit 0 with the extended selection
  (read-only live API probe, run locally)
- actionlint container form over changed workflows; `pnpm lint` (yml/quotes on
  any yaml edit) and `pnpm check-types` on touched surfaces
- full suite at the final gates; rm-228's dispatch-proof rides the CI gate

## Next-cycle candidates (2026-09-26)

- **rm-208 (auth-expired badge + stale indicator, App.tsx poll loop)** — the
  moment PR #196 lands; cycle-17 queued it and this cycle defers it on surface
  collision only.
- **rm-107 (composed system-status panel, p65, five recurrences)** — staged as
  the cycle-19 lead once rm-192's DTO/Monitoring groundwork lands.
- **rm-230 (webhook-poked refresh)** — unblocked the moment WEBHOOK_SECRET is
  provisioned and the org webhook subscription exists; then the highest
  reliability add open.
- **rm-227 (license/release columns)** — small and seam-ready; blocked only on
  its minted installation-token probe acceptance; bundle with whatever cycle
  can produce that probe.
- **Strand reconvergence audit** — PRs #155/#157 carry cycle-14/15 content
  already at main via integrates (PRs left open at old bases); #206 likely
  racing-superseded (opened 13:08:14Z, six minutes before the 13:14Z aaa84dff
  integrate); #196 live; CI-validation drafts #210/#67/#36/#33/#28/#27/#26/#25/
  #24/#22/#20/#18 accumulate. Close-sweep before the next mint, per the
  mint-collision protocol.
- **Watch calendar** — 2026-09-28 04:13Z base-drift first cron (green at parity
  expected: live digest equals the pin), 05:23Z canary first fire (rm-228
  predicts scope-class red absent this batch), dependabot window ~2026-10-03,
  rm-133 / rm-139 re-eval windows 2026-10-21 / 2026-10-28 (the latter sits
  mid-rollout of the ubuntu-26.04 alias flip).
