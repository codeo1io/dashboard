# Dashboard maintenance — cycle 15 batch (run 70ba5270, 2026-09-25)

Placed in-repo by the implement phase (B0) from delegate/2e2910cfcf2a4107aede2da6b04fa6b1.batch.md. Lineage slot 15, run-suffixed (parallel lineage 040f1ae0 claimed cycle-16 in-flight; bf5d7753 run-suffix convention); renumber at integrate if cycle-15 lands from another lineage first.

**Routing disclosure.** The compound-engineering router has no `ce-prioritize` skill (full ce-*
listing verified this phase); nearest descriptions (ce-plan = plan-from-requirements, ce-strategy =
STRATEGY.md authoring, ce-sweep = feedback-source sweep) are all broader or off-target for
impact/risk/effort scoring over an existing roadmap. Direct authorship of this selected-batch doc,
following the fleet precedent (040f1ae0 `batch-329782543…`, bf5d7753 `beaa9160…batch.md`,
2026-09-22 `80f064ff…`). No subagent passes specified; scoring done in-thread.

**Inputs (prior phases of THIS run — used, not redone).** assess 34dbdf08 (3 new + 5 carried
findings, all re-verified at tip 20ade64) · research 761db5e1 (8 ranked candidates + registry/GH
evidence) · roadmap 265348bc (rm-213/214/215 minted + 4 dated signals + 7 deduped non-mints,
delivered by diff). Canonical open-items list: origin/main 20ade64 ROADMAP (landed max rm-206).
In-flight landscape measured THIS phase: 23 open PRs; payloads verified by name-only diff for
PRs #155/#166/#157/#136/#113; TWO parallel lineage batches in spool (040f1ae0 cycle-16 theme
"self-governance + trust-boundary hardening" taking their rm-209/210/212 + rm-196 rider;
bf5d7753 "auth-path latency + client/gate truth" taking their rm-207 session-cache + rm-209
client-truth + rm-208 visual-triggers). Base for implementation: origin/main 20ade64 fresh
(dispatch frame 7809df6 = strict ancestor, 0/45; same realignment prep as both parallel batches).

## Scoring (top candidates; impact / risk-of-not-doing / effort / dependencies / strategic)

| Candidate | Impact | Risk | Effort | Deps | Strategic | Verdict |
|---|---|---|---|---|---|---|
| **rm-213 renovate.json5 guard gap** (reliability 40, this run's assess F1) | **H** — the re-injection it would catch EMPIRICALLY RECURRED inside 48h (absent b185335 09-23 → present 7809df6 → cured only at 0616de2 09-25 04:30); fork invariant #rm-136 currently defended by one-off probes, not a test; same class as rm-131/rm-164 guards that already proved their worth | **L** — one assertion + negative check in an EXISTING guard test; zero runtime surface | **XS** | none (no gate, no decision, no external owner) | converts a recurring manual tax into a tested guarantee — the fork's cheapest remaining invariant hardening | **SELECTED (B1 lead)** |
| **rm-214 env-var docs + coverage guard** (docs 22, assess F3 + research C8) | **M-H** — 27 `DASHBOARD_*`/`RATE_LIMIT_*` tokens live in src/ vs README documenting 1; `RATE_LIMIT_TRUSTED_PROXY` misconfiguration silently re-keys who gets throttled; every future operator handoff pays the undocumented-env tax | **L-M** — README + new test only; content-overlaps PR #166's env paragraph (reconcile-by-content, ours subsumes) | **S** | none beyond #166 reconciliation | makes README env truth MACHINE-CHECKED (rm-164 prose-guard style) — durable, not one-shot docs | **SELECTED (B2)** |
| **README:53 healthz wording** (rm-107's wording half, assess carried finding) | **M** — README publicly promises `{ ok, lastFetch, rateLimit }` while routes/api.ts serves liveness-only nulls by design (rm-107); a docs lie about a public endpoint | **L** — one-line truth fix; NOT the gated view decision (that decision is moot below) | **XS** | same README reconcile as B2 | closes the last open half of a 65-priority item for one line | **SELECTED (rider R1)** |
| **rm-215 listener parse-drop visibility** (reliability 26, this run's mint) | M — silent per-message drops vs `unreadCount` mismatch | **M** — HEAD-ON COLLISION: PR #155's unlanded payload rewrites exactly `web/src/api/listener.ts` + `.test.ts`, `web/src/views/Listener.tsx` + `.test.tsx`, `src/listener/contract.ts`+`store.ts` (cycle-14 listener items riding #155) | S-M | effectively gated on #155 landing | real item, wrong cycle — same exclusion 040f1ae0 applied to rm-107 ("re-scope after #155 lands") | **DEFERRED — next-cycle lead candidate post-#155** (its drop-count surface becomes richer on #155's Monitoring view) |
| **C4 operator panel** (research top pick) | was scored H — **SUPERSEDED BY EVIDENCE**: PR #155 carries `web/src/views/Monitoring.tsx` + `web/src/api/monitoring.ts` — the view EXISTS in flight; this run's research "unowned in all open PRs" scanned only #166's diff and was WRONG about #155. Recorded as a research-phase correction; no selection. | — | — | owned by #155 | re-score after #155 lands (rm-107 ledger flip + App.test.tsx:73-85 guard removal ride it) | **EXCLUDED (owned by unlanded #155)** |
| C5 session-cache / client-truth trio / majors / stale-PR / absorb riders | — | — | — | all owned in-flight: bf5d7753's selected batch (their rm-207/209) + rm-108's dated signal + rm-159 + rm-157 | cross-lineage dedupe — three maintenance lineages stack the same repo TODAY; a fourth re-implementation race is pure cost | **EXCLUDED (in-flight elsewhere)** |
| rm-114 SSE (040f1ae0's first alternate), rm-194/195, rm-117/118, rm-205, rm-141 (=#166), rm-192/186/197-199 (=#155), rm-104/102/103/106/116/157-class | — | — | — | alternate-of-another-lineage, post-#155 detail work, live-repo mutation, or unlanded-PR-owned | below cut for THIS lineage's scope | **EXCLUDED (see 040f1ae0's ledger — reasons carry over verbatim)** |

**Why this batch.** (1) **It is exactly the set this run uniquely surfaced** — rm-213/214 were minted
by this run's roadmap phase after four-layer dedupe; no parallel lineage selected them (verified
against both in-flight batch docs + all 23 open PR payloads). (2) **Coherent theme — binding
truth**: every unit converts a documented-but-unguarded claim into a machine-checked one (fork
exclusion, env surface, healthz wording) — the same theme-strand as the rm-131/rm-164/rm-202
guard lineage that has repeatedly paid off. (3) **Fits the constraint envelope**: all units XS-S,
zero decision gates, zero external owners, zero time windows, no head-on PR collisions (only
merge-manageable same-file overlaps with #166, both append-style or section-subsuming); end-to-end
completable under the 45-commit realignment + three-lineage stack — deliberately sized lean
because a fourth lineage over-stacking is itself a landing risk (the exact tax 040f1ae0's theme
names). (4) **Deferral is principled, not scope-shyness**: rm-215 defers on a verified head-on
file collision, recorded with its re-entry condition.

## Selected batch

**B0 (prep, mandatory — same shape as both parallel batches).** Re-align the run worktree to
origin/main 20ade64 (dispatch base superseded; worktree HEAD-only assess deltas already dead at
tip by design). Apply this run's roadmap artifacts byte-identical:
`delegate/265348bc0bfa4cf991eaf75c19c208dd.roadmap-proposed.md` → `ROADMAP.md` (3 items + 4 dated
signals; renumber per the d00d095/8bbff5c6 convention if 40c5bb4d (rm-207..212) or d0d342d7
(rm-207..209) land first — both their proposals and mine are pure-addition over the same base, so
content-reconcile is mechanical). Place this doc at the in-repo path above.

**B1 — rm-213: fork-exclusion guard asserts `.github/renovate.json5` absence** (lead).
`test/fork-exclusion-guard.test.ts`: extend the existing renovate.yaml absence assertion block
(:52-53) with `.github/renovate.json5`; cite rm-136's keep-deleted rationale inline.
*Acceptance:* guard red when the file is planted (negative-verified via `git show <sha>:…` fixture
or a temporary plant + revert in a scratch worktree — never the landing tree), green at base; the
guard's exclusion-set comment now names both Renovate surfaces.
*Conflict note:* PR #166 also edits this file (its own guard extension) — different hunks,
append-style; reconcile by content at integrate.
*Evidence:* test diff + negative-check transcript in the PR body.

**B2 — rm-214: env-var truth table + structural coverage guard.**
README.md Configuration section: full table (var / type / default / where-read) covering every
`DASHBOARD_*`/`RATE_LIMIT_*` token read in `src/` at the realigned base (27 at 20ade64; fold in
PR #166's `DASHBOARD_MONITORING_REFRESH` at reconcile — subsumes its paragraph), with
`RATE_LIMIT_TRUSTED_PROXY` semantics spelled out (overwrite-vs-append, who gets throttled).
New `test/env-docs-guard.test.ts` (rm-164 prose-guard style): the set of env tokens matched in
`src/` must equal the set documented in README — new vars fail the suite until documented.
*Acceptance:* guard green at base with the table in; negative-verified by un-documenting one var
in a scratch run; `pnpm lint` clean (yml/yaml rules n/a, markdown lint clean).
*Conflict note:* README.md is in #166's set (env paragraph) — ours subsumes; single-file textual
hunk, reconcile by content.
*Evidence:* README diff + guard test + negative-verification transcript.

**R1 — rider: README:53 healthz wording truth (rm-107 wording half).**
README:53 `returns { ok, lastFetch, rateLimit }` → state the rm-107 reality (liveness-only
placeholders; fields reserved, values null until the view decision lands — and note #155's
in-flight view makes that landing likely). One line; rides B2's README edit.
*Evidence:* grep before/after; rm-107 ledger flip for the wording half at validation (the view
half flips when #155 lands, not before).

**Deliberately NOT in the batch (decision record):** rm-215 (head-on #155 — next-cycle lead
post-#155), C4 panel (owned by #155 — research correction recorded), session-cache + client-truth
trio (bf5d7753's selected units), majors/26.04/canary-first-fire (dated watches: rm-108/rm-133
re-eval 2026-10-21, rm-188 re-pin window, rm-179 fire 2026-09-28 — all watch, none implement),
stale-PR stewardship (rm-159 — stewardship-phase material, 12 conductor/ci-* drafts + 11 unlanded
measured), absorb riders (rm-157 sequencing, gateway-gated), everything in 040f1ae0's and
bf5d7753's exclusion ledgers (reasons carry over).

## Verification (whole batch, implement phases)

Per-unit targeted tests first (`node --test`/vitest single-file), then full `pnpm check-types`,
`pnpm lint`, `pnpm test` (server + web suites; 3116-era count at realigned base) — exit 0
authoritative. No workflow files touched → no actionlint needed. Worktree hygiene: no repo-root
scratch; roadmap + batch doc delivered via spool artifacts at B0. Prohibited stages
(final_validation/commit/push/pr/ci) remain gated to their own phases.
