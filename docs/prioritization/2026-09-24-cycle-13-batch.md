# Prioritization batch 2026-09-24 — repo lineage cycle 13

Provenance: conductor run c2ae28ab99624b8db5135a4b897fe6c0, prioritize attempt
a1c54d4b1730453ba0a8af37ca000568, repository-maintenance lineage 28211cbd cycle:1 (engine label).
This doc is authoritative for this run's implement phase.

> **Refiled at integrate 2026-09-25 (case 053af7e0, run c2ae28ab9962…)**: this batch authored as "cycle
> 10" on 2026-09-24, but run 3538ec96's batch claimed the cycle-10 label on main first (landed at 539c632
> with its own `docs/prioritization/2026-09-24-cycle-10-batch.md`), and cycles 11-12 have since landed
> (6fe26972 / 41c7d471) — re-filed here as **cycle 13**. Internal "cycle 10" phrasings below are the
> authoring-time engine label and are left as recorded. Second id renumber at this integrate: this run's
> minted rm-164/rm-165/rm-166 collided with the landed registry again (rm-164 prose-residue guard and
> rm-165 worktree-freshness via 3538ec96, rm-166 base-drift readback via 41c7d471), so green-main repair →
> **rm-181**, hygiene riders → **rm-182**, binding-doc claims → **rm-183** (full chain: 160/161/162 →
> 164/165/166 → 181/182/183). B4 thirds close into rm-112, B3 pins absorb into rm-103, B2 protection state
> folds into rm-116 — see the ROADMAP integrate note. This merge (candidate 76c7e2f into origin/main
> 57a182b) is the landing.

Lineage numbering (disclosed per convention): the engine labels this run cycle:1 of its lineage, but
repo docs carry landed batches cycle 1–9 (docs/prioritization at origin/main `82312f7`, verified by
ls-tree 2026-09-24; cycle-8 was burned by a PR title without a doc, cycle-9 landed at `6b6f4e7`).
No batch, doc, or PR claims the cycle-10 label (grep at main: only cycle-9's deferred-queue prose
mentions "cycle 10+"). Taking lineage **cycle 10**.

## Live frame re-measurement (stale-base rule)

- `git fetch origin` 2026-09-24 (this phase, 05:46Z): worktree HEAD `7809df6` on the conductor branch
  (unique delta vs merge-base `d73fbe7`: only `.github/workflows/visual.yaml` → hosted runners,
  superseded at main by `aa9937f`'s digest-pinned playwright container). `origin/main` tip =
  **`82312f7`**, 18 commits ahead of HEAD. Note for the record: this run's assess-phase evidence
  wrote the range as "82312f7..b2ef125" — `b2ef125` is an interior boundary of that listing, not the
  tip; `git rev-parse origin/main` = `82312f7` verified fresh this phase.
- **Main is RED at tip**: run 35954752299, failure 2026-09-24T04:13Z — `fork-exclusion-guard.test.ts:81`
  "no .conductor/ engine state is tracked in git" fails; `git ls-tree -r origin/main .conductor` →
  13 tracked breadcrumb ndjsons (the `ecedc26` landing re-tracked them; fifth occurrence of the class).
- **Id-space collision (discovered this phase)**: main's ROADMAP carries **57 unique ids** including
  `rm-155`, `rm-157`, `rm-159` (free: 151, 152, 156, 158). This run's roadmap phase minted
  `rm-155/156/157` from the stale worktree frame (max seen id 133) — two of three collide. Landing map:
  this run's rm-155 (green-main repair) → **`rm-160`**; rm-156 (binding-doc claims) → **`rm-161`**;
  rm-157 (hygiene riders) → **`rm-162`**. This run's rm-154 "registration" is redundant — main's
  registry already carries rm-154 (the release-trigger item landed at `6b6f4e7`); the gap existed
  only in the stale worktree copy. Root cause: cycle-9's B0 governance rule ("audit open PR titles
  and `git log --all -- ROADMAP.md` before allocating ids") was not applied by this run's roadmap
  phase — the batch landing must re-apply it.
- **Main-side registry currency** (things the worktree frame got wrong or stale): rm-112 is more
  advanced than the worktree copy records — cycle-9's implement (`9b17c4e7`, landed `6b6f4e7`)
  removed the revisit cache and made `repository:null` fail-visible, and DECLINED negative caching
  with rationale recorded at the site. Remaining thirds, verified live at tip this phase:
  warm-empty banner (`aggregator.ts:710` — `staleBanner: enumerationFailed` is `false` when
  enumeration succeeded but the working set came up empty), resolver-failure absence entries
  (`aggregator.ts:675` no-resolver filter; `installations.ts:218-236` mint/list failures
  `continue` with warning only), and the `MonitoringDto` degradation signal (no DTO exists yet;
  only a comment at `aggregator.ts:518`). rm-123: base-drift.yaml landed (`c4404af`), first
  scheduled fire Monday 2026-09-28 04:13 UTC.
- Branch protection exists but is VACUOUS: `gh api /repos/codeo1io/dashboard/branches/main/protection`
  → `required_status_checks.contexts=[] checks=[]`, `enforce_admins.enabled=false`. Four recorded red
  landings, including the current tip.
- Upstream drift 3 (all dep-only, merge-base `0c7489d`): `66a542d` #521 fro-bot/agent **v0.115.0**
  (fork pins `v0.114.1` at `fro-bot.yaml:337` — main's rm-157, deferred cycle-9 by G2), `e51b480`
  #519 bfra-me/.github action v4.32.0 (**no fork `uses:` — verified `git grep 'uses:.*bfra-me'`
  → nothing; prose mentions only**), `f35e281` #520 pnpm **11.27.1** (fork pins `11.27.0` at
  `package.json:56` + `Dockerfile:4,24` corepack, and the rm-131 guard asserts the exact pin).
- node:24-slim digest re-drifted: pinned `0e0ff40` vs live `5cbc7cab` (`Dockerfile:1,21,39`;
  `docker manifest inspect` 2026-09-24).
- Cycle-9's own deferred queue for cycle 10+ was: rm-144 (with rm-114), rm-107 (enabled by its B2),
  rm-116 (owner action), rm-105, rm-103+rm-149 (absorb window with v0.115.0). This batch honors
  rm-116 and the verifiable half of the absorb window; overrides are itemized in the deferred table.

Inputs used, not redone: assess `0893d708` (12 findings, HEAD/fingerprint evidence), research
`2d3de73e` (R1–R8, /tmp note + evidence list), roadmap `60b1caa5` (worktree ROADMAP +65/−44,
39 ids, structural verification).

## Gated scoring (impact, risk-to-skip, risk-to-do, effort, dependencies)

Gates: (G1) restores/locks a broken invariant? (G2) completable end-to-end this cycle with
hosted-runner CI evidence? (G3) depends on unlanded or out-of-repo work?

| item | impact | risk-to-skip | risk-to-do | effort | deps | gated |
| --- | --- | --- | --- | --- | --- | --- |
| rm-181 green-main-at-tip repair (initially cited rm-160) | 9 — Main red at tip poisons every subsequent landing's signal | 9 — fifth occurrence, silent by construction (discovered only out-of-band) | 1 — one `git rm -r --cached` riding a main-direct landing + docs | S | none | PASS |
| rm-116 branch-protection fill | 8 — four recorded red landings | 8 — nothing is actually required today | 2 — gh api PUT + an AGENTS.md override note; drift-watch sub-bullet deferred | S | wants B1 first (fill on a green tip) | PASS |
| rm-103 absorb rider: pnpm 11.27.1 + node digest 5cbc7cab | 6 — base currency; #3308 triage convention wants fixed-version action | 5 — Monday's drift check files it anyway; dependabot window ~10-03 | 2 — the rm-131 guard asserts the exact pnpm pin: coordinated edit, else self-redden | S | none | PASS |
| rm-112 remaining thirds (warm-empty banner, resolver-failure absence entries, MonitoringDto) | 8 — a monitoring dashboard that misreports calm is broken at its purpose (this run's assess HIGH F1) | 7 — the exact class the product exists to surface | 3 — semantic changes against a 2000+ test net; negative-caching stays DECLINED (cycle-9 rationale) | M | none | PASS |
| rm-182 hygiene riders: .dockerignore, README staleness, cookie-key canonical format (initially cited rm-162) | 5 — build-context hygiene + doc-truth + silent malformed key loads | 4 | 2 — Secure-flag inference EXCLUDED (rides rm-129's trusted-proxy design) | S-M | none | PASS |
| fro-bot/agent v0.115.0 absorb (main's rm-157) | 7 — pin lags a release | 6 | 4 — fro-bot workflow is `disabled_manually`; adoption unverifiable without the live gateway | M | infra pairing | DEFER — cycle-9's G2 stands |
| rm-183 binding-doc claims test (initially cited rm-161) | 6 — guards the recurring wording-regression class | 5 | 2 | S-M | none | DEFER — keeps this cycle's diff reviewable; no dependency pressure |
| rm-105 SBOM/provenance | 7 | 4 — attestations already land via buildx (#3317) | 3 | M | Release theme | DEFER |
| rm-107 composed status surface | 6 | 5 | 4 — joint design | M-L | design | DEFER — but B4's MonitoringDto lays its input layer |
| rm-114 SSE single-sourcing + rm-144 | 5 | 3 | 3 | M | build infra | DEFER (cycle-9 queue) |
| rm-106 listener push/digest | 6 | 5 | 4 — SW decision first (recorded below) | M | decision | DEFER — decision recorded this cycle, implement next |
| rm-102 dependabot first-PR proof | 5 | 3 | 2 | S | time window ~2026-10-03 | DEFER — window hasn't opened |
| bfra-me action v4.32.0 | — | — | — | — | — | EXCLUDE — zero fork `uses:` (verified) |
| rm-159 origin validation-residue sweep | 4 | 3 | 2 | S | push authority | DEFER — orthogonal sweep; B1 is the cure, rm-159 the residue pass |

## Selected batch — cycle 10: green, guarded, current, fail-visible

### B1 — rm-181: green-main-at-tip repair (FIRST)

- What: land `git rm -r --cached .conductor` (13 files) **main-direct** — staged untracks do not ride
  candidate-delta merges (the `:(exclude).conductor/**` pathspec drops them; root mechanism + cure in
  `docs/solutions/workflow-issues/ephemeral-ci-base-pr-excluded-by-branch-filters-2026-09-24.md`).
  Disk `.conductor/` preserved untracked — back it up byte-identical before any `reset --hard`/merge.
- Acceptance: `git ls-tree -r origin/main .conductor` EMPTY; Main green at the landing sha (the
  fork-exclusion guard turning green IS the proof); the landing-pipeline trap recorded in AGENTS.md
  or the runbook (one line pointing at the solutions doc).
- Evidence: gh run id green at landing sha; ls-tree empty; doc line diff.

### B2 — rm-116 fill: make branch protection real

- What: `gh api -X PUT /repos/codeo1io/dashboard/branches/main/protection` — required checks = the
  Main job conclusions (Lint / Design / Types / Test / Check Workflows / Scripts Load as named)
  plus CodeQL; `enforce_admins.enabled=true`; an AGENTS.md line documenting the hotfix override
  path (temporary unenforce + re-enforce, with a runbook pointer).
- Acceptance: re-read protection object shows populated `checks`, `enforce_admins.enabled=true`;
  the next landing merge is structurally gated (a red candidate cannot merge).
- Evidence: before/after `gh api` JSON; AGENTS.md diff. (Weekly drift-watch sub-bullet DEFERRED —
  protection is repo settings, not tree state; revisit with rm-120's watch theme.)

### B3 — rm-103 absorb rider: pnpm 11.27.1 + node digest 5cbc7cab

- What: `package.json:56` packageManager → `pnpm@11.27.1`; `Dockerfile:4,24` corepack prepare →
  `pnpm@11.27.1`; `Dockerfile:1,21,39` digest → `sha256:5cbc7cab…` (full digest from
  `docker manifest inspect`); **coordinated**: `test/fork-exclusion-guard.test.ts` asserts the exact
  `11.27.0` pin — update in the same change or the guard self-reddens.
- Acceptance: frozen-lockfile install clean; full gates green (`pnpm lint`, `check-types`, `test`);
  docker build succeeds on the new digest (Release-path build or local); lockfile hash churn
  expected only via pnpm's own lockfile-version touch — none expected for a patch bump.
- Evidence: diff of the four files + guard test; green Main run; `docker build` log.
- Explicitly OUT (recorded): fro-bot/agent v0.115.0 (main rm-157, cycle-9 G2 deferral stands —
  disabled workflow, gateway pairing); bfra-me v4.32.0 (zero fork `uses:`).

### B4 — rm-112 remaining thirds: fail-visible aggregator (finish the item)

- What: (a) warm-empty banner — `aggregator.ts:710` path sets `staleBanner:true` when a previously
  non-empty snapshot empties on a GOOD enumeration (data lost, not quiet); (b) resolver-failure
  absence entries — per-repo resolver failures (`aggregator.ts:675` filter; installations-side
  mint/list failures at `installations.ts:218-236`) recorded as absence entries / a counted skip
  the operator can see, not a silent `continue`; (c) `MonitoringDto` degradation signal — a
  monitoring DTO (or field on the snapshot DTO) carrying enumeration/skip counts so the client can
  render degradation; negative caching stays DECLINED (cycle-9 rationale recorded at the site).
- Acceptance: tests for all three branches (warm-empty after good enumeration, resolver failure on
  one repo of several, installations mint failure); the client surfaces at least the banner + count;
  MonitoringDto is additive (no breaking DTO change to the SPA contract without a compat check).
- Evidence: new tests in `pnpm test`; aggregator/installations diffs; SPA render of the degradation
  state (screenshot or test).

### B5 — rm-182 hygiene riders

- What: (a) `.dockerignore` (`.git`, `.slim`, node_modules, docs, .conductor, web/dist variants as
  appropriate — measure context size before/after); (b) README truth pass — remove the Workbox
  caching credit (deps removed at `f334fb2`, SW is a kill-switch), fix branch badges to the fork;
  (c) `src/session.ts` cookie-key loader — enforce a canonical format (expected wrapped-base64
  length for the key size) with fail-fast logging + test.
- Acceptance: `docker build` context size recorded before/after; README contains no claims the tree
  disproves (grep `workbox` in README → 0 outside historical notes); malformed-key test red before,
  green after; Secure-flag inference NOT included (rides rm-129).
- Evidence: context-size numbers, README diff, session tests.

### Sequencing and landing runbook

1. B1 lands FIRST, main-direct. Then B2 immediately after (fill protection on a green tip).
2. B3–B5 are the code batch — apply at the FRESH tip (the worktree base `7809df6` is 18 commits
   behind; reset/merge to post-B1 main before applying, per the cycle-9 base-correction precedent).
3. Standing hygiene: explicit stage lists only; after EVERY merge `git ls-files .conductor` must
   end empty (guard is armed now); backup disk `.conductor/` before any reset.
4. ROADMAP reconciliation at landing: main's 57-id registry is the base; re-apply this run's
   additive deltas with the id map (→160/161/162), drop the redundant rm-154 Completed duplicate
   and the superseded rm-112 rescoping (main's wording is more advanced — keep main's, add this
   run's `installations.ts:218-236` line evidence to its signals), keep the dated signals that are
   still fresh (drift measure, protection-vacuous, fourth red landing), and re-apply cycle-9's
   id-allocation governance rule in the header note.

### Recorded decision input — rm-106 (implement next cycle, not this one)

Recommendation to carry forward: the push-capable surface should be a **minimal push-only service
worker** — handles `push` + `notificationclick` only, never navigation/controller interception
(preserving the kill-switch rationale recorded in `web/src/sw.ts` and memory #3836) — riding the
VAPID/subscribe/reconcile path main already hardened (#508/#511). The alternative (pivot delivery
to a non-SW channel) forfeits the hardened path. The implementing cycle records the final call in
its batch doc, per rm-106's acceptance.

## Outcome (2026-09-24 — pre-review compound, run c2ae28ab)

All five units executed in-tree, uncommitted per the prohibition list; review and shipping follow this record.

- **B1 (rm-181)** — tree-side complete: AGENTS.md landing-pipeline trap section added; the worktree's `.conductor` is untracked. The main-direct `git rm -r --cached .conductor` (main still tracks 13 ndjsons at 82312f7) requires push authority and executes at the landing gate, riding the landing merge per the exclude-pathspec solutions doc.
- **B2 (rm-116)** — **landed live**: `gh api PUT /repos/codeo1io/dashboard/branches/main/protection` with 7 required checks (Analyze, Check Types, Check Workflows, Design Check, Lint, Test, Test Scripts Load) + `strict` + `enforce_admins`, re-read verified. Remaining rung for rm-116: the weekly drift-snapshot workflow.
- **B3 (rm-103)** — pnpm 11.27.1 pins landed in-tree (package.json + both Dockerfile corepack pins + fork-exclusion-guard regexes coordinated). **Digest component was a no-op**: this doc's `5cbc7cab` drift citation measured the amd64 platform-manifest digest; the Dockerfile pins the multi-arch index digest `0e0ff40`, which is registry-current. Lesson compounded to `docs/solutions/workflow-issues/container-digest-pin-index-vs-platform-manifest-2026-09-24.md` (which also records base-drift.yaml's latent non-verbose extraction bug before its first scheduled fire, Monday 2026-09-28 04:13 UTC).
- **B4 (rm-112)** — all three remaining thirds implemented: warm-empty stale banner, resolver/mint absence entries + `failedInstallations` accounting, `MonitoringDto` degradation block consumed by the new SPA `DegradationBanner` polling `/api/monitoring`. rm-112's registry entry updated to reflect full-scope implementation pending landing.
- **B5 (rm-182)** — `.dockerignore` (build context 339.5 MB → 2.9 MB), README service-worker paragraph truthed to the kill-switch, session cookie-key canonical 64-hex format enforced + tested.

Validation consumed (not re-run here): local suite 3143/3143 (+28 new), lint 0, types 0, docker build green; targeted phase ephemeral PR #73 10/10 SUCCESS (candidate 02c53b10); full_tests ephemeral PR #76 10/10 SUCCESS (candidate 8e2ec75a, ok:true).

Id-allocation correction: this doc minted rm-160/rm-161/rm-162 before a `git log --all` audit surfaced the pending 6cbc2a1d lineage (b0fc297) allocating rm-155..rm-163. The registry renumbered: green-main repair → `rm-164`, hygiene riders → `rm-165`; binding-doc machine-verification (deferred) minted clean as `rm-166`. At review-fix (2026-09-24) all tree citations were renumbered to the registry ids; the only remaining rm-160/rm-161/rm-162 strings in the repo are the historical minting records (this note and the ROADMAP header note).
