# Prioritization batch 2026-09-25 — repo lineage cycle 13

Provenance: conductor run f1ae52a3f04b43f48d71569ebe3b6f4f, prioritize attempt
4a7264416ea94391aaab3dbdb6416f81, repository-maintenance campaign 4fd7a0d8 cycle:1 (engine label).

Lineage numbering (disclosed per convention): engine labels this run cycle-1 of campaign 4fd7a0d8, but repo
docs carry landed batches cycle 1–12 at origin/main 57a182b (cycle-9..12 landing waves verified live:
aggregator `#`-form comments at :131/:169, base-drift digest-extraction contract, canary.yaml). The only
in-flight batch-doc name is `2026-09-24-cycle-1-batch-2.md` (PR #114). Taking lineage
**cycle 13** avoids collision at authoring time; the run id `f1ae52a3…` is the disambiguator.
This doc is authoritative for this run's implement phase.

## Id-renumber amendment (integrate merge into d00d095, 2026-09-25)

Mint-time ids rm-184/rm-185/rm-186 collided with the parallel cycle:1 batch (run
5b6e8e8019524c22b90232a1837e9905, "drift guard, fail-closed private, token expiry"), which landed on
main first as d00d095 and owns rm-181..rm-185 — its rm-184 is the metadata private fail-closed item and
its rm-185 the installation-token expiry item. Per the repo's id-collision convention — the incoming
batch renumbers up, exactly as cycle:1 itself was renumbered rm-177..180 → rm-182..185 against cycle-12 —
this batch's items are now **rm-186** (`.dockerignore` seal, was rm-184), **rm-187** (links-parse
degradation, was rm-185), and **rm-188** (ubuntu-24.04 runner pins, was rm-186). All in-repo references
were updated in lockstep: the ROADMAP items and header note, the `.dockerignore` provenance header, the
`test/dockerfile-context.test.ts` guard block, the nine workflow re-eval comments, and this doc (the
mint-time ids remain only inside this amendment note as history). The PR #130 body still cites the
mint-time ids — it is the historical record of the pushed branch, not a live reference.

## Live frame re-measurement (stale-base rule)

- `git fetch origin main` this phase (2026-09-25): `origin/main` = `57a182b` (advanced past 480c92c since
  this run's assess; re-based by the roadmap phase). Worktree HEAD `7809df6` (22+ behind), carrying exactly
  ` M ROADMAP.md` (this run's roadmap deliverable: rm-186..rm-188 + signal appends, built on the 57a182b
  base) plus untracked `.conductor/`. All line evidence below derives from the origin/main tree per the
  truth-scoping rule.
- This run's three mints re-verified live at 57a182b: `git show origin/main:.dockerignore` → absent;
  `git grep 'JSON.parse(row.links)' origin/main -- src/listener/store.ts` → :51 unguarded;
  `runs-on: ubuntu-latest` ×16 across 9 workflow files.

## In-flight payloads that gate sequencing (de-dup + order)

Full open-PR map (`gh pr list --state open`, 2026-09-25) — many are stranded landing PRs whose payloads
already landed via later waves; the relevant unlanded deltas:

- **PR #97** (`conductor/run-c2ae28ab9962`, cycle-10 era, 88-file stale diff vs main): its rate-limiter and
  prose-guard payload landed long ago, but **its `.dockerignore` (24 lines, tagged rm-165/cycle-10, with
  the measured ~340 MB → ~3 MB context cut) never landed** — `origin/main:.dockerignore` absent. This is a
  stranded artifact exactly like the PR #9/#10 pattern: land it by content, do not re-derive from scratch.
  Its exclusion set was re-checked against main's CURRENT Dockerfile this phase: builder stage
  `COPY web/ ./web/` + `RUN pnpm build:web` (dist built in-image, line 18; final stage copies dist
  `--from=builder`, line 53) — so excluding `.git`, `node_modules`, `test`, `scripts`, `docs`, `web/dist`,
  `.env*`, `*.pem`, `*.key` etc. remains correct: nothing the image needs enters via context except
  manifests, `web/`, `src/`, `public/`.
- **PR #23** (`conductor/run-b9c36244d5ff`, cycle-8) and **PR #113** (`conductor/run-d6455cdcf51c`,
  cycle-11) both carry unlanded `src/listener/store.ts` deltas for the same dedupe-un-ack replay bug —
  **double lock on the file that rm-187's links-parse guard would edit.** Precedent (cycle-12: rm-141
  sequence-blocked behind PR #45's `src/github/*` lock) says block, don't pile on.
- **PR #45** still carries a 117-line unlanded `src/github/aggregator.ts` + 82-line `app-client.ts` delta —
  **rm-112 remainder / rm-107 / rm-119 / rm-141 stay sequence-blocked** (re-verified this phase by direct
  `git diff --stat origin/main origin/conductor/run-91e4626dccaf -- src/github`).
- **PR #114** (`conductor/run-5b6e8e801952`, cycle:1 sibling): metadata.ts truthy-private fail-closed fix +
  token-expiry + base-drift drift guard — owns the metadata surface this run's assess independently
  corroborated (assess finding = duplicate-in-flight, NOT selected).
- **PR #10/#13/#14/#15/#43/#82/#104**: stale workflow-era branches; their workflow content predates the
  current hosted-era files (all landed via later waves). B2's pins touch the same filenames but current
  content — landing reconciles by content, same as every prior wave.

## Inputs used, not redone

assess 9ca979e9 (9 findings; live-on-main subset became rm-186/rm-187 signals; GraphQL `//`-bug and
upstream-fixed items already absorbed by landed rm-177), research 9ca3c1b6 (ecosystem/upstream evidence;
npm dist-tags 2026-09-25; runner-images 26.04-published signal; absorb-set growth to 5 commits),
roadmap 2979c123 (rm-186..rm-188 minted above all-lineage max rm-183; signals appended to rm-108/140/157).
Dedupe correction discovered THIS phase: rm-186's "no open lineage covers it" claim was scoped to PR #113
and PR #114 only — **PR #97 strands the exact artifact.** rm-186 therefore selects as a content-landing
reconcile, not a fresh implementation; the roadmap header note's PR enumeration is corrected here.

## Gated scoring (impact, risk-to-skip, risk-to-do, effort, dependencies)

Gates: (G1) restores/locks a broken invariant? (G2) completable end-to-end this cycle with hosted-runner CI
evidence? (G3) depends on unlanded or out-of-repo work?

| item | impact | risk-to-skip | risk-to-do | effort | deps | gated |
| --- | --- | --- | --- | --- | --- | --- |
| rm-186 .dockerignore landing (security 54.0; assess + PR #97 stranded content) | 8 — every docker build ships the repo-root context (.git, 394 MB node_modules, any local .env/.pem/.key) to the daemon; Release builds run on every push-to-main | 8 — secret-scanning exposure class on shared builders + 100× context waste, invisible until it bites | 1 — land PR #97's 24-line file by content (re-checked against main's Dockerfile), plus a new structural guard test | S | none (new file + one test file; no open PR adds .dockerignore — #97 IS the source) | PASS |
| rm-188 ubuntu-latest alias-flip guard (reliability 33.0; research) | 6 — 16 alias sites across 9 workflows silently swap system deps + image-bundled node the day GitHub flips ubuntu-latest→26.04; only the visual job is containerized, and the visual baselines are already mixed-provenance fragile | 6 — a flip lands mid-cycle with zero repo-side signal; the cost concentrates exactly where baselines are least reproducible | 2 — mechanical pins + dated re-eval comments; actionlint-gated; stale-PR overlap reconciles by content | S-M | none in-repo | PASS |
| rm-187 links-parse degradation (reliability 47.0; assess) | 6 — one corrupt links cell 500s the whole messages endpoint | 5 | 1 | S | **G3 fail — PR #23 + PR #113 both lock `src/listener/store.ts`** (same precedent as rm-141/#45) | NOT selected (blocked) |
| rm-157 agent v0.115.0 absorb (72.0) | 7 | — | — | M | G3 fail — needs the live gateway for contract validation + the upstream pnpm 11.27.1 pin deliberately diverges (rm-140) | DEFER |
| rm-112 remainder / rm-107 / rm-119 / rm-141 | 6–8 | — | — | M-L | PR #45 owns aggregator/app-client (117-line unlanded delta re-verified) | NOT selected (blocked) |
| metadata truthy-private (assess HIGH) | 7 | — | — | S | duplicate-in-flight: PR #114 owns metadata.ts + tests + solution doc | NOT selected (de-duped) |
| checkout-provenance view | 6 | — | — | S-M | in flight as an item gated on rm-157 (PR #113) | NOT selected (de-duped) |
| rm-116 branch protection (58.0) | 6 | — | — | S | account-level owner action; PR queue still 12 deep — enabling mid-queue risks wedging landings | DEFER (raise out-of-band) |
| rm-102 dependabot first-PR (90.0) | 6 | — | — | — | window opens ~2026-10-03 | NOT selected (waiting on evidence) |
| rm-126 drift-surface decision (73.0) | 5 | — | — | M | decision-heavy, operator-visible semantics | DEFER |
| rm-139 Node 26 / rm-140 pnpm 12 / rm-108 majors | decision docs | — | — | S | time-gated (2026-10-28 LTS promotion; signals refreshed this run) | DEFER |
| rm-168/rm-169-era riders, visual-fragility family | 3–4 | — | — | S-M | gated on rolling-image decision | NOT selected |

## Selected batch — theme: environmental determinism (stop the build/runtime environment from lying)

All units are unblocked, in-repo, test-backed, and none duplicates an open PR's unlanded payload —
rm-186 is the deliberate exception that CONSUMES one (the stranded PR #97 artifact). Implement order
B0→B1→B2. The shared thread: the daemon-side build environment currently receives secrets and 340 MB of
junk it must never see (B1), and the CI environment silently floats on a rolling alias that will flip
under the most fragile baselines in the fleet (B2).

- **B0 — landing reconciliation rider (XS).** Land this run's already-staged `ROADMAP.md` (rm-186..rm-188,
  signal appends, header note) with TWO corrections at implement time: (1) rm-186's item gets a
  dedupe-note appended recording the PR #97 stranded-artifact discovery (this phase's finding — the
  roadmap header's PR enumeration scoped to #113/#114 was incomplete); (2) rm-187 gets a
  sequence-blocked note behind PR #23/#113, mirroring the rm-141/#45 precedent. Place this batch doc.
  Gate: `pnpm lint` on all landed md; ROADMAP item-ids unique (53 open, 0 dupes).
- **B1 — rm-186: land the stranded `.dockerignore` by content + structural guard.** Byte-carry PR #97's
  24-line `.dockerignore` (re-verified against main's current Dockerfile: builder builds `web/` in-image,
  final stage copies dist `--from=builder`; exclusions cover `.git`, `.github`, `.conductor`, `.slim`,
  `docs`, `node_modules`, `test`, `scripts`, `web/dist`, `web/dist-fixture`, `*.md` + `!README.md`,
  `.env*`, `*.pem`, `*.key`, `coverage`, `.vite`), updating its header comment to cite rm-186/cycle-13 +
  the PR #97 provenance instead of the stale rm-165/cycle-10 tag. ADD the structural guard PR #97 lacked:
  `test/dockerfile-context.test.ts` (rm-132 block) asserts `.dockerignore` presence + the minimal
  security entry set (`.git`, `node_modules`, `.env*`, `*.pem`, `*.key`), negative-verified against its
  absence. Acceptance (rm-186): context-size delta recorded (PR #97's measurement ~340 MB → ~3 MB
  re-measured on the implement machine), a Release/docker build green with the ignore in place, guard test
  red-then-green.
- **B2 — rm-188: pin `runs-on: ubuntu-24.04` at all 16 sites across 9 workflow files** (main.yaml ×6,
  release.yaml ×3, base-drift/canary/codeql/dependency-review/fro-bot/scorecard/visual ×1 each), with a
  dated re-evaluation comment (aligned to rm-139's 2026-10-28 Node-26 window: re-evaluate the pin when
  the 26.04 pre-flight decision happens) placed once per file near the first `runs-on`. The visual job's
  containerized playwright stage is unaffected. Acceptance (rm-188): `git grep 'runs-on:'` shows zero
  remaining `ubuntu-latest` without a recorded reason; actionlint 0 via the container form; full suite
  green. Stale-PR branches sharing these filenames reconcile by content on landing (their workflow
  content is the older self-hosted era).

## Deferred queue (carried, with this phase's re-verification)

rm-187 (store.ts double-lock — first candidate when #23/#113 land), rm-157 (gateway absorb — the 5-commit
set 0c7489d..eb8ca95 with the renovate.yaml/release.yaml conflict points already mapped in rm-157's
signals), rm-112/107/119/141 family (behind PR #45), rm-116 (owner action, out-of-band), rm-126/rm-139/
rm-140/rm-108 (time-gated decisions, signals current as of 2026-09-25).

## Cycle outcomes and next-cycle handoff (compound, 2026-09-25, pre-review)

Recorded from pre-review cycle evidence only; review and landing outcomes are not yet known and belong
to the next cycle's assessment.

- B0 landed in-tree: ROADMAP signals for rm-186 and rm-187 now carry the PR #97 stranded-branch dedupe
  correction and the PR #23/#113 sequence-block note; rm-188 carries its pin decision.
- B1 implemented: the 24-line `.dockerignore` landed by content from PR #97's stranded branch with an
  updated provenance header, plus the structural guard PR #97 never had (existence, minimal entry set,
  never-excludes-a-COPY-source cross-check) in test/dockerfile-context.test.ts. Measured context cut
  re-verified at 403 MB -> 8.2 MB (~49x). Red-to-green and non-vacuity both proven during implement.
- B2 implemented: all 16 `runs-on` sites across 9 workflows pin `ubuntu-24.04` with dated per-file
  re-eval comments aligned to the rm-139 Node-26 window; zero `ubuntu-latest` remains.
- Validation (recorded outcomes, not re-run here): targeted gate fell back to the authoritative full
  route (package.json in the engine's surface list) and went 10/10 green at PR #118 (commit d56d9d6)
  after one doc-only markdownlint fix; the full gate passed first-run 10/10 green at PR #120 (commit
  bcba21c). Porcelain fingerprint e1f560efcb63c067 was stable across both, proving zero
  executable-surface drift during validation.

Reusable lessons this cycle (prevention rules):

- Hard-wrapped markdown must never start a line with a `#` token — PR refs like the ones above read as
  malformed ATX headings and fail the Lint gate; see
  docs/solutions/workflow-issues/hard-wrapped-markdown-line-initial-hash-fails-atx-lint-2026-09-25.md.
- The ephemeral-PR validation snapshot includes untracked files (proven twice: the failing doc and the
  new `.dockerignore` both rode it), so doc-only edits are never lint-neutral and untracked fixes are
  genuinely exercised by CI.
- A validated artifact can sit stranded on an unlanded stale branch (PR #97's `.dockerignore`, measured
  but never landed); sweep every open PR's diff for the target surface before deriving fresh work, and
  prefer content-landing plus the missing structural guard over re-implementation.

Next-cycle candidates in priority order (carried): rm-187 the moment PR #23 or PR #113 lands (store.ts
corrupt-links resilience, acceptance block authoritative); rm-157 absorb batch (5-commit set, the two
mapped conflict points); the rm-112/107/119/141 family behind PR #45; the dated re-eval decisions
(rm-126/rm-139/rm-140/rm-108) whose windows open from 2026-10-20. Review/landing outcomes for this
batch: to be folded in by the next cycle's assessment, not asserted here.
