# Prioritization batch 2026-09-24 — repo lineage cycle 11

Provenance: conductor run 41c7d4711ef349fba6c47a2bb73f93b0, prioritize attempt
219487c51b85439f999b4127742a4167, repository-maintenance campaign 3a8069ee cycle:1
(engine label).

Lineage numbering (disclosed per convention): engine labels this run cycle:1 of campaign
3a8069ee, but repo docs carry landed batches cycle 1–10 at origin/main 539c632 (cycle-10's
integrate merged as 539c632 itself). Taking lineage **cycle 11** continues the visible
sequence with no filename collision; the run id `41c7d471…` is the disambiguator. This doc
is authoritative for this run's implement phase.

## Live frame re-measurement (stale-base rule)

- **GitHub origin/main = 539c632** (`gh api repos/codeo1io/dashboard/branches/main` →
  539c6320…, verified twice, 2026-09-24 ~12:05Z) — unchanged since this run's roadmap phase
  (08:22Z cycle-10 integrate). All assess/research currency evidence stands against it.
- This run's worktree base is 7809df6 (~20 commits behind): implement phases MUST
  fast-forward to ≥539c632 before editing (recorded in the assess PhaseResult).
- **TRAP (new, measured live):** a /tmp clone whose `origin` aliases the local canonical
  checkout (`origin = /work/projects/dashboard`) fetches **d0d17fc — 31 commits behind
  GitHub main** — because the canonical checkout itself lags. Never judge main's position
  from a local-aliased remote; use `gh api …/branches/main` or the worktree's real origin.
- **Open PRs: 0** (twice-verified). The 21 open PRs counted by this run's assess ~3h
  earlier have been swept — the merge-order collision surface is empty; no in-flight
  payload touches any batch file. (PRs #43/#45 referenced by the cycle-10 doc are MERGED
  — 2026-06-19 on GitHub numbering; their payloads landed in the 539c632 wave.)
- Gates at 539c632 (this run's assess): lint ✓, check-types ✓, full suite 3160/0.

## Inputs used, not redone

assess c5bf00b9 (adversarial sweep + gates at 539c632; F1–F8 with file/line evidence),
research 5cf830f2 (C1–C9; agent v0.115.0 contract-1.7.0 payload verified at the tag),
roadmap 737837ea (rm-166..rm-172 minted + 9 dated updates on 539c632 content).

## Gated scoring (impact, risk-to-skip, risk-to-do, effort, dependencies)

Gates: (G1) restores/locks a broken invariant? (G2) completable end-to-end this cycle with
hosted-runner CI evidence? (G3) depends on unlanded or out-of-repo work?

| item | impact | risk-to-skip | risk-to-do | effort | deps | gated |
| --- | --- | --- | --- | --- | --- | --- |
| rm-166 sentinel repair + digest-parse guard (88.0; TIME-BOXED: cron fires 2026-09-28T04:13Z) | 8 — the only standing CI sentinel for the node:24-slim pin chain is structurally broken (empty live digest, exit 1) | 9 — Monday's first scheduled fire is a guaranteed false red AND real drift becomes indistinguishable; a sentinel that cries wolf erodes the whole drift-watch discipline (rm-123/rm-111 lineage) | 1 — workflow edit + guard test; a re-dispatch proves the readback | XS-S | none; base-drift.yaml byte-stable 82312f7→539c632 | PASS |
| rm-167 should-release rule-2 lockfile consult (56.0; assess F2) | 6 — release correctness: a devDep-only PR whose lockfile regen moves an in-range runtime resolution ships a changed image with no release | 7 — silent by construction; no alarm ever fires | 1 — pure logic + table-driven quadrant test | S | none; release.yaml `paths:` already includes both files | PASS |
| rm-168 aggregator generic-FORBIDDEN matcher (48.0; assess F3 live at 539c632:492-506) | 6 — repos where the App lacks vulnerability_alerts:read degrade to whole-repo STALE every 60s instead of the designed graceful marker; first slice of rm-112's degradation semantics | 6 — recurring operator-visible staleness misattributed as fetch failures | 2 — fixture tests; matcher must stay anchored to the alerts call site (never a global 403 swallow) | S | none | PASS |
| rm-169 listener replay un-ack (44.0; assess F8: store.ts:91-95) | 5 — a replayed webhook silently un-acks operator-read notification state | 5 — silent state corruption under retry storms | 1 — upsert splits identity from mutable state + replay-after-ack test | S | none | PASS |
| rm-172 kill-switch readFileSync (30.0; assess F6: server.ts:829) | 4 — synchronous disk I/O on every authenticated page load | 4 — hot-path jank, trivially fixed | 1 | XS | none | PASS |
| rm-170 mint-fallback transient scoping (40.0) | 6 | 6 | 3 — auth-path error-shape surgery; a wrong fallback narrows the read-only token surface | M | none, but review-heavy | DEFER — auth path deserves a dedicated reviewed cycle |
| rm-171 SIGTERM/SIGINT graceful shutdown (36.0) | 5 | 4 | 2 | S-M | full acceptance needs a live container-stop drain observation | DEFER — no-docker host; evidence gap |
| rm-157 agent v0.115.0 absorb (72.0) | 7 — contract 1.7.0 payload ready (research C1) | 6 | 2 | M | G2 fail — acceptance requires a live gateway login regression | DEFER |
| rm-104 render hardening (98.0) | 7 | — | — | M | hermes-roadmap-side fleet tooling, not this repo's tree | DEFER — fleet-side |
| rm-102 dependabot first-window proof (90.0, in-progress) | observation | — | — | — | external window closes ~2026-10-03 | NOT selected |
| rm-103 absorb cadence automation (85.0) | 6 | — | — | M | wants rm-116 + alert-issue machinery; ledger itself refreshed by this run's roadmap | DEFER |
| rm-105 SBOM/provenance (80.0) | 7 | — | — | M | Release-pipeline evidence needs a real release run | DEFER |
| rm-112 remainder (75.0: absence entries, warm-empty banner, MonitoringDto degradation signal) | 8 | — | — | M-L | sequenced on rm-168 landing — this batch is its first slice | DEFER — next-cycle headline |
| rm-114 SSE single-source + multi-data join (45.0; F7 folded there) | 7 | — | — | M-L | browser+server parser consolidation is high-touch | DEFER — dedicated cycle |
| rm-106/107/115/116/117/118/119/120/127/138/139/140/141/146/147/159/165 | — | — | — | — | blocked-external / gateway-gated / time-gated (2026-10-28) / account-level / push-authority / fleet-side | NOT selected |

## Selected batch — theme: sentinels that tell the truth (silent-failure elimination)

All five units are unblocked, in-repo, offline-testable, pairwise file-disjoint (workflow +
scripts + aggregator + listener store + server middleware), and untouched by any in-flight
payload (open PR set is empty). Implement order B1→B5; the worktree MUST fast-forward to
origin/main ≥539c632 first.

- **B1 — rm-166 (THE time-box; land before 2026-09-28T04:13Z).** `.github/workflows/base-drift.yaml`
  live-digest readback switches to the proven `docker buildx imagetools inspect node:24-slim |
  awk '/^Digest:/{print $2; exit}'` form with an explicit empty-guard whose failure message
  distinguishes read-failure from drift; rider: a workflow-statics guard test
  (pattern: `test/release-trigger-paths.test.ts`) that reddens on any `manifest inspect`
  piped into a `/^Digest:/` awk across `.github/workflows/`, with a pointer to the buildx
  form (actionlint cannot catch this class). Verify: `pnpm test` green, actionlint
  (container form) clean, and post-landing a workflow_dispatch run logs BOTH digests.
- **B2 — rm-167.** `scripts/should-release.ts` rule 2 skips only when devDeps-only AND
  `pnpm-lock.yaml` unchanged; quadrant table test (devDep/runtime × lockfile
  unchanged/changed) including an in-range runtime-resolution move inside a devDep-only
  diff (hono ^4.7.11 class). Verify: quadrant matrix green; the devDep+lockfile case
  produces a release verdict.
- **B3 — rm-168.** `src/github/aggregator.ts` isVulnerabilityAlertsPermissionError covers
  the generic `Resource not accessible by integration` message, anchored to the alerts call
  site; fixture test asserts the permission-denied degradation marker (not STALE) and a
  companion asserts genuine failures still fail visible (rm-112 semantics).
- **B4 — rm-169.** `src/listener/store.ts` replay upsert preserves `read_at` (identity
  fields vs mutable state split); replay-after-ack test; replay-vs-redelivery note at the
  site.
- **B5 — rm-172 (XS rider if budget holds).** Push-killswitch state cached (startup read
  or short mtime/stat guard); no synchronous file I/O on the authenticated `/` path;
  cached-semantics test.

Cycle gates for the implement phase: `pnpm lint`, `pnpm check-types`, full `pnpm test`
green at the fast-forwarded base, actionlint on the touched workflow, and the ROADMAP
status flips for rm-166..rm-172 (whichever land) recorded with landing evidence.

## Outcome (compound phase, 2026-09-24, pre-review)

All five batch items were implemented (run 41c7d471, implement attempt f043c0ad) as
working-tree edits on origin/main 539c632 content; none are landed yet — implemented is
not landed.

| Item | Batch | Status at compound time |
| --- | --- | --- |
| rm-166 | B1 | implemented + guard test; local actionlint 0, focused 267/267, remote Check Workflows green (ephemeral PRs #110/#111); pushed-sha re-dispatch evidence rides the landing; deadline 2026-09-28T04:13Z |
| rm-167 | B2 | implemented (rule 2 consults hasLockChange); quadrant pins flipped/added; validated as above |
| rm-168 | B3 | implemented (generic FORBIDDEN matcher, alerts-site-anchored); fixture test green |
| rm-169 | B4 | implemented (replay upsert preserves read_at); stays-read/unread/ack-preservation tests green |
| rm-172 | B5 | implemented (async-once SPA shell cache, 5s TTL); hermetic flip + missing-shell tests green |

Validation record (consumed here, not re-run): focused vitest 267/267 across the 7
impacted test files; the engine's authoritative full validator green TWICE via
ephemeral PRs #110 (targeted_tests attempt e83a29cf) and #111 (full_tests attempt
853e71c9) — 9/9 checks each, both PRs auto-closed. Durable lesson recorded at
docs/solutions/workflow-issues/docker-manifest-inspect-no-digest-header-false-drift-2026-09-24.md.
ROADMAP statuses for the five items flipped to `implemented ... pending landing`.

### Context for the next cycle

- Landing is the open half of this batch: everything above is uncommitted in the run
  worktree; the review/fold phase carries it. The rm-166 deadline (2026-09-28T04:13Z
  first cron fire) is the only clock.
- Deferrals with reasons stand as written above: rm-170 (auth-path surgery), rm-171
  (needs live container evidence), rm-157 (G2-blocked on live gateway — agent v0.115.0
  absorb, contract 1.6.0→1.7.0), rm-112 remainder (sequenced on rm-168), rm-114.
- Residue surfaces a push-authorized phase should sweep: `conductor/ci-*`
  ephemeral branches + closed PRs #110/#111, the ~44 `conductor/*` heads, the foreign untracked
  scratch file in the canonical checkout (zz-conflict-lint-check.md).
- Harness hazard worth re-checking next cycle: the run-worktree branch ref was reverted
  to the stale dispatch base twice mid-run by an external actor; next-cycle fix phases
  must re-verify `git rev-parse HEAD` before building any HEAD-based patch.
