# Dashboard maintenance — cycle 13 batch (2026-09-25)

<!-- Renamed from 2026-09-25-cycle-13-batch.md at the integrate merge of run
     11d3a522 into 4d8ec12: the parallel f1ae52a3 batch doc of the same name
     landed on main first (f1ae52a3 = environmental determinism; this doc =
     11d3a522's fail-visible enumeration/toml/shutdown batch). Suffix follows
     the 2026-09-24-cycle-1-batch-2.md convention for a second same-window
     batch doc. -->

- run: `11d3a522b7bf47ec97c9a3e1cdd1756b` (repository-maintenance campaign `a45798f4da014815b981349baee66076`, engine cycle 1 — repo-roadmap cycle 13)
- selector: prioritize pass `857fbce7c5ea4d5697d251deab5888c8`
- base: origin/main `57a182b` (freshness-gated: `git fetch origin` read-only at selection time; the dispatch frame `7809df6` sat 25 commits behind and was re-aligned at the roadmap phase — the ROADMAP in this tree carries this run's research signals and the PR #82 correction below)
- pool: 25 open candidates at `57a182b` (statuses: candidate / partially implemented / blocked / push-gated / time-gated), scored on impact × effort × risk × dependency-unblocking, under the Vision pillar rule that reliability work outranks customer-experience work

## Selection correction of record (feeds B1/B2)

The 2026-09-25 research pass claimed PR #82 and PR #104 were both superseded-by-content at main. That was wrong for PR #82 and the ROADMAP's rm-159 signal has been corrected by this pass:

- PR #104 — superseded-by-content, solid: the identical buildx readback + empty guard landed as rm-166/rm-178 at integrate `217a18866568`.
- PR #82 (`conductor/run-b4aac9ba84e6`, run `b4aac9ba84e6`, OPEN, 5 commits, targeted 702/702 + ephemeral CI green per its body) — only its visual-ubuntu and `.pnpm-store` halves landed elsewhere (PR #12). Its three core halves are UNLANDED at `57a182b`:
  - `pnpm-workspace.yaml` has no `toml` override and the lockfile still resolves `toml@4.1.2` — the fix closing GHSA-82x6-q7mm-w9cf (transitive `@opencode-ai/plugin>effect>toml`, the single open HIGH) is missing from main;
  - `src/github/installations.ts` `enumerateRepos` still `continue`s on per-installation mint/list-repos failures, serving a partial repo union as a complete one;
  - `.gitignore` has no `.conductor/` entry (only `.pnpm-store/` at line 52) — the recurring tracked-breadcrumbs class (#3256/#3233 lineage) stays possible.
  - PR #82's internal `rm-171..rm-176` numbering collides with main's current ids (main's rm-171 = graceful shutdown); rescue is by content with renumbering, closing #82 at the push phase.

## The batch (selected)

### B1. Fail-visible partial installation enumeration — PR #82 B2 rescue, absorbing rm-112's remaining scope

- ids: `rm-112` (remaining halves: resolver-failure absence entries, warm-empty banner, MonitoringDto degradation signal) + PR #82's B2 anchor by content
- value (impact): the top remaining reliability gap — a degraded snapshot is currently indistinguishable from a fresh complete one; this is the fail-closed invariant's data-truth half
- design source: PR #82 commit `9835111` — `EnumerateReposResult.failedInstallationIds` (installation ids only, never account names), `AggregatorSnapshot.enumerationIncomplete` (count on partial, null on total failure/pre-refresh), `markSnapshotStale` consolidating metadata-fail-closed + enumeration-failure + unexpected-refresh-throw, `MonitoringDto` wired additively (SPA rendering deferred to the rm-126/rm-107 consumer decision)
- effort: medium — the design exists and was validated (11 new tests, 8 test files track the grown contract); re-apply on `57a182b`, do not merge the branch
- risk: medium-low — additive Dto fields; the aggregator rework at main (cache-Map removal, verified live) already reshaped the surrounding code, so re-verify each seam against `57a182b` rather than assuming the branch applies clean
- dependency value: unblocks rm-107's operator panel data story next cycle

### B2. toml `'>=4.2.0'` override closing GHSA-82x6-q7mm-w9cf — PR #82 B1 rescue

- value (impact): the single open HIGH advisory (transitive `@opencode-ai/plugin>effect>toml`); `pnpm audit -r` goes to 0 findings
- design source: PR #82 commit `e5986fc` — override lives in `pnpm-workspace.yaml` (pnpm 11 no longer reads the `pnpm` field in package.json, WARN-only); toml re-resolves 4.1.2 -> 5.0.0 under `'>=4.2.0'`; the GitHub dependabot alert sits auto_dismissed (dev-transitive) with rationale recorded
- effort: tiny; risk: low — lockfile refresh + audit gate; watch the rm-133 `minimumReleaseAge: 1440` gate against the override resolution (toml 5.0.0 is months old, expected clean)
- evidence: `pnpm audit -r` 0 findings; lockfile shows the re-resolution; frozen-lockfile install green

### B3. Mint fallback scopes: classify transient vs permission errors (`rm-170`)

- value: security + reliability — today any mint error (including a network blip) triggers the full->core permission fallback and caches the scope-less token ~55min with no telemetry distinguishing scope-loss from error
- effort: small-medium; risk: low — error-shape classification at `src/github/installations.ts:167-190` (the fallback path, distinct from B1's enumeration loop but the same file — sequence B1 first)
- evidence per ROADMAP acceptance: installations tests for both error classes; a forced-transient scenario never produces a cached core-scope token; log marker distinguishes fallback-by-permission from mint-failure

### B4. Graceful shutdown: SIGTERM/SIGINT handlers (`rm-171`)

- value: ops correctness — Node as PID 1 with no handlers means `docker stop` kills in-flight requests and SSE streams mid-frame; no drain, no store close, no shutdown log
- effort: medium; risk: medium — stop accepting, bounded drain, listener store close, reason logging; the aggregator stop wiring is currently only reachable via a server `close` event nothing triggers (make the signal path drive it)
- evidence per ROADMAP acceptance: handler in the server entry; a local `docker stop` during a streaming request logs the drain; suite green

### Riders

- `.gitignore` gains `.conductor/` (PR #82 B3 rescue) and `test/fork-exclusion-guard.test.ts` asserts the entry survives upstream absorbs — closes the recurring tracked-breadcrumbs prevention gap (#3256/#3233 lineage)
- rm-105 narrowed supply-chain rung: `release.yaml` build-push-action gains `sbom: true` + `provenance: mode=max`, runbook note documents the registry-side verification path (imagetools raw manifest + in-toto predicate inspection — live-verified 2026-09-25: `:latest` already carries SLSA provenance v1 at sha256:9543359e…, GitHub-side `attestations` API 404s so `gh attestation verify` cannot see it); evidence expectation rides the next Release run's image index
- push phase: close PR #82 as superseded-by-content when this batch lands (rm-159 signal records the corrected state)

## Excluded, with rationale

- `rm-104` (pri 98): the fix lives in the external hermes-roadmap renderer, not this repo's tree — no repo-side acceptance to complete
- `rm-102` (90): in-progress, evidence pending the ~2026-10-03 dependabot window
- `rm-103` (85) + `rm-157` (72): large absorb-cadence machinery; rm-157 stays G2-deferred (unverifiable without the live gateway) and rm-103's next concrete step rides it
- `rm-159`: push-gated — needs push authority no phase of this run may exercise
- `rm-141`: sequence-blocked (fleet-refresh, gated on upstream absorb sequencing)
- `rm-116` (58) + `rm-146` (38): branch protection / auto-merge — enabling required checks on main would deadlock the fleet's direct-push landing convention itself (three recorded red landings are tolerated precisely because landings are direct pushes; automerging on unprotected main automates the red landings). Requires an orchestration-level decision, not a repo-tree batch
- `rm-107` (65): sequenced after B1's MonitoringDto data — top candidate for cycle 14
- `rm-106` (70): blocked-external (gateway-side contract extension first)
- `rm-136` / `rm-135` / `rm-139`: time-gated (~2026-10), operator decision required (PWA), and an evaluation item — none fit an end-to-end implementation batch this cycle
- `rm-108`/`rm-114`/`rm-117`/`rm-118`/`rm-115` (CSRF): lower value, no thematic coherence with the fail-visible/supply-chain thread

## Sequencing notes for the implement phase

1. B1 first (aggregator/installations seams), then B3 (same file, distinct path), then B2 + riders any time, B4 last (server entry; independent).
2. Re-verify PR #82's design seams against `57a182b` — the aggregator was reworked after that branch was cut.
3. Renumber PR #82's internal ids onto current ROADMAP ids (B2 content folds into rm-112's remaining scope; toml override + gitignore ride as their own signals) — never reuse the branch's internal rm-171..rm-176 numbering.
4. Tests for B1 ride the existing aggregator/installations suites; keep MonitoringDto fields additive.

## Verification

- `pnpm lint`, `pnpm check-types`, full `pnpm test` green on the base `57a182b` + batch tree
- `pnpm audit -r` reports 0 findings after B2
- focused suites for B1/B3 (enumeration + mint-class tests), B4 (drain/stop test or documented procedure)
- ephemeral CI PR pattern (per cycle-11/12 precedent) for remote green before landing
- watch (next compound, time-based): first scheduled base-drift fire 2026-09-28 04:13Z and canary fire 05:23Z — expect green-at-parity (rm-178/rm-179 recorded the pipelines at parity 2026-09-24)
