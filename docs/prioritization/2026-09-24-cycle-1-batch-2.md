---
date: 2026-09-24
topic: dashboard maintenance cycle 1 (run 5b6e8e80) — batch selection, take 2
mode: delegated-conductor
run: 5b6e8e8019524c22b90232a1837e9905
phase: prioritize
attempt: a1bf2e08a6014270846f2c709e7b8c1c
skill: ce-plan (scoring/batch selection, in-process — no dedicated ce-prioritize skill in the installed router). Deviations declared: no subagent surface in this session; interactive gates replaced by autonomous adjudication per work order. The unsuffixed cycle-1 name is taken by run cf5527c1 (2026-09-19) — this is the take-2 record for THIS run's cycle:1.
---

# Dashboard maintenance — cycle:1 batch, take 2 (2026-09-24)

## Live frame facts

- Fork tip `origin/main` == `539c632` (verified 2026-09-24, unchanged across this run's assess → implement).
- CI green at the exact assessed sha (Main run 2026-09-24 08:22Z). The one red (base-drift run 35970747110, 07:38Z) is a FALSE positive: the workflow's live-digest extraction came back EMPTY (anonymous Docker Hub manifest rate limiting) and compared unequal to the pin while registry truth was pin == live `sha256:0e0ff40c…` — Docker Hub token + registry HEAD verified during assess/research.
- PR #82 (parallel campaign, cycle-11) OPEN at selection time — owns `rm-171..176` and touches `src/github/installations.ts` + `test/installations.test.ts` (overlap flagged for B3; content-disjoint ROADMAP append adjacency).
- First weekly base-drift cron fires **2026-09-28 04:13 UTC** and will false-red without rm-177.

## Selected batch — "cycle:1 reliability/security repair batch" (3 items + 2 riders)

**B1 = rm-177 — base-drift false-red repair** (reliability 57.0, assess F1)
- Impact: HIGH and time-boxed (cron 2026-09-28). Risk: LOW (workflow-only). Effort: S.
- Acceptance: empty LIVE_DIGEST → distinct extraction-failure message (not "drift"); curl token/HEAD fallback when `docker manifest inspect` yields no digest; a drift verdict requires BOTH digests non-empty in the log; actionlint clean (container form).

**B2 = rm-179 — metadata `private` fail-closed coercion** (security 29.0, assess F3)
- Impact: MED-HIGH — closes the only redaction-bypass class found (`private: "true"` string passed `=== true` as public). Invariant #2. Risk: LOW (fail-closed direction). Effort: S.
- Acceptance: fixtures `private: "true"` / `1` / `null` / `"false"` → denylist, never public; coerced count surfaced in a warning; suite green.

**B3 = rm-180 — installation-token expiresAt seam** (reliability 27.0, assess F4)
- Impact: MEDIUM — removes the 401-mid-cycle class (cache guessed 55 min; real expiry discarded at `app-client.ts`). Risk: LOW-MED (hot path). Effort: M-.
- Acceptance: cache honors a 5-minute expiresAt fixture at the right refresh boundary; 55 min becomes a cap/fallback, not the truth source.

**Rider R1 — ROADMAP extension byte-identical**: `ROADMAP.md` == the roadmap phase's proposed file (sha `6cb678f0a108e294`; 60 items, max rm-181, ids collision-free against PR #82's rm-171..176 and the live max rm-165) so rm-177..181 + 13 status truthings land with the batch.

**Rider R2 (stretch, cut first) — rm-178 collapse detector**: log-once warn + runbook line on `server.ts:577-611`. Deferred this cycle.

## Scoring

| item | impact | risk | effort | deps | strategic | verdict |
|---|---|---|---|---|---|---|
| rm-177 | high (time-boxed) | low | S | none | high | IN — anchor |
| rm-179 | med-high (invariant) | low | S | none | high | IN |
| rm-180 | medium | low-med | M- | none | med-high | IN |
| rm-178 | medium | low | S | rm-129 landed | med | stretch (cut) |
| rm-107 panel | high | med | L | operator decision | high | DEFER — 4th recurrence; decide-or-ship next cycle |
| rm-181 draft hygiene | low | low | S | none | low-med | DEFER |
| rm-171..176 | varies | — | — | PR #82 in flight | — | EXCLUDE — parallel run owns; reconcile rm-112↔rm-172 on landing |
| rm-103/rm-108 pin riders | low | low | S | none | low | EXCLUDE — dependabot window per rm-133 (re-eval 2026-10-21) |

Coherence: three small, independent, test-bounded correctness/reliability repairs on surfaces freshly verified this run — one reviewer context, one CI loop.

## Implementation record (implement attempt 20e41d4e, 2026-09-24)

Worktree `conductor/run-5b6e8e801952` rebased `7809df6` → `539c632` (`git reset --hard origin/main`) so the batch sits on the live frame; the 20-commit gap touched none of the src surfaces (diffstat: only `.github/workflows/base-drift.yaml` differed, and it is B1's target).

- **B1** `.github/workflows/base-drift.yaml`: extraction moved into `extract_live_digest()` — primary `docker manifest inspect` (stdout-guarded, `|| true` under `set -euo pipefail`), fallback Docker Hub token (`auth.docker.io`) + registry HEAD (`registry-1.docker.io`, `docker-content-digest` header), and an empty-result guard that reds with the DISTINCT message "live digest EXTRACTION FAILED (empty) — … NOT drift". The existing empty-PIN guard completes the both-digests-nonempty precondition. `::warning::` notices go to stderr so they never pollute the captured digest.
- **B2** `src/github/metadata.ts`: the `private === true` gate is now fail-closed — any value that is not strictly `false` or absent classifies the entry private (denylisted, never queried); coerced values are counted (`privateCoercedCount`) and surfaced via `logger.warning`.
- **B3** `src/github/installations.ts` + `src/github/app-client.ts`: new `MintedToken {token, expiresAt}` seams through `InstallationsClient.mintInstallationToken`, `mintReadOnlyToken`'s `mintFn`, and `DashboardAppClient.mintInstallationToken`; `app-client` normalizes the auth result's real `expiresAt` (ISO string or Date; `null` on ill-formed) instead of discarding it; `setCachedToken` honors it with a 55-min CAP (far-future and NaN expiries fall back to the cap — a NaN must never compare fresh forever). `server.ts:987` needed no change (types flow).
- **Tests**: `test/metadata.test.ts` +5 fixtures (string/number/null/string-false → denylisted; coerced-count warning surfaced); `test/installations.test.ts` +2 fake-timer tests (5-min expiry honored at the real boundary; 8-h expiry capped at 55 min); mint fakes in `test/installations.test.ts`, `test/server.test.ts`, `test/aggregator.test.ts` updated to the `MintedToken` shape.
- **R1**: `ROADMAP.md` copied byte-identical from the roadmap artifact — `sha256sum ROADMAP.md` → `6cb678f0a108e294…`.

## Verification

- `pnpm exec vitest run test/metadata.test.ts test/installations.test.ts test/aggregator.test.ts test/server.test.ts` (impacted suites).
- `pnpm check-types`; targeted `eslint` on every changed file; `actionlint` (container form) on `.github/workflows/base-drift.yaml`.
- Full suite + house gates at the pushed HEAD belong to the later validation gate.
