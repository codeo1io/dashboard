# dashboard — Roadmap

> Autonomously maintained by the roadmap sync (reliability-first). Items cite reproducible codebase signals; acceptance is proven by cited evidence.

<!-- manual-revision 2026-09-20: restored + extended by maintenance cycle run 270220e7 (roadmap phase)
     after the 2026-09-20 fleet render (2f3a884) regressed: it re-introduced the exact bare-array
     lint failure rm-104 predicted (ROADMAP.md:20:32, Main run 35479225529 Lint red), resurrected
     superseded rm-001/rm-002 with pytest/ast evidence on vendored .agents/skills/impeccable paths,
     deleted every tracked open item and this directive, and added a Fleet context section naming
     hermes-agent as upstream (not this repo's upstream; removed). This revision restores the
     3075f4a content, updates cycle-1 items against what landed at 3075f4a (dependabot.yml present;
     actionlint container form in main.yaml; AGENTS.md Fro Bot note present — rm-109 completed),
     and adds cycle-2 items from run 270220e7 assess findings and
     docs/ideation/2026-09-20-repository-extensions-ideation.md.
     hermes-roadmap render: preserve or explicitly supersede these items on the next render; do not
     silently drop them. rm-001/rm-002 remain superseded — do not resurrect vendored-path signals.
     cycle-1 outcome (2026-09-19, run cf5527c1): rm-100 (container actionlint + codeql-action digest
     1c5b675), rm-102 (.github/dependabot.yml), rm-109 (AGENTS.md Fro Bot ops note) landed on
     origin/main at 3075f4a. CodeQL green at the pushed sha remains open (see rm-100). Batch
     decisions: docs/prioritization/2026-09-19-cycle-1-batch.md.
     manual-revision 2026-09-20 (run 779e7271, cycle-2 batch-2 implement — lineage reconciliation):
     the two divergent curated lineages were reconciled with this file (aa4ff9f) as base.
     Id-collision mapping applied verbatim from docs/prioritization/2026-09-20-cycle-2-batch-2.md:
     unlanded f4622d7e items renumbered rm-116 merge-hygiene, rm-117 security panel, rm-118 per-repo
     tokens, rm-119 gate-health roll-up, rm-120 gateway watch, rm-121 binding-docs sync (implemented
     this batch), and its rm-118 aggregator-hygiene folded into landed rm-112; this run's items
     renumbered rm-122 port upstream #481 (implemented this batch), rm-123 digest-drift, rm-124
     server/docs hygiene (implemented this batch). Landed-id meanings (rm-112..rm-115 at 916783f)
     unchanged. rm-100/rm-110/rm-113 closed against aa4ff9f outcomes (Main 35490785353 green,
     CodeQL 35490785207 green); rm-111 CLOSED at compound time — Release 35491419600 completed
     success at aa4ff9f with the '🚦 Enforce fixed HIGH/CRITICAL' Trivy step green (image 9e6cbb41,
     zero enforceable findings). cycle-2 outcome (2026-09-20, run 779e7271 compound): batch-2
     (B0/B2/B3/B4/B5) implemented and validated pre-review in this tree (lint/check-types/test
     3026/3026, actionlint container 0); review/shipping outcomes follow this phase and carry into
     the next cycle's assessment. Next-cycle candidates queued: rm-116, rm-119, rm-123, rm-117,
     rm-118, rm-120. Cycle lessons compounded to
     docs/solutions/workflow-issues/parallel-conductor-campaigns-frame-movement-collisions-2026-09-20.md,
     docs/solutions/best-practices/binding-doc-consistency-greps-avoid-pathspec-miss-2026-09-20.md, and
     docs/solutions/workflow-issues/codeql-semmle-typescript-home-2026-09-20.md
     manual-revision 2026-09-20 (run f69cd740, repository-maintenance cycle 1 — repo research lineage
     cycle 3; research artifact docs/ideation/2026-09-20-repository-extensions-research-2.md):
     added rm-125..rm-130 from this run's assess findings and the cycle-3 research re-measure;
     refreshed rm-103 signals (drift 8: the two new upstream commits are the #495/#496 privacy
     policy, absorbed by rm-125), rm-106 (UNBLOCKED — upstream published the public operator push
     privacy policy, this item's stated precondition), rm-107 (healthz placeholder-null confirmation),
     rm-116 (re-probed 2026-09-20: still unprotected), rm-119 (first all-green tip discovered only
     out-of-band). Completed and superseded sections untouched.
     manual-revision 2026-09-22 (run f11b7255, repository-maintenance cycle 4 — repo research lineage
     cycle 5; research artifact docs/ideation/2026-09-22-repository-extensions-research.md):
     added rm-142..rm-148 from this run's assess findings (worktree HEAD 7809df6 == origin/main
     aa9937f) and the 5th research refresh; refreshed rm-112 (cache-TTL no-op re-verified live at
     aa9937f; denylistComplete disagreement NOT substantiated on current main), rm-123 (pin == live
     digest again via registry API 2026-09-22; the detector exists only in open PR #10), rm-126
     (fix sits in open PR #10 = stranded rm-135 payload, pending landing). Id-collision mapping:
     open PR #10's ROADMAP copy carries stranded rm-134..rm-141 (the 2026-09-21 research doc's
     rm-131..134 dispositions, renumbered); those ids remain RESERVED — new items start at rm-142;
     on merge, resolve by content, never renumber or duplicate stranded ids.
     Reconciliation executed 2026-09-22 (run f11b7255 implement, cycle-7 B1.1): stranded rm-134..rm-141 joined
     this copy by content — implemented-status lines from the stranded lineage win verbatim, rm-123 signals carry both
     resolutions with the 2026-09-22 self-resolved digest correction, and rm-128/125/130/131/132/133 stand recorded
     completed by the stranded lineage at f334fb2 / 2f4d118.
     hermes-roadmap render: preserve or explicitly supersede these items on the next render; do not
     silently drop them. rm-001/rm-002 remain superseded — do not resurrect vendored-path signals.
     manual-revision 2026-09-24 (run 04c7d9c2, repository-maintenance cycle 9 — lineage doc
     docs/prioritization/2026-09-24-cycle-9-batch.md): this run's roadmap phase authored
     rm-148..rm-153 in a worktree whose base (7809df6) went stale mid-cycle — origin/main advanced
     to 763c1fa and landed rm-136/rm-137/rm-145/rm-148 covering four of those six items, so the
     unlanded ids never reconciled into this file. Id mapping for cross-referencing the batch doc:
     unlanded rm-148 (release-trigger completeness) is re-authored here as rm-154 (implemented
     this cycle); unlanded rm-149 (fro-bot/agent v0.115.0 absorb) as rm-155, itself renumbered
     rm-157 at compound time (2026-09-24) after the git log --all audit surfaced
     conductor/run-91e4626dccaf 943d3ed already using rm-155/156/158 for HTTP-bounding
     work — the governance rule below applied with one phase's lag;
     unlanded rm-150..rm-153 are content-absorbed by landed rm-145, rm-136+rm-148, rm-148, rm-137
     respectively and stay unallocated. Governance rule (learned the hard way twice now): before
     allocating ids, check open PR titles AND git log --all -- ROADMAP.md for stranded lineages —
     HEAD history alone under-counts the id space.
     manual-revision 2026-09-24 (run 3538ec96, repository-maintenance campaign 32d16713 cycle 1 —
     repo research lineage cycle 10; research artifact staged for docs/ideation/ — hygiene held it
     out-of-tree this phase, the landing phase commits it): added rm-164/rm-165 from this run's
     assess (1a9c85ac, base 7809df6, every finding currency-checked at origin/main 82312f7) and
     research (501c5ddc); dated signals appended to rm-103/rm-107/rm-108/rm-112/rm-129/rm-139/
     rm-141 — existing items otherwise verbatim. The id audit applied the governance rule above and
     found a THIRD colliding lineage: open PR #43 (run 6cbc2a1d, b0fc297, merge-base d73fbe7)
     claims rm-155..rm-163 in meanings that collide with landed rm-157/rm-159 AND with 943d3ed's
     rm-155/156/158 — on that merge, resolve by content, landed meanings win, stranded ids
     renumber; new ids therefore mint above the all-lineage max (rm-163). Completed and superseded
     sections untouched. -->
<!-- cycle-6 compound (2026-09-22, conductor run 2ae7d10a68d04c1b88e536e280a4e405: implement bce42c8c + targeted_tests 0a7d4d13 + compound 03a9efd3; folds re-applied after a worktree rollback ate the in-phase edits): rm-135/rm-136/rm-137-pins-half/rm-123/rm-126/rm-134 implemented in-tree on the 92688a2 base — validation green (suite 3116/3116 = 3115+1 exactly, actionlint 0, types 0), pending commit/push/CI; rm-106 → blocked-external (gateway-side push contract); stranded rm-132 renumbered rm-141; the 2 remaining PR-diff docs were already on main content-identical (da6809d absorb — no restore needed); commit phase explicitly stages the fro-bot.yaml/visual.yaml/dependabot.yml riders -->
<!-- cycle-7 compound (2026-09-22, conductor run f11b7255b43c40b491a43480550838dc — repo lineage cycle-7, engine label cycle:4; assess a7e67232 + research f38d8956 + roadmap 7088c41a + prioritize 2f4eaa22 + stewardship 6309385b + implement e9f71b1c + targeted_tests a6c0c5bd + compound ca21ae49): B1 landed PR #10's 18-file payload in-tree via worktree reset to aa9937f + cherry-pick --no-commit (0 conflicts, exactly as merge-tree predicted) with ROADMAP reconciled by content (50 ids, reservation rule honored); B2 deterministic visual gate — job container mcr.microsoft.com/playwright:v1.63.0-noble (digest sha256:eff16c30e6f3…), runtime chromium step removed, all three dark baselines regenerated AND verified 3/3 inside that image; B3 hosted-era truthing + setup-node cache:pnpm + eslint 10.11.0 absorb; B4 docs/runbooks/security-posture.md (Scorecard 7.1 by-design deviations). Pre-review validation: actionlint 0, lint 0, types 0, suite 3116/3116 (2038 server + 1078 web), visual 3/3 in-image — staged 37 files +1633/−213 including this compound's three solution docs, NOT committed; review/shipping outcomes follow this phase and carry into the next cycle's assessment. Mid-cycle discovery compounded as a prevention rule: PR #10's base-drift.yaml still declared runs-on self-hosted (authored under the retired policy; the hosted flip lived on another lineage) — flipped in-batch; see docs/solutions/best-practices/stranded-pr-landing-reaudit-payload-against-current-policy-2026-09-22.md. Next-cycle candidates queued: rm-116 (branch protection — gates rm-146 dependabot auto-merge), rm-144 (property suites for both SSE parsers + listener ingest), rm-112 fix-half (per-repo cache TTL 60s == refresh interval 60s no-op; decision-heavy, deferred), rm-147 stays blocked-external (license decision). Watch items: dependabot first docker/npm PRs ~2026-10-03 (rm-102 window opened 2026-09-19), Scorecard Maintained self-resolves ~2026-11-08 → update the security-posture row then. Cycle lessons compounded to docs/solutions/workflow-issues/container-pnpm-install-poisons-host-node-modules-2026-09-22.md and docs/solutions/workflow-issues/visual-baseline-provenance-pinned-image-2026-09-22.md -->
<!-- cycle-9 compound (2026-09-24, conductor run 04c7d9c2949a40a49b96587440fcbf93 — repo lineage cycle 9; assess d2a81faa + research 433a7ece + roadmap 4b1665c5 + prioritize 6d0f6d1e + stewardship e36b5047 + implement 9b17c4e7 + targeted_tests 409ba5bb + full_tests d4b515a6 + compound 3799ed24): batch 'close the silent-failure gaps' (docs/prioritization/2026-09-24-cycle-9-batch.md) implemented in-tree at base 763c1fa after the stale-base reset (7809df6 was a diverged sibling, nothing unique lost) — B1 release-trigger completeness for pnpm-workspace.yaml (release.yaml paths + should-release.ts isHardReleasePath + test/release-trigger-paths.test.ts structural guard, negative-verified), B2 rm-112 fix-halves (dead per-repo cache REMOVED with the decision recorded at the site; repository:null now fails visible stale:true with tests), B0 ROADMAP reconciliation, Unit C staged untrack of the tracked .conductor residue. Riders: conductor/ci-base-** added to the pull_request filters of main.yaml/codeql.yaml/dependency-review.yaml (the zero-checks wedge, see solutions doc). Pre-review validation: local suite 3124/3124 (2046 server + 1078 web, 0 failed), lint 0, types 0, actionlint 0; the cloud ephemeral-PR route registered 9 checks post-filter-fix — 8 green plus exactly one structural red (rm-131, the exclude-pathspec artifact documented at that item; self-cures when Unit C's untrack lands on main). Id-space: rm-155 renumbered rm-157 (943d3ed collision), rm-159 minted (origin validation-residue sweep). Next-cycle candidates queued: rm-157 (fro-bot/agent v0.115.0 absorb — needs the live gateway), rm-159, rm-112 remaining halves (resolver-failure absence entries, warm-empty banner, MonitoringDto degradation signal), rm-116/rm-144 carried from cycle 7. Watch items: dependabot first docker/npm PRs ~2026-10-03 (rm-102 window), rm-133 major-pin re-evaluation 2026-10-21. Cycle lessons compounded to docs/solutions/workflow-issues/ephemeral-ci-base-pr-excluded-by-branch-filters-2026-09-24.md and docs/solutions/workflow-issues/validation-clone-exclude-pathspec-drops-staged-breadcrumb-deletion-2026-09-24.md -->
<!-- cycle-10 compound (2026-09-24, conductor run 3538ec96ece446b18ca438b9e485928f — repo lineage cycle 10, engine label cycle:1; assess 1a9c85ac + research 501c5ddc + roadmap 1c078a48 + prioritize 6cf9420d + stewardship 67a20d24 + implement e8f1b4fa + targeted_tests b3f1ebad + full_tests a17c7793/530dc3f6/6deb972f + compound 8303168f): batch 'fair throttle, true prose, hardened parsers' (docs/prioritization/2026-09-24-cycle-10-batch.md) implemented in-tree at base 82312f7 after the rm-165-motivated re-alignment (dispatch base 7809df6 sat 18 behind with its one unique commit superseded at main) — B1 rm-129 rate limiter re-keyed with per-path-class budgets (public/operator/ingest, each with its own env — ingest is its OWN budget, not an exemption) + opt-in `rateLimitTrustedProxy` XFF keying, legacy bare-call 429-compat preserved (68/68); B2 rm-164 six stale self-hosted prose sites era-qualified/dropped + filter-free repo-wide guard with curated allowlist (19/19, negative-verified); B3 rm-144 property suites on fast-check 4.9.0 pinned as explicit devDep — ingest contract, SSE framing, attention ordering (17 tests, five generator/spec bugs caught during development); B0 ROADMAP patch applied byte-identical (sha 1de6707a…) + ideation/batch doc placements; bfra-me workflow-pin rider SKIPPED on failed premise (live upstream v4.33.0 vs the batch doc's v4.32.0, and no fork workflow pin exists — the only bfra-me surfaces are the npm @bfra.me/* deps, rm-103 proper). full_tests took three dispatches: sole structural rm-131 cloud red (the documented exclude-pathspec artifact, byte-consistent across six red snapshots) → cured in the fix-bearing surface by scoping the guard's assertion to durable refs (it.skipIf on the engine's conductor/ci-* prefix — a scope correction, not a weakening: still hard-fails on main/real PRs, 15/15 locally with `git ls-files .conductor` empty) → verbatim full_command rc=0 ok=true (ephemeral PR #86, run 35966577151: 37 files/2075 passed + 1 skipped server, 28 files/1078 web, guard's skip visible in the CI log). Pre-review validation consumed as recorded evidence per compound rules — no suites re-run in this phase; lint 0/types 0 had been re-verified after the last test-file edit. LANDING-CRITICAL: the staged `git rm -r --cached .conductor` (13 breadcrumbs, the #3256 cure) MUST ride the landing merge or post-landing CI re-reds rm-131 on durable refs. Next-cycle candidates queued: operator self-observability panel over the dead /api/monitoring surface (research idea 1, top pick, unselected — assess F3), rm-107 remaining cache-growth half (assess F4), rm-141 sequence-blocked behind PR #45's src/github/* lock (assess F2/F5 fixes ride that PR), rm-165 verification (next assess breadcrumb records HEAD == origin/main tip at dispatch), rm-103 @bfra.me/* npm refresh. Watch items: dependabot first docker/npm PRs ~2026-10-03 (rm-102 window), rm-133 major-pin re-evaluation 2026-10-21, PR #43 (b0fc297: CSRF ack tokens + security.txt + pnpm 11.27.1 + ROADMAP rm-155..163 id collision) and PR #45 (943d3ed) still open — landing reconciles by content. Cycle lessons compounded to docs/solutions/workflow-issues/validation-clone-exclude-pathspec-drops-staged-breadcrumb-deletion-2026-09-24.md (updated with the durable-ref guard-scope cure), docs/solutions/workflow-issues/lint-after-any-test-file-edit-regexp-rules-2026-09-24.md (new), docs/solutions/best-practices/property-test-generator-pitfalls-fast-check-2026-09-24.md (new) -->
<!-- manual-revision 2026-09-24 (run 6fe26972, repository-maintenance cycle 1 — assess 06462e0a + research fa89b529 + roadmap 368f26dd; built on origin/main 539c632 fetched fresh, not the stale dispatch frame 7809df6): added rm-177 (aggregator GraphQL query syntax — live GitHub rejects both templates' //-comments, proven via `gh api graphql` UNKNOWN_CHAR 2026-09-24; verified still un-owned in every open lineage: PR #82 c2ae28ab9962 and PR #104 a4a97e30f3a2 both carry both comments), rm-178 (base-drift digest extraction broken-by-construction — `docker manifest inspect` emits JSON with no `^Digest:` line so LIVE_DIGEST is always empty; all 4 recorded runs false-red; registry HEAD == pin under both Accept headers, the historical 5cbc7cab reading was a per-platform digest from --verbose dumps; PR #104's rewrite keeps the broken command at its base-drift.yaml:48), rm-179 (reality canary: structural query guard + GITHUB_TOKEN-only live smoke — distinct from PR #104's rm-167 pin-drift watch, which watches version pins not the runtime contract), rm-180 (code-truth micro-batch: dead parseRepoResponse openAlertCount param, envIntOrDefault parseInt lenience). Id-space: all-lineage max verified rm-176 (branch conductor/run-b4aac9ba84e6) — new ids mint above it; enumeration-gap visibility deliberately NOT re-minted — doubly in-flight as PR #82 rm-172 and PR #104 rm-164, reconcile by content on landing. Dated signals appended: rm-103 (third 2026-09-24 measure — upstream delta 4 dep-only commits at tip 9908bcf; npm surface at latest except two minors; all GitHub Actions pins at latest majors), rm-123 (re-opened: extraction broken, fix split to rm-178, first-green acceptance moves there), rm-133 (watch note: jsdom 30.1.1 third major; @bfra.me toolchain compat precondition for TS 7; node 24 base EOL 2028-04-30 — no platform pressure). Correction of record for the 2026-09-20 header provenance below: its 'rm-106 (UNBLOCKED …)' line was already stale by the 2026-09-22 re-assessment — the authoritative status remains blocked-external at the item; dated history comments stay verbatim, this note is the correction. Completed and superseded sections untouched. -->
<!-- manual-revision 2026-09-24 (run 6fe26972 compound 8313fdd8, pre-review): rm-177..rm-180 status flipped candidate→implemented with recorded validation evidence (targeted_tests df92b07a: 665/665 after the web/dist fresh-worktree build cure; full_tests d1e71ea0: ephemeral CI PR #105 commit bdd68b4, 9/9 checks SUCCESS incl. Check Workflows over the new canary.yaml; dispatch digest e54b76e5 declared verbatim at stable fingerprint 4f51664b821b8502). Durable lessons written to docs/solutions/workflow-issues/: graphql-template-comments-rejected-live-green-ci-transport-fakes-2026-09-24.md, ci-digest-readback-empty-extraction-is-error-2026-09-24.md, fresh-worktree-missing-web-dist-static-assets-red-2026-09-24.md. Residual watches for next cycle: 2026-09-28 04:13Z base-drift cron (expected first-ever green at parity), 2026-09-28 05:23Z canary first fire, PR #104 same-file reconcile by content. -->
<!-- manual-revision 2026-09-24 (run a4a97e30, repository-maintenance cycle 1 of this work order — roadmap phase attempt f8230f54, based on origin/main 763c1fa fetched fresh, NOT the stale worktree frame 7809df6): id-space audit per the governance rule found open PR #43 (run 6cbc2a1d) already carrying rm-160..rm-163 in its stranded lineage — new items here start at rm-164 (on merge, reconcile by content, never renumber). Added rm-164 (enumeration-gap visibility, assess F2), rm-165 (dead gateway-client surfaces + header truth, assess F3), rm-166 (docs-truth micro-batch 3: README Workbox claim, vite precache comments, ideation wiki-writer residue, assess F4/F5/F6), rm-167 (extend the drift watch to dependency/action pins, research C1). Dated signals appended to rm-103 (fresh upstream measure), rm-106 (blocked re-check at v0.115.0), rm-120 (reference clone absent from every checkout), rm-129 (scope widened to the sensitive-path limiter, assess F1), rm-138 (comment-truth half split to rm-166). Status corrections: rm-123/rm-126/rm-134/rm-135/rm-136/rm-137/rm-142/rm-143/rm-145/rm-148 verified landed on origin/main 763c1fa (payload entered main via c4404af land-cycle-7-batch; PR #10 itself remains open-but-superseded, rm-159 sweep family) and relocated to Completed with per-item landing evidence. [Preserved verbatim at the 2026-09-25 a4a97e30 integrate; the relocation pass did not ride this merge — see the integrate-merge comment.] -->
<!-- manual-revision 2026-09-25 (run 11d3a522b7bf47ec97c9a3e1cdd1756b, repository-maintenance campaign a45798f4, engine label cycle:1 — assess d54ddc27 + research 94f6de68 + roadmap f325f9ff; built on origin/main 57a182b fetched fresh, NOT the stale dispatch frame 7809df6 which sat 25 behind with its content superseded — worktree re-aligned by ancestor-safe reset after backing up .conductor/): NO new ids minted — every fresh signal from this run's research maps onto an already-tracked item (two research passes landed 2026-09-24, keeping the discovery bar for new ids high; the six rejected new-item candidates with reasons live in the research ideation artifact /tmp/compound-engineering-1001/ce-ideate/11d3a522-research/ideation-2026-09-25-repository-extensions.md). Dated signals appended to rm-103 (fifth upstream measure), rm-105 (LIVE provenance discovery — the 'no attestations' signal was half-stale), rm-146 (allow_update_branch precondition), rm-157 (rider set grew), rm-159 (residue counts + two superseded-by-content PRs). rm-133 is COMPLETED so its 2026-09-25 negative measure rides THIS comment per the completed-sections-untouched rule: typescript-eslint 8.70.1 (latest) still peers `typescript >=4.8.4 <6.1.0`, so TS 7.0.2 stays hard-blocked for the 2026-10-21 re-eval; @bfra.me/eslint-config@0.52.2 declares no typescript peer of its own — the gate rides its transitive typescript-eslint, as recorded. CORRECTION OF RECORD for this run's research PhaseResult: it mislabeled the supply-chain item as rm-142 (rm-142 is the implemented visual deterministic gate); the correct id is rm-105, and the evidence string's 'workbox 7.4.1' upstream-commit claim was wrong — the true five-commit delta at eb8ca95 carries no workbox commit (verified by git log origin/main..autonomy-upstream/main; no workbox pin exists in this repo). Completed and superseded sections untouched. -->

<!-- manual-revision 2026-09-25 (run f1ae52a3f04b, repository-maintenance cycle:1 — assess 9ca979e9 + research 9ca3c1b6 + roadmap 2979c123; built on origin/main 57a182b fetched fresh, not the stale dispatch frame 7809df6): added rm-186 (no .dockerignore — every docker build ships the repo-root context including .git, node_modules, and any locally-present .env to the daemon; verified absent on main and in both open-lineage PR trees #113/#114), rm-187 (src/listener/store.ts:51 unguarded JSON.parse(row.links) — one corrupt cell 500s all of GET /api/listener/messages; PR #113's store.ts touch is the dedupe-un-ack bug and leaves its line-53 twin unguarded, so no open lineage covers it), rm-188 (ubuntu-latest alias-flip guard — actions/runner-images maps ubuntu-latest to 24.04 while ubuntu-26.04 images are already published (README fetched 2026-09-25); 16 runs-on: ubuntu-latest sites across 9 workflow files ride the alias and only the visual job is containerized). Id-space: all-lineage max verified rm-183 (origin/conductor/run-d6455cdcf51c, PR #113) — new ids mint above it. Deliberately NOT minted (dedupe): the metadata private truthy fail-open (src/github/metadata.ts:286 `entry.private === true` — independently re-found live on main 57a182b by this run's assess, corroborating the in-flight fix riding PR #114 b3ea173b's metadata.ts + test/metadata.test.ts + metadata-private-flag-fail-closed solution doc); run-status checkout-provenance surfacing (in flight as an item gated on rm-157 in PR #113); dependency majors TS7/vitest5/jsdom30/pnpm12/Node26 (already tracked rm-108/133/139/140 — refreshed as dated signals, not new items). Dated signals appended: rm-157 (pending absorb set grew to 5 commits 0c7489d..eb8ca95: pnpm 11.27.1 #520, fro-bot/agent v0.115.0 #521, bfra-me/.github v4.32/v4.33 #519/#523, codeql-action digest 2892aa5 #524; touch set exactly codeql.yaml/fro-bot.yaml/release.yaml/renovate.yaml/scorecard.yaml/package.json — renovate.yaml is fork-absent (rm-136 keep-deleted) and release.yaml is fork-diverged by the #3274 App-token gating, the two pre-identified conflict points), rm-140 (pnpm latest now 12.6.0, was 12.5.1), rm-108 (dated refresh 2026-09-25: in-range drift hono 4.13.9 vs lockfile 4.13.8, @hono/node-server 2.1.1, vite 8.3.1 rides the rm-102 dependabot window; typescript 7.0.2/vitest 5.0.1/jsdom 30.1.1 unchanged since the 2026-09-24 measure with the typescript-eslint <6.1.0 peer blockage standing; endoflife.date re-confirms node v24.21.0 active LTS with support ending 2026-10-20, EOL 2028-04-30, v26.10.0 Current; scorecard-action v2.4.4 == latest). Completed and superseded sections untouched.  Id-renumber at the integrate merge into d00d095: the parallel cycle:1 batch (run 5b6e8e801952) landed on main first and owns rm-181..rm-185 (its rm-184 = metadata private fail-closed, its rm-185 = installation-token expiry), so cycle-13's items moved rm-184→rm-186 (dockerignore seal), rm-185→rm-187 (links-parse), rm-186→rm-188 (runner pins); every in-repo reference (these items, this header, the .dockerignore provenance header, the dockerfile-context guard block, the nine workflow re-eval comments, the cycle-13 batch doc) updated in lockstep.-->

<!-- manual-revision 2026-09-25 (run ec07634c80bc46e9b5be50b1ef333913, repository-maintenance cycle:1 — assess 82fc77b8 + research 57cc38a7 + roadmap 2edb645c + prioritize a86bf91b + stewardship 5e3c40ec + implement c3bbc0c8; built on origin/main 6fd7d04 fetched fresh, not the stale dispatch frame 7809df6 — worktree re-aligned by ancestor-safe reset preserving untracked .conductor/): added rm-197 (aggregator cold-start in-flight degradation contract — the last false-fresh window after 6fd7d04 closed rm-112's warm half), rm-198 (boot-time last-known snapshot bridge, decision-gated, supersedes the rm-112 TTL-removal rationale restart-only), rm-199 (degradation delivery — zero triggers while signals exist consumer-less; three recorded delivery forms, decision-gated), rm-200 (redaction single-char token boundary), rm-201 (aggregator/shutdown lifecycle re-entry), rm-202 (AGENTS.md check-types gate description). Id-space: minted above the all-lineage max rm-196 — run 4e7c674's integrate (8bbff5c6) renumbered its batch rm-181..rm-186→rm-191..rm-196 while this run's roadmap phase was composing, so this batch's items mint rm-197..rm-202 (the phantom rm-191..rm-196 allocation in this run's roadmap PhaseResult text is void — see chain note). CHAIN-INTEGRITY DISCLOSURE: this run's roadmap phase (2edb645c) recorded succeeded but materialized only its orientation step — no ledger edit, no spool artifact (consumed), one breadcrumb; the delta is materialized HERE by the prioritize phase's B0 definition (docs/prioritization/2026-09-25-cycle-14-batch.md §B0) with the renumber applied. B5 stretch rider DROPPED as a phantom: its 'rm-183 kill-switch per-request re-read' title association was stale — rm-183 is the rate-limit collapse warning and no kill-switch read site exists in src/ at 6fd7d04 (grep kill → web PWA only). Dated signal appended to rm-157 (v0.115.1 supersedes v0.115.0-latest; measured 2026-09-25 live). rm-143 is COMPLETED so its fresh measure rides THIS comment per the completed-sections-untouched rule: Scorecard 7.8 (api.securityscorecards.dev, analyzed 2026-09-24T21:49Z, up from 7.1 at 2026-09-22) with all six sub-10 rows unchanged (Maintained 0 self-resolves ~2026-11-08; Code-Review 0; CII 0; Signed-Releases −1; Branch-Protection −1 = rm-116 open; License 0 = rm-147 blocked-external, upstream license re-probed 404 2026-09-25). Completed and superseded sections untouched. -->

<!-- manual-revision 2026-09-25 (run ec07634c80bc46e9b5be50b1ef333913 compound d49ae5a3, pre-review — consuming implement c3bbc0c8 + targeted_tests 8b11a67e + full_tests 1cab3836 outcomes only, no local test re-run): rm-197/rm-200/rm-201/rm-202 statuses flipped implemented-in-tree → implemented-and-validated-pre-review with the gate record inline (local focused 248/248; authoritative ephemeral-PR validation ALL 9 checks SUCCESS twice — PR #146 @ f2a23bd3 via the targeted command's built-in shared-config fallback, PR #147 @ e55dcb1 via full_command verbatim; both PRs auto-closed; origin/main still 6fd7d04 throughout, no landing raced in). rm-198/rm-199 remain decision-gated candidates untouched — the next cycle's assessment carries review/shipping outcomes per the compound contract. Durable lessons written to docs/solutions/best-practices/: roadmap-id-renumber-race-parallel-landing-2026-09-25.md (mint-time is not id ownership — re-verify the ledger ceiling at implement start and renumber in lockstep; this cycle's third fleet renumber after d00d095 and the 5b6e8e80 one) and property-assertion-must-mirror-anchoring-2026-09-25.md (a boundary-anchored scrubber cannot be property-tested with not.toContain — lexical embedding is not identity survival; a generator's min-length silently blinds whole token classes). Also recorded in those docs: the B5 phantom (stale roadmap-title association burned a batch slot — verify the read site exists before accepting an item) and the hollow-roadmap-phase chain-integrity event (prioritize verified prior-phase artifacts on disk before building — narration is not materialization). Batch doc docs/prioritization/2026-09-25-cycle-14-batch.md appended a pre-review outcome section. -->
<!-- manual-revision 2026-09-25 (run ec07634c80bc46e9b5be50b1ef333913 independent_review fix 07afb0e5 — closing review 6f4e6190 verdict REQUEST_CHANGES, 1×P1 + 2×P3, all three findings fixed): F1/P1 — rm-197 acceptance (2) had been recorded done while the two GitHub transports constructed in server.ts (metadata contents Octokit :1056, per-repo graphql.defaults :1080) carried NO timeout and the new in-diff comments asserted the opposite; both now wired with GITHUB_REQUEST_TIMEOUT_MS, comments reworded to enumerate all five transport constructions, and test/transport-timeout-contract.test.ts pins the wiring at source level (the unit fakes hid the real transports — rm-190 fixture-seam lesson repeating). F2/P3 — the full `owner/name` pair token now replaced UNANCHORED ('a/b-42' no longer leaks the name; bare tokens stay anchored; residual documented). F3/P3 — EMPTY_SNAPSHOT duplicates in server.ts + routes/api.ts consolidated to exported COLD_START_SNAPSHOT with a parity assert. rm-197/rm-200 status + evidence lines extended above. -->
<!-- INTEGRATE-MERGE 2026-09-25 (candidate 3013451, run a4a97e30 cycle-10 batch 'fail-visible enumeration, pruned gateway, true docs', PR #104 — merged into main 8ef806f lineage; conflict case b96d9163): the batch was authored at base 82312f7/763c1fa while main landed cycles 11–14 (57a182b→8ef806f), so every same-file hunk was resolved against main's LANDED truth, landed meanings own ids (the renumber convention of d00d095/8bbff5c6 — the batch's own 'reconcile by content' reservation honored). ID RECONCILIATION: the batch's rm-164..rm-167 all collided with landed meanings (rm-164 = cycle-10 prose-residue guard, rm-165 = worktree-base freshness, rm-166 = base-drift readback repair, rm-167 = should-release rule 2), so the two items whose content survives land as rm-203 (docs-truth micro-batch 3, theirs rm-166) and rm-204 (pin-drift watch, theirs rm-167). CONTENT DISPOSITIONS (B labels per the batch doc's own authored mapping — B1 enumeration, B2 pin-drift, B3 hygiene pair): B1 enumeration visibility (theirs rm-164) — CONVERGED with main's landed cycle-13 B1 `failedInstallationIds`→`enumerationIncomplete` (rm-112, 481b840); the merged tree keeps the landed field everywhere (src/github/installations.ts, aggregator, server, routes, web dto), their `skippedInstallations` shape was superseded before landing — recorded at rm-112. B2 pin-drift (theirs rm-167) — job merged into .github/workflows/base-drift.yaml alongside main's rm-178-fixed digest job, adapted to main's rm-188 ubuntu-24.04 pin → rm-204. B3 hygiene pair (theirs rm-165 + rm-166) — the prune half SUPERSEDED by main's landed rm-193 operator-contract parsers on exactly those surfaces (launchRun/getRunSnapshot/connectRunStream stay, hardened); the header-truth half APPLIED: the operator-client module header now states the live getCurrentSession use and zero-non-test-caller status truthfully (recorded at rm-193); the docs-truth half clean-merged (README stack list, web/vite.config.ts comments, binding-docs exclusion-set widening) → rm-203. NOT TAKEN from theirs: the batch doc path collision (landed cycle-10 doc keeps the canonical name; theirs renamed to 2026-09-24-cycle-10-batch-run-a4a97e30.md with a landing header), the 10-item relocation-to-Completed pass (main keeps those items in-place with landed-status notes), and theirs' aggregator/installations/server/routes/test files whose shapes predate the landed cycle-13/14 semantics (main's versions kept byte-for-byte; the batch's parser-prune test edits would have resurrected superseded shapes). Dated signals the batch appended to rm-103/rm-106/rm-120/rm-129/rm-138 were superseded by main's later measures at the same items (theirs measured at 763c1fa/9908bcf; main's measures at 57a182b..6fd7d04 are fresher) — theirs preserved verbatim in the batch doc, not duplicated here. -->

<!-- INTEGRATE-MERGE 2026-09-25 (candidate 399447b, run b4aac9ba cycle-11 batch 'fail-visible enumeration, toml GHSA override, gitignore belt-and-braces', PR #82 — merged into main 6371757 lineage; conflict case 636b69c7): the batch was authored at base 7809df6 while main landed cycles 11–14 (57a182b→6371757), INCLUDING cycle-13's content-rescue of this very batch (compound 753a69d9 at dd86ef5: the toml '>=4.2.0' override, the fail-visible partial-enumeration half, and the `.conductor/` gitignore half all landed by content — lockfile re-resolved to toml 5.0.0, `pnpm audit -r` clean, GHSA-82x6-q7mm-w9cf closed; PR #82 closed unmerged 2026-09-24T19:43Z by run 11d3a522 pr 6f885f6a) — so every same-file hunk was resolved against main's LANDED truth, landed meanings own ids (the d00d095/8bbff5c6 renumber convention). ID RECONCILIATION: the batch's rm-171..rm-176 were minted in-flight while the fleet's cycles 11–13 landed; rm-171 and rm-172 now carry landed meanings (graceful shutdown cycle-13 B4; SPA shell cache cycle-11 B5) and rm-173..rm-176 are released by this landing. CONTENT DISPOSITIONS (B labels per the batch doc's own mapping): B1 toml override (theirs rm-171) — CONVERGED with the landed cycle-13 rescue: pnpm-workspace.yaml keeps the landed override and now carries the batch's GHSA rationale comment with corrected lineage. B2 fail-visible enumeration (theirs rm-172) — CONVERGED with rm-112's landed `failedInstallationIds`→`enumerationIncomplete` channel incl. the banner-on-partial review fix F2: main's installations/aggregator/server/routes/test files kept byte-for-byte, the batch's parallel shapes (`staleBanner: false` on partial; the un-bannered EMPTY_SNAPSHOT cold-start literal rm-197/F3 removed from routes/api.ts) predate the landed cycle-13/14 semantics and were NOT taken, and the batch's aggregator/installations test additions (auto-merged, asserting the superseded banner-off-on-partial shape) were reverted to main's landed suites. B3 gitignore belt-and-braces (theirs rm-175) — CONVERGED: `.pnpm-store/` and `.conductor/` both landed in .gitignore with the guard's gitignore assertions. FOLDED, not separate items: theirs rm-173 (keyless attestations; the batch's own sequencing 'after rm-105's SBOM/provenance step') rides rm-105's remaining post-landing scope — its Scorecard Signed-Releases acceptance contradicts the recorded deliberate design (no GitHub releases ship; image-based deploys, rm-143 notes) — dated signal appended at rm-105; theirs rm-176 (explicit fork license posture, OWNER DECISION REQUIRED) is rm-147's interim acceptance clause — dated signal appended at rm-147. ONE item survives as new content: the standing zlib1g CVE-2023-45853 triage (theirs rm-174) → rm-205, minted above the all-lineage max rm-204. NOT TAKEN from theirs: the batch doc path collision (main's landed cycle-11 doc from run 41c7d471 keeps the canonical name; theirs renamed to 2026-09-24-cycle-11-batch-run-b4aac9ba.md with a landing header) and the dated signals the batch appended to rm-102/rm-103/rm-105/rm-131/rm-133 (measured at 7809df6; main's measures at 57a182b..6371757 are fresher, and the two events they uniquely record — the dependabot-vs-local-audit toml divergence and the 82312f7 guard recurrence — are both since cured and recorded at rm-159's cycle-13 note and rm-112/rm-131's landing resolutions) — theirs preserved verbatim in the renamed batch doc, not duplicated here. -->

<!-- INTEGRATE-MERGE 2026-09-25 (candidate 76c7e2f, run c2ae28ab cycle-10 batch 'green main, pins, fail-visible thirds, hygiene riders', PR #97 — merged into main 0616de2 lineage; conflict case 6a7122e8): the batch was authored at base 7809df6 while main landed cycles 11–14 (57a182b→0616de2), including cycle-13's content-reconcile of this very PR (rm-186 landed the .dockerignore seal by content from PR #97's measurement) — so every same-file hunk was resolved against main's LANDED truth, landed meanings own ids (the d00d095/8bbff5c6/a4a97e30 renumber convention). ID RECONCILIATION: the batch's rm-164/165/166 (green-main repair / hygiene riders / binding-doc claims) all collide with landed meanings (rm-164 = cycle-10 prose-residue guard, rm-165 = worktree-base freshness, rm-166 = base-drift readback repair); one surviving item mints as rm-206. CONTENT DISPOSITIONS (B labels per the batch doc's own authored mapping): B1 green-main-at-tip repair (theirs rm-164) — CONVERGED: main's cures landed independently (breadcrumbs untracked, .conductor/ gitignored at rm-175, landing merges clean; `git ls-files .conductor` empty at 0616de2). B2 branch-protection fill (live settings, rm-116) — NOT CONVERGED, corrected at independent review (case 0bbfc38f, live re-probe 2026-09-25): the batch's in-tree-recorded live fill (7 required checks + strict + enforce_admins, re-read verified in-batch) did NOT persist — the live protection object reads as an empty shell again (0 checks, strict false, enforce_admins false), matching rm-116's landed candidate status; rm-116 stays open, and only its acceptance clause 3 (the hotfix-override recipe in AGENTS.md, worded to the shell state) landed at this merge. B3 pins (pnpm 11.27.1 + node index digest) — CONVERGED: packageManager/Dockerfile corepack + digest pins and the guard's 11.27.1 all landed on main independently; the batch's own drift citation was a measurement artifact (per-platform manifest vs index digest — see the renamed batch doc and the landed digest-readback solution docs). B4 fail-visible thirds (rm-112 rescope) — SUPERSEDED by main's landed rm-112 channel (`failedInstallationIds`→`enumerationIncomplete`, resolver-failure absence entries, warm-empty handling, staleBanner on partial): main's installations/aggregator/server/routes/tests kept byte-for-byte; the batch's parallel `degradation` object (enumerationFailed/failedInstallations/warmEmpty/absentRepos) predates the landed shape — recorded at rm-112. B5 hygiene riders (theirs rm-165) — the .dockerignore half CONVERGED with rm-186 (identical patterns; main's reconcile header retained); the cookie-key canonical-format enforcement half SURVIVES as unique content → rm-206 (src/session.ts + test/session.test.ts kept, citations renumbered off the collided rm-165; README.md Configuration paragraph landed); theirs rm-166 (machine-verified binding-doc claims) SUPERSEDED by main's landed guards (test/prose-residue-guard.test.ts + fork-exclusion-guard BINDING_DOCS evolution). NOT TAKEN from theirs: the batch doc path collision (main's landed cycle-10 doc from run 3538ec96 keeps the canonical name; theirs renamed to 2026-09-24-cycle-10-batch-run-c2ae28ab9962.md with a landing header); the batch's web view-riders (web/src/api/monitoring.ts, DegradationBanner + its test, AppShell wiring) — view consumption of the degradation signal stays decision-gated at rm-107/rm-199 and web/src/App.test.tsx:73-85 regression-guards that no monitoring view exists; the auto-merged degradation-shape test edits (test/dashboard.test.ts reverted to the landed snapshot maker; aggregator/installations/operator/static-assets/fork-exclusion conflicts kept byte-for-byte main). TAKEN with a landing header: docs/solutions/workflow-issues/container-digest-pin-index-vs-platform-manifest-2026-09-24.md (its index-vs-platform-manifest framing is the standalone statement; its instance-3 'latent base-drift extraction' framing is superseded by main's landed rm-166/178/182 readback repairs). -->

**Vision**: A reliable, customer-friendly repository advanced by evidence-cited roadmap cycles owned by the autonomy loop

**Pillars**: reliability work outranks customer-experience work; every roadmap item cites reproducible codebase signals; acceptance is proven by cited evidence, never claimed

<!-- cycle-1 extension #2 (2026-09-24, conductor run 5b6e8e8019524c22b90232a1837e9905 — repository-maintenance cycle:1; assess 536cff99 + research b27ad37b + roadmap 2c0cabed): five new items originally minted rm-177..rm-181 above the then-known max (origin/main 539c632 tops at rm-165; in-flight PR #82 holds rm-171..rm-176 from parallel run b4aac9ba — no overlap with either). INTEGRATE-MERGE RENUMBER 2026-09-24: the landing branch had meanwhile integrated cycle-12 (run 6fe26972), which owns the LANDED rm-177..rm-180 meanings — landed meanings win ids, in-flight claims renumber (the cycle-12 convention), so this block's items carry rm-182/rm-183/rm-184/rm-185 (from rm-177/rm-178/rm-179/rm-180; rm-181 kept — free on both sides); rm-182 CONVERGES with landed rm-166 (cycle-11) + rm-178 (cycle-12) on the base-drift readback repair. Dated signals appended to rm-103/107/108/112/114/123/165; cycle-9/10 pending-landing statuses truthed to completed/landed with direct at-539c632 verification (grep/test-presence evidence cited inline). Completed and superseded history untouched. -->

<!-- cycle-1 extension (2026-09-25, conductor run 71991cb31f2041ee9932217a420ceb22 — repository-maintenance cycle:1 parallel run; assess 8e16b753 + research 77dda854 + roadmap 8fb8bd41; minted rm-207..rm-209 above the all-lineage max rm-206 (PR-strand heads verified at ≤rm-199); dated signals appended to rm-102/rm-103/rm-105/rm-117/rm-165/rm-181/rm-192/rm-205; rm-105 flipped candidate→implemented with its post-landing attestation verification CLOSED; authored against origin/main 20ade64 fetched fresh, NOT the stale 7809df6 worktree frame, per the rm-165 cure) -->
<!-- cycle-17 compound (2026-09-25, conductor run 71991cb31f2041ee9932217a420ceb22 — repository-maintenance cycle:1 parallel run, batch 'release-image truth: CVE census response + gate coverage'; phases assess 8e16b753 + research 77dda854 + roadmap 8fb8bd41 + prioritize ac3bd6f0 + stewardship a24c9316 + implement 73932b46 + targeted_tests c8303835 + full_tests b6300bc9; this compound appended cycle signals to rm-117/157/165/205/207/208/209, queued next-cycle context in docs/prioritization/2026-09-25-cycle-17-batch-run-71991cb31f2.md, and codified the stale-dispatch-base + parallel-lineage mint-collision protocol in docs/solutions/workflow-issues/stale-dispatch-base-and-parallel-lineage-mint-collision-2026-09-25.md; validation evidence: engine ephemeral PRs #170 + #174, 10/10 checks each) -->

## Open items

### Harden the roadmap render (vendored-path exclusion, stack-correct evidence, lint-clean output)
- id: `rm-104` | track: reliability | priority: 98.0 | status: candidate (ESCALATED: failure recurred 2026-09-20)
- signals: the 2026-09-20 fleet render (2f3a884) repeated every defect class rm-104 predicted: bare-array prose broke `pnpm lint` at ROADMAP.md:20:32 (main unlandable; run 35479225529), pytest/ast evidence strings re-cited in a Node 24/pnpm/Vitest repo, all 21 signals again under vendored `.agents/skills/impeccable/**`, and — new this time — the manual-revision directive, superseded section, and all tracked open items rm-100/102/103/104/105/106/107/108/109/110 were silently deleted (88 deletions in `git show 2f3a884 -- ROADMAP.md`)
- acceptance:
  - next hermes-roadmap render emits zero signals under `.agents/skills/impeccable/**` (vendored-path exclusion)
  - rendered evidence strings reference repo-native commands only (`pnpm lint`, `pnpm check-types`, `pnpm test`, `gh run ...`), never pytest/ast tooling
  - `pnpm lint` exits 0 immediately after a render (generator lints its own output before commit)
  - manual-revision, superseded, and completed sections survive a render verbatim unless explicitly superseded; tracked items may only close with an explicit superseded marker citing evidence
- evidence: diff of ROADMAP.md across the next sync commit; `pnpm lint` exit 0 at that commit; upstream fro-bot tracking issue/PR link for the generator fix; delegate-side recovery recipe documented at `docs/solutions/workflow-issues/fleet-roadmap-render-clobber-recovery-2026-09-20.md` (compound-phase 2026-09-20)

### Aggregator GraphQL queries are a live syntax error (//-comments inside templates)
- id: `rm-177` | track: reliability | priority: 97.0 | status: implemented 2026-09-24 (run 6fe26972 implement 6e6cde38, validated df92b07a/d1e71ea0 — both comments now `#`-style and hoisted prose-free, both templates exported as the guard/canary seam; test/query-shape-guard.test.ts negative-verified; targeted 665/665 + ephemeral CI PR #105 9/9 green; live proof: `node scripts/graphql-canary.ts` exit 0 against the real API with the exact shipped REPO_STATUS_QUERY; pending landing)
- signals: `src/github/aggregator.ts:131` and `:169` (origin/main 539c632) carry `//`-style comments INSIDE the two GraphQL template literals (REPO_STATUS_QUERY / REPO_STATUS_QUERY_NO_ALERTS; text: `// GraphQL max page size; repos with >100 suites still understate failingChecks (documented ceiling, rm-110)`); GraphQL comments are `#`-prefixed — GitHub's API rejects the query verbatim, proven live 2026-09-24: `gh api graphql -f query='… // c …'` returns `Expected NAME, actual: UNKNOWN_CHAR ("/") at [4, 5]`; the transport sends the template unmodified (`gql(query, variables)` in src/server.ts), so every per-repo status fetch falls into the catch and all repos render `stale:true, rollupState:'unknown', failingChecks:0, openPrCount:0, openIssueCount:0, openAlertCount:null` (aggregator.ts:585-586) — a falsely-calm board; introduced 7de0de3 2026-09-20; every aggregator suite either fakes the transport (27 tests in test/aggregator.test.ts) or exercises the parser offline (test/aggregator-invariants.property.test.ts, rm-144) — none executes a real query, so nothing could catch this class; still un-owned in every open lineage as of 2026-09-24 (PR #82 c2ae28ab9962 and PR #104 a4a97e30f3a2 both carry both comments — PR #104 at shifted lines 139/177)
- acceptance: the two comments become `#`-style or move outside the templates (the rm-110 ceiling note survives in valid form); a structural guard test asserts no exported query template contains a `//`-prefixed line (negative-verified); full suite green; the board's live data path recovers — proven by rm-179's canary or one recorded live query
- evidence: `gh api graphql` rejection transcript recorded in the cycle batch doc; before/after live query or canary output; guard test green

### base-drift digest extraction yields empty (gate can never pass)
- id: `rm-178` | track: reliability | priority: 91.0 | status: implemented 2026-09-24 (run 6fe26972 implement 6e6cde38, validated via ephemeral CI Check Workflows — extraction swapped to `docker buildx imagetools inspect | awk '/^Digest:/'` with empty⇒extraction-error exit 1, header documents both wrong-tool traps; pipeline proven at parity on-runner 2026-09-24: buildx read sha256:0e0ff40c39bc… == Dockerfile pin, so the 2026-09-28 04:13Z cron is expected green-at-parity — first scheduled fire pending; fix un-owned in PR #104's lineage, reconcile by content on landing; LANDING RESOLUTION 2026-09-24 at integrate 217a18866568: run 41c7d471's rm-166 (cycle-11, candidate 52259e6) independently implemented the identical buildx readback + empty guard — merged here as ONE repair, with rm-166's rider test/base-drift-digest-readback.test.ts fencing both wrong-readback classes repo-wide)
- signals: `.github/workflows/base-drift.yaml:39` (origin/main 539c632; PR #104's branch at :48) extracts LIVE_DIGEST via `docker manifest inspect node:24-slim | awk '/^Digest:/{print $2; exit}'` — `docker manifest inspect` emits JSON with no `^Digest:` line, so extraction is ALWAYS empty: reproduced locally (`… | wc -c` to 0, exit 0) and visible in CI run 35970747110 (`live node:24-slim digest: ` empty, then pin != empty-string verdict); all 4 recorded workflow runs failed — the gate has never been green and every red is a false positive; first scheduled fire Mon 2026-09-28 04:13 UTC; registry truth 2026-09-24: token-authed HEAD probes return docker-content-digest sha256:0e0ff40c39bc… == the Dockerfile pin under BOTH oci-index and docker-list Accept headers — the historical `live 5cbc7cab` readings were per-platform manifest digests from `--verbose` dumps (wrong digest class)
- acceptance: extraction switched to the repo's proven convention (release.yaml's `docker buildx imagetools inspect` default pretty-dump `Digest:` line — the 2026-09-19 digest-parse solution doc family) OR a token-authed registry HEAD probe reading Docker-Content-Digest; empty extraction hard-fails as `extraction error`, never as drift; a manual dispatch goes green at parity (pin == live) before the 2026-09-28 cron; actionlint container-form gate passes
- evidence: dispatch run log printing both digests with a no-drift verdict; a deliberate mismatch dry-run (or fixture) proving the alert path still fires on REAL divergence; actionlint exit 0

### Land fork dependency automation — first-PR proof
- id: `rm-102` | track: reliability | priority: 90.0 | status: in-progress (config landed 3075f4a 2026-09-19; first dependabot PR evidence pending ~2026-10-03)
- signals: `.github/dependabot.yml` present on origin/main (landed at 3075f4a — verified by `git log origin/main -- .github/dependabot.yml`); Renovate never runs here (PR #1 deleted renovate.yaml; config .github/renovate.json5 persists uninvoked); weekly cadence, first window opened 2026-09-19 (fresh measure 2026-09-24, run 41c7d471 assess: gh pr list --author 'app/dependabot' -> 0 PRs ~10 days into the window; closes ~2026-10-03) (fresh measure 2026-09-25, run 71991cb3 research 77dda854: `gh pr list --author app/dependabot` → still 0 PRs ~6 days into the window; queued candidates for the first window: codeql-action codeql-bundle-v2.27.1 @ 416ff0de (fork pins 2892aa5), vite 8.3.1, fast-check 4.10.2)
- acceptance: first dependabot-authored PR visible within 14 days of 2026-09-19; grouped updates honored (npm, docker digests, github-actions; `open-pull-requests-limit: 3` for the single serialized runner); `pnpm test` green after the first merged automated bump
- evidence: `gh pr list --author "app/dependabot"` non-empty; merge commit's Main run green

### Automated upstream-absorb cadence with drift gate
- id: `rm-103` | track: reliability | priority: 85.0 | status: candidate
- signals: drift recurs by design — 2 commits behind on 2026-09-19, 6 behind on 2026-09-20, 8 behind on 2026-09-20 second measure (run f69cd740 research: the two new commits 30839c6 + 3f2fbe9 are the privacy-policy feature absorbed as rm-125; everything else content-converged) (node digests churn ~daily; two tag rebuilds in 24h); absorption is manual-only; upstream PR #481 (redaction chokepoint) MERGED upstream 2026-09-19 and ported by rm-122; upstream #493 (clonedeps-skill removal) must be SKIPPED on absorb — the fork keeps its clonedep artifacts deliberately as the v0.78.0 reference source (rm-120); fresh measure 2026-09-21 (run 87e3c32f research): 3 commits behind (da6809d upstream #511 push never auto-registers without user action, 232f353 upstream #508 unknown reconcile inputs no longer destroy live subscriptions, 31564c4 docs on proxy-collapsed rate-limit identities) — the fork SHIPS pre-fix reconcile.ts/subscribe.ts, so #511/#508 are live bug fixes on absorb, not noise; upstream open PR #512 (stream retry without snapshot) is the next watch item; scope is push/docs files only (10 files, 635 insertions since merge base 9f9adc5), so this absorb batch is low-residue-risk, but the exclusion hazards ride in the merge base itself — pair with rm-131 (fresh measure 2026-09-21T21:50Z, run 2ae7d10a research: upstream delta 0 — da6809d fully absorbed, #508/#511 live on main, so this item's next instance is whatever lands upstream next; absorb ledger folded 2026-09-22 from the stranded cycle-4 batch: #497 (Caddy public-route) absorbed as docs/solutions/integration-issues/public-route-swallowed-by-caddy-extensionless-rewrite-2026-09-20.md — riding the rm-135 payload, #498's wiki-writer-existence claim is a standing never-absorb-verbatim item (rm-131 guard), #499 moot — all three were inside the da6809d absorb that took upstream delta to 0) (fresh measure 2026-09-24, run 04c7d9c2 research: upstream delta 3 dep-only commits — f35e281/0c7489d/b8c7982 carrying pnpm 11.27.1, fro-bot/agent 0.114.1, eslint 10.11.0, each already content-absorbed on the fork; the next absorb instance is whatever lands upstream after f35e281) (fresh measure 2026-09-24, run 3538ec96 research 501c5ddc: upstream delta is 3 again — merge-base 0c7489d, commits #519 bfra-me/.github v4.32.0, #520 pnpm 11.27.1, #521 fro-bot/agent v0.115.0, with fork pins at pnpm 11.27.0 / agent v0.114.1 (b711f08) — the agent half is rm-157's payload, pnpm+bfra-me are cheap absorb riders; upstream PR surface empty at gh pr list -R fro-bot/dashboard) (fresh measure 2026-09-24 third, run 6fe26972 research fa89b529: upstream delta grew to 4 dep-only commits at tip 9908bcf — #523 bfra-me/.github v4.33.0 added atop #519/#520/#521; fork pnpm 11.27.0 vs 11.27.1 already rides the PR #43 lineage; bfra-me action-side N/A (no fork workflow pin); npm view 2026-09-24: only two minors trail the lockfile — hono 4.13.8 to 4.13.9 and fast-check 4.9.0 to 4.10.2 — while vite 8.3.0, react 19.3.0, tailwind 4.3.3, eslint 10.11.0, playwright 1.63.0, the @bfra.me trio, and the octokit family all sit at npm latest; every GitHub Actions pin verified at its latest major: checkout v7.0.1, setup-node v7.0.0, upload-artifact v7.0.1, create-github-app-token v3.2.0, dependency-review v5.0.0) (fresh measure 2026-09-24, run 41c7d471 research 5cf830f2 — CORRECTION: git's true merge-base is 9f9adc5 and the upstream delta is 12 commits (#508..#523), not 3; the actionable set is unchanged at 2 — #521 agent v0.115.0 (rm-157's payload) + #520 pnpm 11.27.1 rider; #519/#523 (bfra-me/.github v4.32.0/v4.33.0) touch only .github/workflows/renovate.yaml which the fork deleted per rm-136 — VERIFIED no-op via git show --stat; #512's no-snapshot terminal-run guard is already on-fork content (public/operator-stream.js:676-683), #514's state-machines doc is present, #516's eslint 10.11.0 is absorbed; upstream PR surface still empty) (fresh measure 2026-09-24, run 5b6e8e801952 research: upstream delta 4, all low-value — e51b480/9908bcf touch only .github/workflows/renovate.yaml (deleted fork-side, rm-136), 66a542d pins fro-bot/agent v0.115.0 = rm-157's contract bump, f35e281 pnpm 11.27.1 rider; disposition document-and-close, no absorb batch warranted this cycle) (fresh measure 2026-09-25, run 11d3a522 research 94f6de68: `git rev-list --count origin/main..autonomy-upstream/main` → 5 at tip eb8ca95, DOWN from the 12-commit 2026-09-24 measure — seven upstream commits became history-absorbed by the cycle-10/11 landings; the five: eb8ca95 #524 codeql-action digest 2892aa5 across codeql/release/scorecard workflows (a REAL rider — the fork pins 1c5b675), 9908bcf #523 bfra-me v4.33.0 and e51b480 #519 bfra-me v4.32.0 (both verified no-ops: renovate.yaml absent per rm-136), 66a542d #521 fro-bot/agent v0.115.0 (rm-157's payload), f35e281 #520 pnpm 11.27.1 rider; actionable set unchanged at three — rm-157 plus the pnpm and codeql-digest riders riding the same absorb batch; npm surface 2026-09-25: typescript-eslint 8.70.1 still peers `typescript >=4.8.4 <6.1.0` (TS 7.0.2 stays blocked, feeds rm-133's 2026-10-21 re-eval), @bfra.me/eslint-config@0.52.2 is latest with no own typescript peer — the gate rides transitive typescript-eslint; only trivial minors trail (fast-check 4.10.2, vite 8.3.1) plus caret drift (hono 4.13.9, @hono/node-server 2.1.1) that the next lockfile refresh absorbs; octokit family, react 19.3.0, tailwind 4.3.3, playwright 1.63.0 (no mcr 1.64 image exists), yaml 2.9.1, eslint 10.11.0 all sit at npm latest) (fresh measure 2026-09-25, run 71991cb3 research 77dda854: upstream delta 7 at tip 1c9cbc1 — new since eb8ca95: #525 fro-bot/agent v0.115.1 (latest release 2026-09-24T22:51Z; fork pins 8e6494ed v0.115.0 at fro-bot.yaml:340 = rm-157's payload) and #526 @opencode-ai/plugin 1.18.32 (fork pins 1.18.31 at package.json:35, dev-only); the fork tip 20ade64 ALREADY carries #524's codeql digest 2892aa5 and #520's pnpm 11.27.1, so the actionable absorb set is exactly TWO riders — agent digest bump + plugin dev-dep; npm surface unchanged from the same-day 94f6de68 measure)
- implementation note (2026-09-20, f4622d7e lineage): the 2026-09-20 absorb's CONTENT (hono 4.13.8 + base digest 0e0ff40) landed as direct edits via rm-111 rather than a history merge, so upstream history stays ahead while content converged — a future history absorb must not double-apply (Dockerfile pins and pnpm-lock.yaml already sit at the upstream values)
- acceptance: scheduled workflow compares HEAD to `autonomy-upstream/main` and opens a human-approved merge PR when behind (auto-prepare + gate, never auto-push — fork exclusions demand human review); drift past a documented threshold opens an alert issue; #481 fast-followed within one cycle of merging upstream
- evidence: workflow run logs showing the compare step; merge PR links; `git rev-list --count HEAD..autonomy-upstream/main` → 0 (or ≤ threshold with an open alert)

### Supply-chain baseline v2: SBOM + build provenance in Release
- id: `rm-105` | track: security | priority: 80.0 | status: implemented 2026-09-25 (rider landed with the integrate to 20ade64; cycle-13 rider implemented in-tree 2026-09-25, run 11d3a522 implement a14443cc, landed: `.github/workflows/release.yaml` build-push step now sets `sbom: true` and `provenance: mode=max` — actionlint clean; the registry-side verification runbook landed as docs/solutions/workflow-issues/registry-attestation-verification-2026-09-25.md; post-landing: confirm the next release run emits TWO attestation layers (provenance + SBOM) on the published index and that `gh api repos/codeo1io/dashboard/attestations` still 404s as expected for registry-embedded attestations — VERIFIED CLOSED 2026-09-25, run 71991cb3 research 77dda854, see the dated signal below)
- signals: `.github/workflows/release.yaml` has no sbom/attest/provenance/cosign step (verified by grep, 2026-09-19) while Scorecard v2.4.4 is already green — attestations are the missing next rung of the 2026-07-31 security-workflow-baseline lineage (dated refresh 2026-09-25, run 11d3a522 research 94f6de68: the 'no attestations' half is STALE — LIVE registry inspection shows build-push-action v7's implicit provenance already ships: `docker buildx imagetools inspect ghcr.io/codeo1io/dashboard:latest` → index sha256:9543359e… carries exactly ONE attestation-manifest (sha256:137fef6c…, unknown/unknown) whose raw manifest declares artifactType application/vnd.docker.attestation.manifest.v1+json with an in-toto layer annotated predicate-type https://slsa.dev/provenance/v1 (35567 bytes); SBOM is ABSENT (single attestation layer) and `gh api repos/codeo1io/dashboard/attestations?digest=…` → 404 in both query and path forms, so `gh attestation verify` cannot see registry-embedded attestations — verification must stay registry-side, which this item's evidence line already anticipates) (dated signal 2026-09-25 at the 636b69c7 integrate: candidate run b4aac9ba's keyless-attestations item (its rm-173, sequenced 'after rm-105's SBOM/provenance step') folds into this item's remaining post-landing scope; its Scorecard Signed-Releases acceptance contradicts the recorded deliberate design — no GitHub releases ship, deploys are image-based (rm-143 notes) — so it is not carried as a separate open item) (post-landing verification CLOSED 2026-09-25, run 71991cb3 research 77dda854: the Release run at 20ade64 (05:12Z, success) published `:latest` whose OCI index carries the image manifest plus ONE attestation-manifest whose TWO in-toto layers decode to named statements `https://spdx.dev/Document` and `https://slsa.dev/provenance/v1` — both rider outputs live on the published index; `gh api repos/codeo1io/dashboard/attestations` still 404s, as anticipated)
- acceptance: Release publishes the GHCR image with SBOM (syft) artifact and build provenance (`actions/attest-build-provenance`); verification reads the digest via the Digest line (`awk '/^Digest:/{print $2; exit}'` — the `--format` template is silently ignored for attestation-bearing indexes on this buildx) and confirms the attestation manifest exists
- evidence: release run log showing SBOM + attest steps green; registry manifest for the released digest shows attestation layer; docs note how an operator verifies (`docker buildx imagetools inspect` or `gh attestation verify`)

### Aggregator degradation and staleness semantics
- id: `rm-112` | track: reliability | priority: 75.0 | status: implemented 2026-09-25 (SERVER SURFACE landed run 4e7c674 implement f9529920, integrated at this merge onto main's landed `enumerationIncomplete` channel: resolver-failure absence entries, warm-empty banner, the MonitoringDto degradation signal, plus the independent-review hardening — warm-empty absence/last-good dedup on node_id preferring the absence verdict, with last-true fetchedAt inherited so absence rows never read as fresher than their last real fetch (review fix c8b926e6, 2 new tests, aggregator suite 62/62); FIELD CONVERGENCE at the integrate: the batch's parallel `degradedInstallations` channel (minted at 57a182b before main's cycle-13 B1 was known) was superseded by main's landed `enumerationIncomplete` (number on partial via failedInstallationIds, null on total failure) — the merged tree keeps the landed field and carries the batch's warm-empty/absence semantics under it; 508 focused green + full-suite CI green at snapshots 8c58add (PR #125) and f5d2721 (PR #128); the VIEW-CONSUMPTION half remains deferred to the rm-107 monitoring-consumer decision — web/ was untouched by the batch and web/src/App.test.tsx:73-85 regression-guards against a monitoring view existing, so nothing yet renders the banner/degradation signal in the SPA; earlier halves closed on main: cache-Map removal at 539c632 2026-09-24, and the installation-enumeration half (`failedInstallationIds` → `enumerationIncomplete`) with cycle-13 B1 (run 11d3a522, landed 481b840 2026-09-25); updated 2026-09-25 run 4e7c674 compound 9d398097 + review fix c8b926e6; FIELD CONVERGENCE 2026-09-25 at the a4a97e30 integrate (candidate 3013451, PR #104): the batch's parallel `skippedInstallations` phase-counts channel (theirs rm-164, minted at 763c1fa before cycle-13 B1 was known) was superseded by the landed `failedInstallationIds`→`enumerationIncomplete` channel — the merged tree keeps the landed field at every layer (installations, aggregator, server, routes/api, web dto); the batch's token-mint vs list-repos phase-count detail remains available to rm-107's composed panel through the landed shape); THIRD convergence 2026-09-25 at the c2ae28ab integrate (candidate 76c7e2f, PR #97, conflict case 6a7122e8): the batch's own `degradation` object channel (enumerationFailed/failedInstallations/warmEmpty/absentRepos, minted at 7809df6 before main's cycle-13 B1 was known) superseded by the landed field — main's shapes kept byte-for-byte at every layer, the batch's web view-riders not taken (rm-107/rm-199 decision gate; web/src/App.test.tsx:73-85 guards no monitoring view))
- signals: `src/github/aggregator.ts` silently drops repos on resolver failure (line 686) and a warm-empty working set replaces `lastGoodSnapshot` with fresh-empty without a stale banner (lines 704-706), contradicting the file's own fail-closed cold-start path (lines 624-641); no test covers either branch (verified 2026-09-20); the operator cannot distinguish "all quiet" from "data lost" (currency check 2026-09-24, run 3538ec96 assess F4: at origin/main 82312f7 the per-repo cache Map is still live (aggregator.ts:609, read-side TTL only at :717-718, no delete/prune) — the cycle-9 removal rides open PR #45, so that landing closes this half; the remaining halves (resolver-failure absence entries, warm-empty banner, MonitoringDto degradation signal) stay open) (THIS HALF RESOLVED 2026-09-24, run 5b6e8e801952: the per-repo cache removal IS landed at 539c632 — src/github/aggregator.ts:716-726 carries the removal comment in-tree and the repo loop consults no cache; the remaining halves overlapped in-flight PR #82's rm-172 enumeration-failure visibility — reconciled by content at the cycle-13 landing, PR #82 closed unmerged 2026-09-25 (run 11d3a522 pr 6f885f6a)) — 2026-09-25 update (run 4e7c674 roadmap): the installation-level twin persisted at main 57a182b: src/github/installations.ts ~:222 logs "Failed to mint installation token; skipping install" and returns ok() with that installation's repos silently absent — a whole install can vanish with no absence entry, banner, or driftCount movement (verified 2026-09-25 run 4e7c674 assess F4; being closed by this cycle's batch, landing at this integrate onto the cycle-13 B1 `failedInstallationIds` seam)
- absorbed signals (2026-09-20, from the f4622d7e lineage item formerly rm-118 — aggregator hygiene batch): the snapshot cache TTL equals the refresh interval (60s) so the cache never serves between refreshes (dead weight plus a false sense of caching); the denylistComplete doc comment and its code disagree; failed installation resolution has no negative caching so a dead install re-hits the GitHub API every cycle; `.github/renovate.json5` persists with nothing invoking it since PR #1 deleted renovate.yaml — undocumented dead config inviting confusion with the landed dependabot (rm-102); fresh re-verify 2026-09-22 (run f11b7255 assess, origin/main aa9937f): the cache-TTL-equals-interval no-op is live (aggregator.ts:715-733 cache check vs :785-796 interval — a fast refresh cycle ages every entry past TTL, so all per-repo GraphQL queries re-run every cycle) and is NOT addressed by open PR #10's payload; the denylistComplete comment/code disagreement could NOT be substantiated on current main (aggregator.ts:330-352 — comment and code agree, the tradeoff is self-documented) — drop that half — and a fresh re-measure plus fix landed 2026-09-24 (run 04c7d9c2 assess d2a81faa + implement 9b17c4e7, base 763c1fa): the cache-TTL no-op RESOLVED BY REMOVAL — CACHE_TTL_MS (60s) equaled the refresh interval with a strict less-than validity check (aggregator.ts:717-718 vs :792), so steady-state revisit ages sat at the interval plus-or-minus per-repo fetch-latency variance — hits possible only for timing-lucky entries, not meaningfully above zero, dead weight; a raised TTL was rejected (it would serve staler data to save trivial API budget on a small fleet), and the Map plus CacheEntry type were removed with the decision recorded at the site; repository:null (deleted, renamed, made private, or access lost) now fails visible — stale:true matching the installation_id-null precedent, warning logged at both parse call sites, attention-first ordering (tests added); negative caching for dead installations DECLINED — the 60s refresh cadence bounds re-hits to one resolve call per minute per dead repo and the skip path already warns each cycle, so growing the auth-path failure surface is not worth the marginal API saving (revisit alongside rm-141 concurrency if the budget ever binds)
- acceptance: partial resolver failure serves last-good with an explicit stale marker and per-repo absence entries instead of silent drop; warm-empty never replaces last-good without a banner; MonitoringDto gains a degradation signal consumed by the operator view; both branches tested
- evidence: new aggregator tests in `pnpm test` covering resolver-failure and warm-empty paths; operator view renders a stale banner against a seeded degraded snapshot
- absorbed acceptance (f4622d7e lineage rm-118): cache TTL raised above the refresh interval or the cache removed with a comment recording why; denylistComplete comment corrected or code aligned — one truth; failed-installation resolution negatively cached with a short TTL and tested; renovate.json5 deleted or an AGENTS.md line documents its inert state

### Reconcile drift-identity reporting: count-only promise vs full_name on discovered repos
- id: `rm-126` | track: reliability | priority: 73.0 | status: implemented 2026-09-22 (docstring truthed to count-only + driftCount repo-identity lock test in-tree via rm-135 — suite 3116 confirms the +1; /api/monitoring consumer decision deferred to next cycle; LANDED — completed at origin/main 539c632, run 5b6e8e801952 roadmap verification: driftCount repo-identity lock coverage live in test/aggregator.test.ts + test/dashboard.test.ts + test/operator-fixture-harness.test.ts; the /api/monitoring consumer tail stays with rm-107)
- signals: `src/github/aggregator.ts:12-19` docstring promises snapshot drift is reported "count only, never by repo identity", but RepoStatusDto carries `full_name` (aggregator.ts:86) and installation-only discovered repos enter the snapshot labeled `discovered` (aggregator.ts:418-419) — emitted by `/api/status` and `/api/monitoring`; during a metadata staleness window that can surface an un-redacted private repo name to authenticated clients; `/api/monitoring` additionally has no SPA consumer (grep shows only self-referential comments in `src/routes/api.ts`) since the operator-first re-architecture, so its exposure surface is maintained for nothing
- acceptance: one truth — either drift surfaces lose repo identity (count and count-by-class only, names stripped from discovered-only entries) or the docstring is corrected and an explicit operator decision to show names is recorded in the cycle batch doc; the `/api/monitoring` consumer question decided in the same change (wire it, fold it into the rm-107/rm-119 status surface, or delete it together with its tests); tests assert the chosen semantics for the discovered-only case
- evidence: `pnpm test` covering the chosen drift semantics; grep shows the docstring and DTO behavior agree; the consumer decision recorded in docs/prioritization/

### Reality canary for the GitHub data path (verify the real transport contract)
- id: `rm-179` | track: reliability | priority: 71.0 | status: implemented 2026-09-24 (run 6fe26972 implement 6e6cde38 — approach (a) landed as scripts/graphql-canary.ts + .github/workflows/canary.yaml, weekly Mon 05:23Z, GITHUB_TOKEN-only, zero-install, executes the exact exported REPO_STATUS_QUERY and sha256-logs its text; approach (c) guard rode rm-177; live-proven: canary exit 0 with token, exit 1 without; malformed GITHUB_REPOSITORY hardening found+fixed during live testing; first scheduled fire 2026-09-28 05:23Z pending — watch for green; (b) offline schema validate() stays a recorded alternative)
- signals: two 2026-09-24 failures share one class — green CI over broken reality: rm-177 (every aggregator test faked the transport or parsed offline, so a live-rejected query shipped for 4+ days) and rm-178 (a drift gate that never once passed while treated as "stranded green"); the repo already validates the real thing elsewhere (Release parses real digests, the visual gate drives a real browser); research sourced three approaches: (a) GITHUB_TOKEN-only scheduled workflow executing ONE real query against a public repo, importing the exact exported template so the shipped string is what runs; (b) offline `graphql`-package `validate()` against a snapshot of GitHub's published schema (github/github schema.graphql) — deterministic, no network, needs schema upkeep; (c) zero-cost structural guard (no `//`-prefixed lines inside query templates) — rm-177's acceptance half (watch note 2026-09-25, run 70ba5270 roadmap 265348bc: first scheduled fire 2026-09-28 05:23Z, with base-drift's 04:13Z cron the same morning — assign both to the next cycle's watch list; a red first fire is itself the live query-contract signal the canary exists to catch)
- acceptance: (c) lands with rm-177; (a) OR (b) lands as a scheduled canary (weekly, read-only, GITHUB_TOKEN only, never writes) whose red means the real API would reject our query today; the canary log shows a hash of the exact query text it executed; alert route documented (run conclusion + issue)
- evidence: first canary run green at parity; a deliberate broken-query dry-run reddens it; structural guard in the suite

### Listener digest push
- id: `rm-106` | track: operator-experience | priority: 70.0 | status: blocked-external (re-assessed 2026-09-22 folding the stranded cycle-4 dependency discovery — the privacy-policy precondition is met but the send path needs a gateway-side contract extension first; see the dependency-discovery signal)
- signals: VAPID push infra is live for run events while listener messages (deploy-health, autoheal, Daily Maintenance Report per `docs/contracts/operator-listener-channel.md`) age out silently under the 500-row/30-day retention — the channel replaced GitHub issues but kept a "must watch a surface" burden
- unblock evidence (2026-09-20): upstream fro-bot/dashboard merged #495 (30839c6, plans the public operator push privacy policy) and #496 (3f2fbe9, publishes it: `web/privacy.html` +87, `web/src/privacy/claims.ts` +239, 258 lines of claims tests, a `/privacy` server route, AppShell link) — exactly the "published first or in the same change" precondition this item's acceptance names
- dependency discovery (2026-09-21 stranded cycle-4, folded 2026-09-22 via rm-135): the push-SEND path lives in the GATEWAY, not this repo — the vendored contract (src/gateway/operator-contract/) models only the GET endpoints (push/vapid-key, push/subscriptions) with no send/notify/dispatch surface, so digest delivery needs a gateway-side contract extension FIRST
- acceptance: push (rate-capped) or daily digest delivered on listener messages; noise calibration documented (what triggers immediate vs digest); upstream #238-style public push privacy policy published first or in the same change
- evidence: integration test covering the notify path in `pnpm test`; one live verified delivery to the operator's subscription; policy doc merged

### Operator system-status panel
- id: `rm-107` | track: operator-experience | priority: 65.0 | status: candidate
- signals: monitoring-of-monitoring signals exist but scattered — `refreshedAt`/`staleBanner` in the MonitoringDto, GitHub rate-limit hooks live but log-only (`src/github/app-client.ts:73-79` `onRateLimit`/`onSecondaryRateLimit`), listener store depth, nothing composes them; the 2026-09-20 assess needed out-of-band `gh` commands to learn CI was red; `/api/healthz` hardcodes its `lastFetch`/`rateLimit` fields to null placeholders (src/routes/api.ts) — liveness-only by design, confirming these signals belong in a composed status surface rather than healthz; new evidence 2026-09-21 (run 2ae7d10a assess): single-runner serialization observed live at tip 92688a2 — CodeQL in_progress 24min+ against a 3m43s norm while Main, Release, and visual queued behind it — composing own-repo queue depth into this panel (jointly with rm-119's gate roll-up) would have made the wait visible instead of discoverable only out-of-band (re-measured 2026-09-24, run 04c7d9c2 assess: /api/healthz placeholders still hardcoded null at 763c1fa — the liveness-only stance is stable, the composed panel remains the home for these signals) (recurrence re-confirmed 2026-09-24, run 3538ec96: assess F3 measured the dead /api/monitoring surface still live at 82312f7 — routes/api.ts:95 + server.ts:176 stale 'SPA monitoring view' comments, sole grep consumer sw.test.ts:104's negative assertion — and this run's research ranked the composed panel its top candidate; three prior ideation cycles name it (2026-09-19 #6, 2026-09-20 #3, research-2 N-series); open PR #45's payload does not touch it; fourth recurrence 2026-09-24, run 5b6e8e801952 assess F5 + research C2: the dead /api/monitoring surface re-measured at 539c632 (routes/api.ts:95-108 dead route + server.ts:234 stale '/api/status' comment, sole consumer a negative assertion in sw.test.ts) — the composed panel remains the top open operator-experience candidate and the cycle-10 compound queued it as next-cycle top pick) (fifth recurrence 2026-09-25, run 70ba5270 assess 34dbdf08 + research 761db5e1 at 20ade64: /api/monitoring still live and still consumer-less (routes/api.ts:104; sole grep consumer sw.test.ts:104's negative assertion), staleBanner machinery still zero-usage in web/src, and the two 30s client polls (web/src/App.tsx:54, web/src/views/Listener.tsx:16) remain the natural hosts — the queued top pick stands; PR #166's 'monitoring gate' is only a DASHBOARD_MONITORING_REFRESH env flag around provider.start(), not a view)
- acceptance: one status surface composes snapshot freshness, rate-limit budget, listener store depth/age, and last refresh failures (fail-closed events); each composed signal has a test; no duplication of `/healthz` liveness semantics
- evidence: `pnpm test` includes tests for each composed signal; operator verification against a seeded stale snapshot renders the panel correctly

### Dated major-upgrade decision matrix
- id: `rm-108` | track: reliability | priority: 60.0 | status: candidate (reframed 2026-09-20 from open-ended watchlist)
- signals: measured 2026-09-20 — typescript 6.0.3 → 7.0.2 (native compiler, announced 2026-07-08; 7.0 ships no programmatic API — typescript-eslint still needs 6.x via the `@typescript/typescript6` side-by-side package); vitest 4.1.11 → 5.0.1 (the 3→4 migration already cost a cycle; flags/`basic`-reporter breaks documented in solutions docs); jsdom 29.1.1 → 30.1.0; Node 26 enters LTS 2026-10 under the new annual schedule (every release LTS 30 months) while the image pins node:24-slim; everything else measured current (octokit modular, vite 8.3.0, react 19.3.0, tailwind 4.3.3, workbox 7.4.1) (dated refresh 2026-09-24, run 3538ec96 research 501c5ddc: typescript 7.0.2 still npm-latest with the typescript-eslint peer blockage unchanged; vitest 5.0.1 confirmed via gh api (released 2026-09-15); nodejs Release schedule.json carries the precise v26 LTS promotion 2026-10-28 feeding rm-139's gate; node:sqlite imports warning-free on v24.19.0 — recorded for any future local-store adoption, no repo use pending; dated refresh 2026-09-24, run 5b6e8e801952 research: majors unchanged (typescript 7.0.2, vitest 5.0.1, jsdom 30.1.1 vs repo 29.1.1); fast-check 4.10.2 minor vs pinned 4.9.0; hono lockfile 4.13.8 vs 4.13.9 caret-sweepable; react 19.3.0/vite 8.3.0/tailwind 4.3.3 measured current; re-eval gate stays rm-133's 2026-10-21) (dated refresh 2026-09-25, run f1ae52a3f04b research 9ca3c1b6: in-range drift hono 4.13.9 vs lockfile 4.13.8, @hono/node-server 2.1.1, vite 8.3.1 — all ride the ~2026-10-03 rm-102 dependabot window, no action; typescript 7.0.2, vitest 5.0.1, jsdom 30.1.1 all unchanged since 2026-09-24 and the typescript-eslint <6.1.0 peer blockage stands; endoflife.date re-confirms node v24.21.0 active LTS, support ends 2026-10-20, EOL 2028-04-30, v26.10.0 Current feeding rm-139; ossf/scorecard-action v2.4.4 == latest so the pin stays) — 2026-09-25 update (run 4e7c674 roadmap): vitest 5.0.1 (released 2026-09-15) and jsdom 30.1.1 are now registry-latest alongside typescript 7.0.2 — fold all three into the spike at the rm-133 ignore-window expiry 2026-10-21; research 2026-09-25 also verified the octokit stack, react 19.3.0, tailwind 4.3.3 and playwright 1.63.0 are all current (registry.npmjs.org/latest), so the spike stays scoped to the three gated majors
- acceptance: this roadmap or a linked doc carries an adopted-vs-available table refreshed each maintenance cycle, each row with a trigger date, blast radius, and go/no-go decision recorded at the date; majors absorb via upstream merges where possible, otherwise explicit upgrade PRs — never a silent ride; REST API-version pin (`X-GitHub-Api-Version: 2022-11-28`, src/auth/oauth.ts:130) reviewed on the same cadence (protective pin — 2026-03-10 REST removals verified unexposed); gate refined 2026-09-21 (run 2ae7d10a research): TypeScript 7.0.2 is hard-blocked — typescript-eslint 8.70.1 peer range is `typescript >=4.8.4 <6.1.0` (`npm view @typescript-eslint/eslint-plugin@8.70.1 peerDependencies`), so the lint toolchain must widen before any TS7 adoption; vitest 5.0.1, jsdom 30.1.0, and @testing-library/jest-dom 7.0.1 remain deliberately deferred (rm-133 ignores); Node 26 and pnpm 12 split out as rm-139/rm-140 (vitest 5 exposure quantified 2026-09-24, run 04c7d9c2 research: 29 strict toHaveTextContent sites, 12 files with manual mock-clear, 4 vi.mock sites, zero @vitest/runner or vitest/expect entry-point imports, zero sequential use, roughly 901 expect call sites to audit for un-awaited assertions — no structural blockers; the 2026-10-21 re-evaluation can proceed suite-first)
- evidence: `npm view <pkg> version` outputs cited at each refresh; each adopted-major bump shows a green Main run before merge

### Merge-hygiene baseline: required checks on main plus drift alert
- id: `rm-116` | track: reliability | priority: 58.0 | status: candidate
- signals: `gh api /repos/codeo1io/dashboard/branches/main/protection` returns 404 "Branch not protected" and `/rulesets` is empty (probed 2026-09-20; re-probed 2026-09-20 run f69cd740 — still 404); nothing structurally blocks red-check merges — PR #4 merged 2026-09-19 17:51Z with TWO red checks on its own run (35459324430) and its defects became the cycle's P1 findings (rm-100); repo is public on a User account, where required status checks and rulesets are available; SECOND INSTANCE 2026-09-20 (f4622d7e review): the fleet render 2f3a884 landed by direct push with its own run already red — a second red landing after PR #4's red merge; THIRD INSTANCE 2026-09-20/21 (run 87e3c32f assess): PR #7 merged with every Main gate green while the same push broke Release at builder step 5/8 (run 35562249072) — a red landing for the third time with zero structural block, and Release stayed red on three consecutive pushes (7ba9c82, b0ed12d, d0d17fc) (2026-09-21 compound, run 87e3c32f implement c6a1806d: exclusions RESTORED from 4b1b406 baseline — Dockerfile/README/AGENTS/copilot/vitest.config plus the .slim/clonedeps.json manifest the merge had deleted; local docker build green (manifest sha256:cb0ce8a…) so Release is expected green once the cycle-5 batch lands; the standing defense is now rm-131, in-tree) (third instance closed: the cycle-5 batch landed 2026-09-21 and Release went green at 493b82f, run 35582769317; the exclusion hazards rode through the da6809d absorb cleanly — rm-131 guard green at tip 92688a2, run 2ae7d10a assess; re-probed 2026-09-21 stranded cycle-4, folded 2026-09-22 via rm-135: branch protection EXISTS but is a shell — /rulesets/branches returns a protection rule with EMPTY required checks, consistent with the third red landing above); FOURTH re-probe 2026-09-25 (independent review 0bbfc38f at the c2ae28ab integrate): the protection object is STILL a shell — required_status_checks.checks/contexts EMPTY, strict false, enforce_admins false — so the cycle-10 batch's live fill (7 checks + strict + enforce_admins, re-read verified in-batch per its doc) did NOT persist; nothing structurally blocks red-check merges today. Landed progress at that integrate: acceptance clause 3 (the hotfix-override recipe documented in AGENTS.md) is done, worded to the actual shell state
- acceptance:
  - main protected (classic branch protection or a ruleset) with required status checks covering the Main workflow's job set (Test, Check Types, Check Workflows, Lint, Test Scripts Load, Design Check) plus CodeQL; strict-on-required-context where feasible
  - a periodic (weekly) workflow snapshots the protection/ruleset API state and alerts (open issue or failing check) on drift from the codified baseline
  - the hotfix override path documented in AGENTS.md (an admin merge over red checks becomes an explicit, visible decision, not a default)
- evidence: `gh api /repos/codeo1io/dashboard/branches/main/protection` returns the configured object post-change; drift-check workflow run log on first run; AGENTS.md note merged; PR #4-style merge attempt with red checks is blocked (observed on a throwaway branch)

### Make the gateway login-redirect topology assumption explicit
- id: `rm-127` | track: reliability | priority: 56.0 | status: candidate (added 2026-09-20, run f69cd740 assess F1)
- signals: gateway mode redirects unauthenticated non-public paths to `GATEWAY_LOGIN_REDIRECT` = `/operator/auth/github/start?return_to=/operator` (src/server.ts:111) — a relative path the dashboard itself never serves (routes/operator.ts mounts only `/`) and that isPublicPath (src/server.ts:577-598) does not exempt, so the intended topology is a same-origin reverse proxy routing that path to the gateway, an assumption the server.ts comment makes silently; deployed any other way an unauthenticated browser hits a browser-terminating redirect loop with no server-side diagnostic
- acceptance: the assumption becomes explicit — startup config validation requiring a documented gateway-origin/proxy topology, or the redirect built absolute against the configured gateway origin, or an isPublicPath exemption plus a local route; at least one config shape's unauthenticated flow is covered by a test and a comment records the supported topology
- evidence: a new gateway-mode unauthenticated test in `pnpm test`; grep shows the redirect target served or validated at startup; a seeded misconfigured-startup test shows the fail-fast log line

### Gate-health roll-up: workflow-run conclusions per repo
- id: `rm-119` | track: operator-experience | priority: 52.0 | status: candidate
- signals: REST actions/runs probed 2026-09-20 returning exactly the needed shape (latest runs at 5b8a2b3: Main=failure, CodeQL=failure, Scorecard=success); the 2026-09-19/20 gate reds sat invisible to operator-facing surfaces for ~26h (merged 17:51Z, found only by out-of-band assess); the dashboard renders check runs on commits but not workflow-level conclusions — and the week's failures lived at the workflow level (schema rejection before any check run existed); even the GOOD state needs this surface: the first fully-green tip (7de0de3, all four workflows green 2026-09-20 06:14Z) was itself only discoverable out-of-band via `gh run list`; THIRD INSTANCE 2026-09-21 (run 87e3c32f assess): Release red on three consecutive pushes (7ba9c82, b0ed12d, d0d17fc; last good 4b1b406 2026-09-20T21:53Z), GHCR :latest stale since — again discovered only by out-of-band assess, zero operator surface
- acceptance:
  - snapshot carries the latest conclusion per workflow at the default-branch tip (dedupe by workflow name, REST pagination with a documented ceiling — rm-110 discipline)
  - any red required-gate at the tip triggers the existing needs-attention surface
  - designed jointly with rm-107 as ONE status surface (repo gate health + monitor health), not two parallel panels — the joint-design decision recorded at plan time
- evidence: pnpm test for the roll-up path; fixture with multiple runs per workflow; manual verification against the live red Main/CodeQL state shows both surfaced

### Base-digest drift visibility: weekly read-only pin check
- id: `rm-123` | track: reliability | priority: 48.0 | status: implemented 2026-09-22, landed on main (byte-identical 82312f7->539c632; in-tree via rm-135's cherry-pick of f8ff412, actionlint-validated); DEFECT open — the first dispatch run (35970747110, 2026-09-24T07:38Z) exited 1 with an EMPTY live digest; repair + guard test tracked TWICE in parallel — rm-166 (run 41c7d471, cycle-11) and rm-178 (run 6fe26972, cycle-12) independently implemented the same buildx readback + empty-guard and land together at this integrate merge; this item's 'first run logs both digests' evidence now rides the rm-166/rm-178 re-dispatch before the first scheduled fire 2026-09-28T04:13Z
- signals: node:24-slim moved through four digests in ~8 days (2fe369e → a9d7043 #491 → 0e0ff40 #492 (the pin since rm-111 landed) → live 5cbc7cab at 2026-09-20, `docker manifest inspect`); dependabot docker updates are weekly (rm-102), so the pin can silently trail a rebuilt, security-fixed base for days — drift is detectable today only by hand-run manifest inspection (exactly how the 779e7271 assess found the old pinned 2fe369e shipping libpcre2 10.42-1 while the live base shipped 10.42-1+deb12u1); fresh probe 2026-09-21 (run 87e3c32f research): live node:24-slim digest == pinned 0e0ff40 (tag last_updated 2026-09-19) — aligned today, a drift-free window; dependabot docker bumps begin around 2026-10-03 (window opened 2026-09-19); a cycle-4 batch reportedly implements this item — verify landing state before scheduling duplicate work (base-drift.yaml confirmed absent at HEAD d0d17fc); resolved 2026-09-21 (run 2ae7d10a assess): the detector EXISTS — stranded green in PR #9 (f8ff412, Main 35597011430 + CodeQL 35597013457 success on its branch push) — merging PR #9 (rm-135) lands this item; and drift is live today: pinned 0e0ff40 vs live 5cbc7cab (`docker manifest inspect node:24-slim`, 2026-09-21T21:35Z); fresh measure 2026-09-22 (run f11b7255): registry-1.docker.io HEAD for node:24-slim == pinned 0e0ff40 (token-authed API probe) — the 2026-09-21 drift self-resolved via tag rotation, so re-baseline the first-fire expectation to green-at-parity and alert only on real divergence; base-drift.yaml enters main with the cycle-7 B1 landing (PR #10, merges cleanly) (re-opened 2026-09-24, run 6fe26972 assess 06462e0a: the landed gate is broken-by-construction — `docker manifest inspect` emits JSON with no `^Digest:` line so the awk yields empty (reproduced locally, 0 bytes, exit 0; CI run 35970747110 logs `live node:24-slim digest: ` empty); all 4 recorded runs are false-reds; registry truth via token-authed HEAD probes equals pin 0e0ff40 under BOTH oci-index and docker-list Accept headers, and the historical `live 5cbc7cab` readings were per-platform manifest digests from `--verbose` dumps — wrong digest class; fix split to rm-178, first-green acceptance moves there; rm-178 also blocks the 2026-09-28 cron from being a meaningful signal until landed) (defect evidence 2026-09-24, run 41c7d471 assess F1: dispatch run 35970747110 logged 'live node:24-slim digest: ' EMPTY and exited 1 — plain `docker manifest inspect` prints no Digest: line on ubuntu-latest while the buildx form does (control: Release run 35974819368, same runner image, same hour); time-boxed before the first scheduled fire 2026-09-28T04:13Z) ; THIRD same-day convergence 2026-09-24: run 5b6e8e801952 (cycle:1 B1, renumbered rm-182 at its integrate merge) implemented the same repair independently with a Docker-Hub-token + registry-HEAD fallback for the extraction path, folded into the workflow at the merge- acceptance: weekly scheduled workflow compares each Dockerfile pin to the live tag digest and opens/updates ONE tracking issue on drift (title carries pinned→live digests); GITHUB_TOKEN only — the fork has no secrets; strictly read-only — it never bumps the pin itself (dependabot owns bump PRs, rm-102); the workflow passes the actionlint container gate
- evidence: actionlint container exit 0 on the new workflow; first run log shows pinned digest, live digest, drift verdict; a dry-run mode prints the issue text verbatim for review

### Single-source the SSE parser invariants
- id: `rm-114` | track: reliability | priority: 45.0 | status: candidate (from run 270220e7 assess F5/F6)
- signals: the server reader and the browser parser are maintained by hand in parallel and share a live bug — the buffer cap counts UTF-16 code units against a BYTES-named constant (`src/gateway/operator-sse-reader.ts:35,526` and `public/operator-stream.js:44,2473`); the first-frame timeout park (operator-stream.js:2505) mirrors the same duplication — 2026-09-25 update (run 4e7c674 roadmap): 2026-09-25 assess F10 sharpened the shared-divergence list: parseSseRecord keeps only the last `data:` line of a multi-line record where SSE spec joins with \n (a gateway that ever multi-lines frames fails closed as a JSON error), and MAX_SSE_BUFFER_BYTES counts UTF-16 code units, not bytes — astral-plane text can ~3x the real byte budget before tripping (both persist at 480c92c); second shared bug (2026-09-24, run 41c7d471 assess F7, first reported by sibling run d6455cdc and never tracked): both parsers keep only the LAST `data:` line of an SSE frame (src/gateway/operator-sse-reader.ts:116-117, public/operator-stream.js:205+) — legal multi-`data:` frames are silently truncated to their final fragment (the gateway emits single-line frames today, so the parser contract is what's wrong, not live traffic)
- acceptance: cap constant and normalization rules extracted to one shared source consumed by both the server reader and the browser bundle (shared module or build-time generation); divergence becomes a build/type failure rather than a live parser bug; multi-byte frame tests cover the corrected cap semantics; the shared source also joins multi-`data:` frame lines per SSE semantics (newline join) so both consumers agree on legal multi-data frames (added 2026-09-24, run 41c7d471 assess F7)
- evidence: single definition site; `pnpm test` includes multi-byte boundary-frame tests; grep shows no remaining duplicated literal; property-suite cases cover multi-data frames
### Security-posture panel: Dependabot and code-scanning alerts per repo
- id: `rm-117` | track: security | priority: 44.0 | status: candidate
- signals: live probes 2026-09-20 — REST `/repos/codeo1io/dashboard/code-scanning/alerts?state=open` returns 10 OPEN alerts on this fork's main (1 critical CVE-2023-45853 + 9 high, created 2026-09-16, refs/heads/main) that no operator-facing surface shows, while the 2026-09-19 Release Trivy gate was green — alert triage lags the release state; GraphQL `vulnerabilityAlerts(states:OPEN)` works (0 open / 28 historical here) but GraphQL has NO code-scanning field (probed — REST only); the token mint already requests security_events and vulnerability_alerts read with graceful fallback (src/github/installations.ts FULL_READ_PERMISSIONS), so the permission plumbing predates the feature (fresh census 2026-09-25, run 71991cb3 research 77dda854: `state=open` totals 61 with per_page=100 pagination — the API's 30-item default page has caused prior undercounts ('10 open' 2026-09-20, 30 on a default-page read); composition: 56 image-level (location path == repo name), 1 workflow (a NEW PinnedDependencies medium created 2026-09-25T04:01Z at base-drift.yaml:172 — filed as rm-209), 4 no-file; criticals are FOUR not one — CVE-2023-45853 plus CVE-2026-42496, CVE-2026-13221, and CVE-2026-8376 (alert #51, perl 32-bit heap overflow; missed by the implement-phase census, corrected 2026-09-25 by review b310d051), all created 2026-09-16 (escalates rm-205's single-critical framing); high families CVE-2026-53613/76642/78408/78409/78410 at 8 instances each; exactly ONE alert created ≥2026-09-21, so the delta vs 2026-09-20 is measurement scope, not a detection wave; triage table landed 2026-09-25 cycle-17 (run 71991cb3 implement 73932b46) in docs/solutions/best-practices/container-cve-census-2026-09-25-no-fixed-versions.md — 14 unique high + 4 critical CVE families grouped by Debian package with the no-fixed-version verdict; workflow-alert dismissal deferred to final_validation); compound 2026-09-25 (run 71991cb3 compound d730c0da): the measurement traps behind every historical undercount are now codified as a methodology section in the census doc — (a) the code-scanning REST default page is 30 items, so any unpaginated read silently truncates (10 seen 2026-09-20 on a 30-item page was itself an undercount of 61), always paginate with per_page=100 until an empty page; (b) an in-image patch is only planable after `apt-cache policy` shows Installed != Candidate against the PINNED digest — this cycle's census had zero such packages, so document-and-watch, not patch, was the correct posture
- acceptance:
  - aggregator snapshot carries per-repo open-alert counts — Dependabot via GraphQL `vulnerabilityAlerts(states:OPEN)`, code-scanning via one REST call per repo — both behind the existing optional-permission graceful degradation (absent permission ⇒ field omitted, never an error)
  - counts render per repo with a needs-attention trigger; only counts and severity buckets are stored/rendered — no alert content beyond that
  - the 10 standing open code-scanning alerts are triaged (dismissed with reason or routed to fix work) and the panel reflects the triaged state — surfacing is the feature, triage is the operator decision it enables
  - rate-limit cost documented: one REST call per repo per cycle, batched with the same pagination/ceiling discipline as the check-suite query (rm-110 family)
- evidence: pnpm test coverage for both alert paths including the graceful-fallback case; seeded fixture with alert counts; operator verification against live data shows the triaged state

### Per-repo scoped installation tokens
- id: `rm-118` | track: security | priority: 42.0 | status: candidate
- signals: mintReadOnlyToken (src/github/installations.ts) mints installation-WIDE tokens — the redaction denylist constrains queries (aggregator excludes denylisted repos before any per-repo query) but a minted token's capability still spans the whole installation including denylisted/private repos; GitHub's create-installation-access-token endpoint accepts a repositories array to scope a token to named repos, so capability can be made to match query intent
- acceptance:
  - per-repo GraphQL/REST status queries use a token scoped to just that repo (repositories array param at mint); installation enumeration keeps the installation-wide token
  - per-repo tokens cached with the existing 55-min pattern; the 5000-mints-per-hour-per-installation cap documented as the budget ceiling (fleet size is far below it)
  - a test proves the scoping: the client seam rejects a repo-scoped token used against a sibling repo
- evidence: pnpm test includes the scoping/rejection test; code diff extends the mint signature and call sites; AGENTS.md security-invariant block notes the capability-scoping layer

### Optional trusted-proxy mode for the ingest rate limiter
- id: `rm-129` | track: security | priority: 40.0 | status: implemented 2026-09-24 (cycle-10 B1 + review fix: per-path-class budgets — public/operator/ingest, each with its own `RATE_LIMIT_MAX_*` env, ingest is its OWN budget not an exemption — plus opt-in `rateLimitTrustedProxy`/`RATE_LIMIT_TRUSTED_PROXY` XFF first-hop re-key (safe only behind an XFF-overwriting proxy; key capped at 64 chars), legacy bare-call 429-compat preserved; 68/68 focused + 5-test env config-surface suite (override honored, invalid falls back to default per the house env convention — the acceptance's fail-fast wording deliberately not adopted — proxy flag only on {1,true,yes}, cap collapses overlong tokens) + AGENTS.md env note; LANDED — completed at origin/main 539c632 via the cycle-10 lineage, verified live by run 5b6e8e801952 assess F2 (server.ts:577-611 per-path-class budgets + opt-in trusted-proxy re-key in-tree); follow-on default-topology signal folded into rm-178)
- signals: the listener-ingest rate limiter keys on the direct connection address and deliberately ignores `X-Forwarded-For` as client-spoofable (src/server.ts:520-535 comment) — correct standalone, but behind the gateway reverse-proxy topology (rm-127's assumed deployment) every producer shares ONE bucket and a single noisy producer throttles all others; no configured middle ground exists today (fresh currency 2026-09-24, run 3538ec96 assess F1: limiter unchanged at 82312f7 (server.ts:79-81 config, :520-530 direct-address keying, :553-582 isPublicPath incl. /api/healthz); research idea 2 argues the middle ground should pair the XFF re-key with path-class budgets — public healthz/auth, operator, ingest — so a flooder on the public class cannot consume the operator's budget even in standalone mode)
- acceptance: an explicit trusted-proxy setting — unset keeps today's behavior (XFF ignored, spoof-safe); set switches rate-limit keying to the validated X-Forwarded-For first hop; both modes tested including the spoof-rejection case when unset, and the trade-off is documented next to the existing comment
- evidence: rate-limiter tests in `pnpm test` for both modes; a config-surface test including fail-fast on invalid values; an AGENTS.md or config-doc note naming the proxy assumption

### Gateway-contract drift watch for the mirrored agent source
- id: `rm-120` | track: reliability | priority: 36.0 | status: candidate
- signals: the cloned dependency source pins fro-bot/agent at v0.78.0 while upstream is at v0.113.2 — 75 releases of drift, with release bodies at v0.113.1, v0.113.0, v0.109.3, v0.107.1, v0.106.2 touching operator/gateway surfaces (gh api repos/fro-bot/agent/releases, 2026-09-20); AGENTS.md names this clone as the reference for the operator OAuth return path, GitHub App client, secret readers, and logger/Result primitives this app mirrors — and no process notices when that contract moves underneath the conformance tests
- acceptance: a periodic watch (checklist item in the maintenance cycle or a small workflow) compares the clonedep pin to the upstream latest tag; on drift, diffs the mirrored contract surfaces and records a short delta note (docs/solutions/ or the cycle batch doc) flagging conformance-test impact explicitly; the clonedep is refreshed on cadence, never auto-merged
- evidence: first watch output showing the v0.78.0→v0.113.2 delta summary with the operator/gateway-touching releases called out; the checklist/workflow merged

### CSRF tokens for listener ack mutations
- id: `rm-115` | track: security | priority: 30.0 | status: candidate (hardening; low severity)
- signals: `src/routes/listener.ts:121,130` accept session-authenticated state-changing POSTs (`/messages/:id/ack`, `/ack-all`) protected only by SameSite=Lax cookies, while logout already implements an explicit CSRF-token dance (`src/routes/auth.ts:168-190`) — an asymmetry; exploit window is narrow (Lax blocks cross-site POSTs except the fresh-cookie grace period) and impact bounded (ack state)
- acceptance: ack mutations require the same csrf-token pattern as logout (or equivalent origin check), with tests for the rejected cross-origin case; no behavior change for the operator's own client
- evidence: route tests in `pnpm test` covering token-missing rejection; manual operator flow unchanged

<!-- cycle-3 extension (2026-09-21, conductor run 87e3c32fa39a429989c7d8e5493b8e13 roadmap attempt 7187054a; items rm-131..rm-133 sourced from cycle-3 assess (e3c515c5) + research (829799a5); existing items preserved verbatim, dated signals appended to rm-103/rm-116/rm-119/rm-123) -->

<!-- cycle-1 extension (2026-09-21, conductor run 2ae7d10a68d04c1b88e536e280a4e405 roadmap attempt 12ac512d; items rm-135..rm-140 sourced from run 2ae7d10a assess (35fc163f) + research (7424eb58); status corrections: rm-125/rm-130/rm-131/rm-132/rm-133 verified landed on origin/main 92688a2 and relocated to Completed, rm-128 completion confirmed at f334fb2 and relocated; dated signals appended to rm-103/rm-107/rm-108/rm-116/rm-123) -->

### Binding-docs truth batch 2 (README badge/endpoints, dependabot comment, vite kill-switch comment)
- id: `rm-134` | track: docs | priority: 32.0 | status: implemented 2026-09-22 (in-tree via rm-135's payload; LANDED — completed at origin/main 539c632: README badge verified fork-owned (README.md:9 reads codeo1io/dashboard, fork/upstream READMEs diverge at line 9), run 5b6e8e801952 research)
- signals: PR #9's payload (gh pr diff 9, 16 files) carries fork-truth fixes beyond rm-135's named scope: README badge still points at fro-bot/dashboard at origin/main:9, endpoints inventory lacks /privacy and the listener routes, .github/dependabot.yml header comment cites a renovate.json5 state that is false once this batch deletes it, web/vite.config.ts lacked the kill-switch divergence note
- acceptance: README badge resolves against codeo1io/dashboard; endpoints inventory names /privacy + listener routes; dependabot comment states the post-deletion truth; vite comment documents the fork divergence — verified in the cherry-picked worktree files (targeted_tests: actionlint/lint/types green)
- evidence: README.md badge line + endpoint list, .github/dependabot.yml:1-9, web/vite.config.ts in the staged index (git diff --cached)

### Merge the stranded cycle-4 batch (PR #9) — drift detector, doc truth, supply-chain gate
- id: `rm-135` | track: reliability | priority: 95.0 | status: implemented 2026-09-22 in-tree (current landing = cycle-7 B1: PR #10 (27b046a) — the superset batch that supersedes PR #9 — cherry-picked --no-commit onto aa9937f with 0 conflicts, its 18-file payload fully staged; validation green: suite 3116/3116, actionlint 0, types 0, visual 3/3 in-image; pending commit/push/CI; LANDED — completed: minimumReleaseAge 1440 active at 3 sites in pnpm-workspace.yaml (2026-09-23/24 integrate gate ledger) and the payload reached origin/main by 539c632)
- signals: conductor run 5351fd4e's cycle-4 batch is stranded green in PR #9 (branch conductor/run-5351fd4e039a, head f8ff412, created 2026-09-21T09:31Z; Main run 35597011430 and CodeQL run 35597013457 both success on its branch push ~12:00Z) while origin/main tip 92688a2 carries none of it — `git log --all -- .github/workflows/base-drift.yaml` returns only f8ff412 and `git merge-base --is-ancestor f8ff412 origin/main` fails; the payload: base-drift.yaml (weekly pin-vs-live digest check), README badge retarget to codeo1io/dashboard plus an endpoints inventory including /privacy and /api/listener, `minimumReleaseAge: 1440` activation (main's `minimumReleaseAgeExclude` at pnpm-workspace.yaml:8 is inert without it), the rm-126 driftCount lock test, and the upstream #497 caddy runbook doc; the base image pin is drifted TODAY (pinned 0e0ff40 vs live 5cbc7cab, `docker manifest inspect node:24-slim` 2026-09-21T21:35Z) with no detector on main; merge conflict surface measured via merge-tree: .github/dependabot.yml, ROADMAP.md, the pwa-service-worker solutions doc, web/vite.config.ts — plus an rm-numbering collision (the stranded batch's rm-131/rm-133 mean different things than the landed rm-131/rm-133)
- acceptance: PR #9 merged (or its payload cherry-picked) with the four-file conflict set resolved; ROADMAP rm numbering reconciled in the merge (the landed rm-131/rm-132/rm-133 keep their landed meaning, stranded duplicates renumbered); base-drift.yaml on main passes the actionlint container gate and its first scheduled run logs pinned and live digests; `minimumReleaseAge: 1440` active alongside the existing Exclude list; README badges resolve against codeo1io/dashboard and the endpoints list names /privacy and the listener routes; full suite green at the merge sha (the rm-126 lock test takes the count 3115 to 3116)
- evidence: `gh pr view 9 --json state,mergedAt`; `git log origin/main -- .github/workflows/base-drift.yaml`; actionlint container exit 0 on base-drift.yaml; Main run green at the merge sha; `pnpm test` count bump

### Fork-truth the fro-bot maintenance prompt's dependency-ownership claims
- id: `rm-136` | track: reliability | priority: 55.0 | status: implemented 2026-09-22 (cycle-6 B2: fro-bot.yaml:128 paragraph truthed to dependabot ownership, `renovate[bot]` dropped from the trusted-author allowlist, dead .github/renovate.json5 deleted — PR #9 only retargeted it; edits sit unstaged in the worktree pending commit; LANDED — completed at origin/main 539c632: .github/renovate.json5 ABSENT (git cat-file -e origin/main:.github/renovate.json5 fails), run 5b6e8e801952 roadmap verification)
- signals: .github/workflows/fro-bot.yaml:128 asserts "Renovate owns routine dependency and version bumps" — false on this fork (PR #1 deleted renovate.yaml; nothing invokes the surviving .github/renovate.json5; dependabot is the actual owner per rm-102, first PRs expected ~2026-10-03) — the same absence-claim class as the Dockerfile:40 sentence rm-124 fixed; the fro-bot agent reading this paragraph will mis-attribute bump ownership when filing maintenance PRs
- acceptance: the ownership paragraph names dependabot (weekly window, rm-102) as the bump owner and notes the major pins (rm-133); the dead .github/renovate.json5 either deleted in the same change (folding rm-112's fourth absorbed acceptance) or explicitly documented as inert; the absence-claim sweep rule from rm-124 applied — any doc sentence asserting a tool's absence changes in the same change that alters the fact
- evidence: fro-bot.yaml diff; repo-wide grep for Renovate-ownership claims returns only intended historical references; actionlint container exit 0

### Workflow pin-consistency and minor-bump batch
- id: `rm-137` | track: reliability | priority: 40.0 | status: implemented 2026-09-22 pins-half (cycle-6 B3: checkout/setup-node/upload-artifact aligned to the sha-pin family, actionlint+lint green; npm minors deliberately deferred to the ~2026-10-03 dependabot window per acceptance; LANDED pins-half — completed at origin/main 539c632: every checkout/setup-node/upload-artifact pin resolved to the v7.0.1 sha family (3d3c42e5), repo-wide grep verified this run; npm minors remain dependabot's window)
- signals: visual.yaml carries a stale mixed pin set — checkout at line 35 pinned to the v5.0.0 commit while every other workflow pins the v7.0.1 commit, and upload-artifact at lines 61 and 69 floats on the bare `v4` tag while other workflows pin the v7.0.1 commit (exactly the class rm-113 eliminated repo-wide); npm minors available beyond dependabot's reach for exact pins: eslint 10.10.0 to 10.11.0, eslint-plugin-erasable-syntax-only 0.4.2 to 0.7.2 (0.7.2 published 2026-09-20), @types/node 24.13.3 to 24.13.6 — lockfile-verified: hono 4.13.8 and @hono/node-server 2.1.1 already resolved at latest, no action (run 2ae7d10a research)
- acceptance: single sha-pinned discipline for checkout and upload-artifact across all workflows including visual.yaml; the three npm minors ride the first dependabot window (~2026-10-03) where possible rather than manual same-day adoption while minimumReleaseAge is inactive on main (activate it via rm-135 first); gates green at the pushed sha
- evidence: `git grep -nE "checkout@|upload-artifact@"` shows one pin family; pnpm-lock diff if manual; Main run green

### Resolve the PWA kill-switch vestige (decision: strip precache wiring or implement offline)
- id: `rm-138` | track: architecture | priority: 30.0 | status: candidate (decision required)
- signals: web/src/sw.ts is a deliberate cacheless kill-switch (no workbox runtime since f334fb2 removed the eight direct deps, rm-128) yet vite.config.ts still wires vite-plugin-pwa with precache-manifest rewrite rules (lines 31 and 40) — generating and rewriting a precache manifest a kill-switch never serves; upstream keeps real PWA caching, so every absorb re-touches this config; either direction is defensible, but the current half-state costs build complexity and absorb-conflict surface for zero served value
- acceptance: a recorded decision — strip: vite-plugin-pwa removed or reduced to the injectManifest kill-switch minimum with grep showing no precache configuration and web build output shrinking; or implement: a true offline shell with an explicit cache-policy note reconciled against the privacy claims; either way the decision and its privacy reasoning land in the cycle batch doc
- evidence: vite.config.ts diff plus build-output comparison; the decision note in docs/prioritization/; suite green

### Node 26 LTS adoption gate (time-gated)
- id: `rm-139` | track: reliability | priority: 24.0 | status: candidate (opens ~2026-10)
- signals: nodejs.org dist index 2026-09-21: v26.9.0 (2026-09-16) is Current, not yet LTS; v24.21.0 (2026-09-07) is the active Krypton LTS the Dockerfile pins via node:24-slim; under the annual schedule every release is LTS for 30 months, so 24 has long runway — no urgency, only a dated decision point; digest-only base updates already carry 24.x security fixes (the live 0e0ff40 to 5cbc7cab drift, rm-135) (precise gate date 2026-09-24, run 3538ec96 research: nodejs Release schedule.json marks v26 'lts' from 2026-10-28 — schedule the go/no-go for that week) — 2026-09-25 update (run 4e7c674 roadmap): re-confirmed 2026-09-25: nodejs.org dist index shows v26.10.0 still lts=false with v24.21.0 (Krypton) the active LTS — Dockerfile 24-slim digest pin remains correct; re-check at the April-2027 LTS rotation
- acceptance: when 26 promotes to LTS (~late 2026-10): a go/no-go recorded in rm-108's matrix covering engines >=24, the node:24-slim tag choice, the @types/node major, and runner tool-cache impact; adoption, if approved, lands as one reviewed change with all gates green
- evidence: nodejs.org index re-probe at decision time; if go: Dockerfile and package.json diff and Main/Release green at the pushed sha

### Code-truth micro-batch: dead param, env-parse lenience
- id: `rm-180` | track: docs | priority: 23.0 | status: implemented 2026-09-24 (run 6fe26972 implement 6e6cde38; LANDING RESOLUTION per independent review b8b051e9 finding 1: the branch removed the dead third param on the stale 7809df6 frame, but main's rm-144 property test pins the 3-arg parseRepoResponse contract — the landed tree KEEPS main's 3-param signature and closes this half via its already-satisfied pinned-by-test alternative (both live call sites pass null; precedence intended and test-pinned). The envIntOrDefault half landed as authored: whole-string /^\d+$/ with '12abc'/'3.5' fallback cases pinned in test/rate-limit-config.test.ts)
- signals: `src/github/aggregator.ts:566`/`:578` pass `null` as parseRepoResponse's third param at BOTH call sites while the fallback logic gives that param precedence over live response data (aggregator.ts:540 at the main 539c632 frame) — a future non-null argument would silently mask real data; `src/server.ts:91` envIntOrDefault uses `Number.parseInt`, so `12abc` parses as 12 while the documented and tested contract says invalid values fall back to the default (test/rate-limit-config.test.ts pins 'abc'/'0'/'-5'/'   ' but not '12abc')
- acceptance: one truth each — the dead param removed (both call sites plus signature) or exercised by a test proving intended precedence; envIntOrDefault tightened to whole-string parsing or the lenient behavior pinned by an added test with the decision recorded in the cycle batch doc
- evidence: suite green on the landed semantics — the 3-param form's provided-alerts precedence is pinned by test/aggregator-invariants.property.test.ts (both live call sites pass null, so nothing masks response data); whole-string env semantics pinned by the '12abc'/'3.5' fallback cases

### pnpm 12 evaluation (pin guarded by rm-131)
- id: `rm-140` | track: reliability | priority: 22.0 | status: candidate
- signals: packageManager pins pnpm@11.27.0 while latest is 12.5.1; the pin is guarded by rm-131's assertions (upstream carries 11.8.0 — the divergence is fork-maintained on purpose) and pnpm majors are not a dependabot ecosystem, so nothing will ever propose this bump; lockfileVersion compatibility and CI corepack behavior are the review gates (dated 2026-09-25, run f1ae52a3f04b research 9ca3c1b6: npm latest is now 12.6.0, was 12.5.1 at the 2026-09-24 measure — no decision change, still deferred to the rm-133 window)
- acceptance: a dated changelog review of 11-to-12 breaking changes recorded in the cycle batch doc; if adopted: packageManager pin, Dockerfile corepack pin, and the rm-131 guard assertion move in ONE change; if deferred: a dated re-evaluation comment lands next to the guard assertion
- evidence: changelog citations; if adopted, a green Main run at the bumped sha with the guard updated in the same commit

### Bounded-concurrency fleet refresh in the aggregator
- id: `rm-141` | track: performance | priority: 47.0 | status: candidate (added 2026-09-22; renumbered from the stranded cycle-4 batch's rm-132 to avoid colliding with the landed rm-132)
- signals: fleet refresh walks every repo serially each cycle — wall time scales linearly with fleet size and the snapshot is gated by the slowest repo; stranded cycle-4 research evaluated a bounded pool and set determinism requirements (its companion ideation note did not ride the payload — recreate before implementing); the skipPreflight decoupling it also proposed already landed upstream (companion ideation recreated 2026-09-24, run 3538ec96 research idea 6 — the note that did not ride the cycle-4 payload now exists in this run's ideation artifact: bounded pool sized against the rate budget, determinism requirements restated (driftCount lock test and attention-first ordering unchanged); sequence against open PR #45's bounded-transport work first and verify overlap before implementing)
- acceptance: per-repo fetches run under a bounded concurrency limit chosen against the rate-limit budget; snapshot ordering stays deterministic (the driftCount lock test and the staleness semantics from rm-112/rm-126 unchanged); suite green
- evidence: aggregator diff with the pool; a before/after refresh-timing measurement recorded in the cycle batch doc

### Deterministic visual gate: pinned Playwright container image
- id: `rm-142` | track: reliability | priority: 86.0 | status: implemented 2026-09-22 (cycle-7 B2: visual job runs in container mcr.microsoft.com/playwright:v1.63.0-noble (digest sha256:eff16c30…) with the runtime chromium-install step removed; all three dark baselines regenerated AND verified 3/3 inside that exact image; actionlint+eslint green; LANDED — completed at origin/main 539c632: visual.yaml:35-40 carries the playwright-container rationale in-tree, verified this run)
- signals: `visual.yaml` runs on rolling ubuntu-latest with a runtime browser install (`visual.yaml:48` — `pnpm exec playwright install chromium --with-deps`) while baseline provenance is mixed: privacy-dark cloud-regenerated at aa9937f, dashboard-listener-dark and dashboard-operator-dark workstation-captured at 4b1b406; this already reddened the gate once (run 35678222963 at 7809df6, cloud font stack, cleared only by an in-PR baseline regen); @playwright/test is pinned 1.63.0 and mcr.microsoft.com/playwright:v1.63.0-noble exists (`docker manifest inspect`, 2026-09-22)
- acceptance: visual job runs inside the mcr Playwright image matching the repo pin (or the documented lighter form: runs-on ubuntu-24.04 plus a written baseline-regen procedure); no runtime browser-install step remains; all three dark baselines regenerated once inside that image with provenance recorded in the visual spec header
- evidence: three consecutive green visual runs with zero baseline edits; visual.yaml diff passes the actionlint container gate; the baseline files' last-touch commit is the in-image regen

### Security-posture transparency: name the by-design Scorecard deviations
- id: `rm-143` | track: docs | priority: 54.0 | status: implemented 2026-09-22 (cycle-7 B4: docs/runbooks/security-posture.md names all seven sub-scores below 8 with tracked/declined/time-gated dispositions; README Overview cross-links it; badge itself fork-owned since B1 landed rm-134; LANDED — completed at origin/main 539c632: docs/runbooks/security-posture.md present in the runbooks tree, verified this run)
- signals: the fork's own OpenSSF Scorecard is 7.1 (api.securityscorecards.dev, analyzed 2026-09-22T04:52Z by the fork's weekly run) while the README badge displays upstream's score (was stranded rm-134; badge fork-owned since cycle-7 B1 landed its payload); four low sub-scores are by deliberate design and never stated anywhere: Code-Review 0 (single-operator autonomous loop — reviews are in-process adversarial lenses, not GitHub approvals), Signed-Releases -1 (image-based deploys, no GitHub releases), CII-Best-Practices 0 (declined as overhead), Maintained 0 (repo age — self-resolves ~2026-11-08)
- acceptance: one posture surface (README section or docs/runbooks/security-posture.md) lists every sub-score below 8 with its reason and owner; once stranded rm-134's payload lands, the badge resolves to the fork's own score
- evidence: posture doc present and lint-clean; the README badge target resolves to codeo1io/dashboard; every sub-score below 8 has a named disposition (fixed, declined, time-gated, or tracked by an rm id)

### Property-based tests for the SSE parsers and listener ingest
- id: `rm-144` | track: reliability | priority: 50.0 | status: partially implemented 2026-09-24 (cycle-10 B3 + review scope correction: fast-check 4.9.0 pinned as explicit devDep, three suites — ingest contract, SSE framing (server-side reader `src/gateway/operator-sse-reader.ts`: line-ending invariance, heartbeat, round-trip, fail-closed, unknown-name, concatenation, + review-added unknown-field/retry transparency), attention ordering — 18 property tests total (17 + review-added unknown-field/retry transparency), five development-time counterexamples caught; OPEN HALF: the browser parser `public/static/operator-stream.js` property suite + byte-layer multi-byte splits (below the string-chunk parser contract) remain undelivered — item stays open for that half; LANDED — server-side half verified at origin/main 539c632: fast-check 4.9.0 devDep + test/sse-parser.property.test.ts + test/listener-contract.property.test.ts + test/aggregator-invariants.property.test.ts present, this run)
- signals: Scorecard Fuzzing = 0 (2026-09-22) while the repo's only network-shaped parsing surfaces — the server-side SSE reader, the production browser parser `public/static/operator-stream.js` (rm-114 lineage), and the listener ingest payload validation — are exercised solely by hand-written example tests; OSS-Fuzz is disproportionate for a monitoring dashboard, in-stack property testing is not
- acceptance: fast-check property suites cover arbitrary SSE fragmentations (multi-byte splits, CRLF/LF mixes, comment lines, retry fields) for both parsers plus round-trip and rejection properties for ingest payloads; suites run inside `pnpm test`; a seeded counterexample demonstrates one caught regression
- evidence: test-count delta in `pnpm test`; property suites green across repeated runs; the Fuzzing deviation documented per rm-143

### pnpm-store caching for hosted CI
- id: `rm-145` | track: reliability | priority: 46.0 | status: implemented 2026-09-22 (cycle-7 B3: .github/actions/setup gains setup-node `cache: pnpm`, self-hosted-store rationale rewritten hosted-true, dead cache-version input removed — zero callers; LANDED — completed at origin/main 539c632: .github/actions/setup/action.yaml:22 carries cache: pnpm, verified this run)
- signals: `.github/actions/setup/action.yaml:9-16` justifies having no pnpm-store cache by the self-hosted store persistence PR #11 removed — on ephemeral ubuntu-latest every one of ~9 jobs cold-installs the full dependency tree (Main jobs measured 5-84s at aa9937f, run 35688534671); the stranded rm-137 batch pins action versions but explicitly defers caching
- acceptance: the setup composite action enables setup-node cache pnpm (or an explicit actions/cache step keyed on pnpm-lock.yaml); the stale self-hosted rationale rewritten to hosted-runner reality; before/after install-step timings recorded
- evidence: consecutive same-workflow run logs showing a cache hit plus reduced install time; the comment cites the hosted era; actionlint container gate green

### Dependabot auto-merge for grouped patch/minor PRs
- id: `rm-146` | track: reliability | priority: 38.0 | status: candidate (added 2026-09-22, research C6; SEQUENCED — blocked on rm-116 required checks; automerging on today's unprotected main would automate the three recorded red landings)
- signals: dependabot groups arrive weekly from ~2026-10-03 (rm-102) and every merge is manual despite deterministic local gates; rm-116 records three red landings through unprotected main — auto-merge without required checks automates exactly that failure mode (dated refresh 2026-09-25, run 11d3a522 research 94f6de68: gh api repos/codeo1io/dashboard → allow_auto_merge already true, allow_update_branch false — a grouped dependabot PR left stale behind moving main cannot be update-branched via the API, so fold the update-branch setting decision into the rm-116 sequencing when this unblocks)
- acceptance: only after rm-116 lands, dependabot.yml sets automerge with target patch then minor on the npm and docker groups; majors stay manual (rm-133); the first auto-merged PR shows green required checks before merge
- evidence: gh api on the dependabot config returns the automerge block; a red-check dependabot PR observed staying open and unmerged

### License decision (blocked-external)
- id: `rm-147` | track: compliance | priority: 20.0 | status: blocked-external (added 2026-09-22, research C2 — the upstream owner must choose a license; the fork has none to inherit)
- signals: Scorecard License = 0 (2026-09-22); gh api repos/codeo1io/dashboard reports license null AND repos/fro-bot/dashboard/license returns 404 — upstream is also unlicensed, so a unilateral fork-side LICENSE is legally hollow (dated signal 2026-09-25 at the 636b69c7 integrate: candidate run b4aac9ba's explicit-fork-license-posture item (its rm-176, OWNER DECISION REQUIRED) is this item's interim acceptance clause — the README no-license internal-tooling posture statement is the fork-side decision it names; recorded here, not as a duplicate open item)
- acceptance: upstream picks a license and the fork mirrors it in the same absorb cycle (rm-103 cadence); until then the README documents the deliberate no-license internal-tooling posture (pairs with rm-143)
- evidence: LICENSE present and Scorecard License 8 or better after the change; the README posture line present meanwhile

### Hosted-era comment-truth micro-batch
- id: `rm-148` | track: docs | priority: 28.0 | status: implemented 2026-09-22 (cycle-7 B3: setup-action rationale, dependabot grouping rationale, main.yaml pipx note, codeql.yaml policy line, release.yaml buildx note, AGENTS.md pipx line all rewritten hosted-era-true; base-drift.yaml runs-on flipped off the retired self-hosted-only policy; LANDED — completed at origin/main 539c632: all runs-on declarations ubuntu-latest repo-wide (verified 2026-09-24, run b9c36244 full_tests; re-verified this run)
- signals: post-#11/#12 stale self-hosted claims on main beyond the setup action (rm-145): `.github/dependabot.yml:8-12` still says the fork has one self-hosted runner so checks serialize; `main.yaml:132` pipx note; `codeql.yaml:42` runs-self-hosted-per-PR-#1-policy comment; `release.yaml:438` buildx note; AGENTS.md:47 binding-doc line about the self-hosted runner having no pipx (binding-docs consistency invariant)
- acceptance: all five lines rewritten to hosted-runner reality in one batch; no live self-hosted claim remains anywhere in workflows or AGENTS.md (historical notes either deleted or explicitly marked historical)
- evidence: grep for self-hosted across .github/ and AGENTS.md returns only explicitly-marked-historical or zero hits; pnpm lint green

<!-- cycle-9 extension (2026-09-24, conductor run 04c7d9c2949a40a49b96587440fcbf93 roadmap attempt 4b1665c5, reconciled at implement 9b17c4e7 against base 763c1fa; items rm-154/rm-155 sourced from this run's assess d2a81faa + research 433a7ece; dated signals appended to rm-103/rm-107/rm-108/rm-112) -->

### Release-trigger completeness: pnpm-workspace.yaml is build-behavior input
- id: `rm-154` | track: reliability | priority: 78.0 | status: implemented 2026-09-24 (cycle-9 B1 in-tree at base 763c1fa; LANDED — completed at origin/main 539c632: test/release-trigger-paths.test.ts present, verified this run)
- signals: Dockerfile COPYs pnpm-workspace.yaml into BOTH stages (Dockerfile:9,29) and the file hosts fork load-bearing config (allowBuilds, shamefullyHoist, minimumReleaseAge 1440 plus the Exclude list) — yet it appeared in NEITHER release trigger surface: .github/workflows/release.yaml paths nor scripts/should-release.ts isHardReleasePath, so any edit to it changed the built image with no release; the pnpm-lock.yaml precedence comment in should-release.ts itself names pnpm-workspace.yaml as the place allowBuilds/shamefullyHoist live; validated 2026-09-24 (compound): the guard runs green inside the 3124/3124 local suite with lint/types 0, and the cloud ephemeral-PR route shows 9 checks with 8 green — the one red is the rm-131 structural artifact, unrelated to this change
- acceptance: pnpm-workspace.yaml listed in release.yaml push paths AND isHardReleasePath with a rationale comment; a structural sync-guard test parses the Dockerfile build-context sources and fails when any COPY source lacks a release-trigger surface (no file-extension or path filters — the Dockerfile is extension-less); negative-verified by removing the trigger and watching the guard redden; cross-gate consistency asserted (every hard-release literal reachable through the workflow filter)
- evidence: test/release-trigger-paths.test.ts green in pnpm test (6 assertions including the regression pin and cross-gate consistency); actionlint container exit 0 on release.yaml; scripts/should-release.ts --changed-files pnpm-workspace.yaml prints release: hard-release path changed

### Absorb fro-bot/agent v0.115.0 (operator-contract drift)
- id: `rm-157` | track: reliability | priority: 72.0 | status: candidate (renumbered from rm-155 at 2026-09-24 compound — 943d3ed collision, see header mapping; deferred cycle-9 by G2: unverifiable without the live gateway; sequenced after any gateway-side change)
- signals: workflows pin fro-bot/agent@b711f08e (v0.114.1); release notes via gh api repos/fro-bot/agent/releases (2026-09-24): v0.114.1 (#1651) makes GATEWAY_OPERATOR_TRUSTED_PROXIES REQUIRED for proxied operator surfaces — the dashboard proxies /operator/* to the agent, so the pin bump is a deployment-contract change, not just a version bump; v0.115.0 (#1656) adds run-status checkout provenance and workspace-prep failure distinction; the clonedep reference source still sits at v0.78.0 (rm-120) (payload verified at the tag 2026-09-24, run 41c7d471 research 5cf830f2: packages/gateway/src/operator-contract/version.ts@v0.115.0 pins OPERATOR_CONTRACT_VERSION '1.7.0' — the fork sits at 1.6.0 (src/gateway/operator-contract/version.ts:15, public/operator-stream.js:32); run-status.ts@v0.115.0 adds `checkoutProvenance?: OperatorCheckoutProvenance` (line 88) plus failureKind values 'checkout-substituted' and 'workspace-unavailable', carried by NEW upstream files provenance.ts/provenance-round-trip.test.ts — additive optional fields, a non-breaking minor bump; v0.115.0 is still the latest release (published 2026-09-23T22:39:25Z); rider: upstream #520 pins pnpm 11.27.1 while package.json:57 pins 11.27.0; dated 2026-09-25, run f1ae52a3f04b research 9ca3c1b6: the pending absorb set grew to FIVE commits (0c7489d..eb8ca95) — pnpm 11.27.1 #520, fro-bot/agent v0.115.0 #521, bfra-me/.github v4.32/v4.33 #519/#523, codeql-action digest 2892aa5 #524 — touching exactly codeql.yaml/fro-bot.yaml/release.yaml/renovate.yaml/scorecard.yaml/package.json; renovate.yaml is fork-absent (rm-136 keep-deleted) and release.yaml is fork-diverged by the App-token gating, the two pre-identified conflict points) (dated refresh 2026-09-25, run 11d3a522 research 94f6de68: v0.115.0 remains the latest release; the rider set grew — upstream eb8ca95 #524 bumps the codeql-action digest to 2892aa5 in codeql/release/scorecard workflows and rides this same absorb batch alongside the pnpm 11.27.1 rider) — 2026-09-25 update (run 4e7c674 roadmap): upstream autonomy-upstream/main advanced the target again 2026-09-25: fro-bot/agent v0.115.0 (commits 66a542d/0c7489d); adopt together with rm-191 (renumbered at this integrate) rather than a standalone pin chase — 2026-09-25 update (run 4e7c674 compound 9d398097): the pin half LANDED with the cycle-1 batch (implement f9529920, landing at this integrate): fro-bot.yaml pins v0.115.0 and the pnpm 11.27.1 rider landed (packageManager + Dockerfile both stages, fork-exclusion-guard test updated); the remaining scope is the gateway-verified 1.7.0 surface work (PINNED_CONTRACT_VERSION, checkoutProvenance plumbing, failureKinds, runbook) — still sequenced after a gateway-side change (dated 2026-09-25, run ec07634c research 57cc38a7): fro-bot/agent v0.115.1 released 2026-09-24T22:51Z (#1658, commit c41502b — workspace clone hardening: askpass executable under restrictive umasks, credentials limited to GitHub HTTPS prompts, global+system git config disabled during clone, planted-URL-rewrite resistance) supersedes every 'v0.115.0 remains the latest' datum above; the pin target advances to v0.115.1 (the 'or later' acceptance already accommodates it — the remaining 1.7.0 gateway-verified scope is unchanged); clonedep reference drift widens (v0.78.0 → v0.115.1, still the v0.78.0 clone per AGENTS.md); rider landed 2026-09-25 cycle-17 (run 71991cb3 implement 73932b46): fro-bot.yaml:340 digest 8e6494ed (v0.115.0) → 930ffc9f (v0.115.1, tag verified lightweight → commit 930ffc9fed93d5a6e5f2c0f9a51b4bc2fcf1e15e) — the item's gateway-verification acceptance remains the deferred core); compound 2026-09-25 (run 71991cb3 compound d730c0da): the v0.115.1 digest rider rode both engine full-gate greens (ephemeral PRs #170 and #174, 10/10 checks each at base 20ade64 + batch) — the open core stays the gateway-verified OPERATOR_CONTRACT_VERSION 1.7.0 surface (checkoutProvenance plumbing, failureKinds, runbook), sequenced behind a live-gateway change per this item's own acceptance; leading reliability candidate for the next cycle alongside rm-103
- acceptance: pin bump to v0.115.0 or later rides a gateway-verified change — GATEWAY_OPERATOR_TRUSTED_PROXIES documented in docs/runbooks/gateway-access.md alongside the proxy topology it assumes, local mirror surfaces (operator-client, sse-reader conformance tests) re-checked against the v0.115.0 contract, full suite plus a live gateway login regression before merge; concrete 1.7.0 surface work (added 2026-09-24, run 41c7d471 roadmap): PINNED_CONTRACT_VERSION -> 1.7.0, parser/Dto plumbing for checkoutProvenance (commit / branch-or-detached / dirty / mid-git-op) and the two new failureKinds, fixture-harness parity fixtures for both new shapes, and the operator run detail rendering the provenance — the release's own payoff: readers see which code a persistent workspace actually ran; the pnpm 11.27.1 rider lands in the same batch
- evidence: workflow diff plus runbook section; conformance-test diff; live operator session evidence recorded in the cycle batch doc

### Origin validation-residue sweep (push-gated)
- id: `rm-159` | track: reliability | priority: 32.0 | status: candidate (added 2026-09-24, cycle-9 compound — the sweep needs push authority this run's phases are prohibited from exercising)
- signals: interrupted and completed engine validations leave residue on origin — ephemeral PRs from `conductor/ci-base-**` branches plus `conductor/ci-*` and `conductor/ci-base-*` refs (observed 2026-09-24: the SIGINT'd zero-checks validator's PR and refs from run 04c7d9c2; PRs #44/#47 and refs from parallel runs 35938942882/35939793512); the rm-131 guard asserts the tracked tree only, so origin-side residue accumulates invisibly until a human or push-authorized phase sweeps it (fresh counts 2026-09-24, run 41c7d471 assess: gh pr list --state open -> 21; git ls-remote origin 'conductor/*' -> 44 of 52 heads; at least 5 open PRs superseded by direct landings) (fresh counts 2026-09-25, run 11d3a522 research 94f6de68: gh pr list --state open -> 23; git ls-remote origin 'conductor/*' -> 38 heads; PR #104 (cycle-10 batch) IS superseded-by-content — the identical buildx readback + empty guard landed as rm-166/rm-178 at integrate 217a18866568; PR #82 (run b4aac9ba84e6 cycle-11) is NOT — corrected 2026-09-25 by prioritize pass 857fbce7 (the research pass's supersession claim was wrong): verified against main 57a182b, PR #82's toml '>=4.2.0' override closing GHSA-82x6-q7mm-w9cf (transitive '@opencode-ai/plugin>effect>toml'; lockfile still resolves toml@4.1.2, no toml entry under pnpm-workspace.yaml overrides), its fail-visible partial-installation-enumeration half (src/github/installations.ts still 'continue's on mint/list-repos failures), and its '.conductor/' .gitignore half are all UNLANDED — only the visual-ubuntu and .pnpm-store halves landed elsewhere (PR #12); PR #82's internal rm-171..rm-176 numbering also collides with main's current ids, so its content rides cycle-13's batch by content with renumbering, closing #82 at push)) (cycle-13 status 2026-09-25, compound 753a69d9: the rescue-by-content is now IMPLEMENTED IN-TREE and fully validated — B1 enumeration + B3 403-only fallback + B4 shutdown + the toml>=4.2.0 override (lockfile re-resolved to 5.0.0, `pnpm audit -r` clean, GHSA-82x6-q7mm-w9cf closed) + the `.conductor/` gitignore entry with guard assertions; targeted+full gates green at ephemeral PRs #131/#132; the close-#82-at-push step remains for the push-authorized phase)
- acceptance: one push-authorized change closes stale `conductor/ci-base` PRs (`gh pr close`) and deletes stale `conductor/ci-*` and `conductor/ci-base-*` refs (`git push origin --delete`), leaving only refs for in-flight runs; the sweep checklist recorded in a solutions doc so future landing phases clean their own residue at push time
- evidence: gh pr list shows no stale ci-base PRs; git ls-remote origin 'refs/heads/conductor/*' shows only live run branches


<!-- cycle-1 extension (2026-09-24, conductor run 3538ec96ece446b18ca438b9e485928f roadmap attempt 1c078a483c44455c866c2582e3c4090b; items rm-164/rm-165 sourced from this run's assess (1a9c85ac) + research (501c5ddc); dated signals appended to rm-103/rm-107/rm-108/rm-112/rm-129/rm-139/rm-141; ids minted above the all-lineage max rm-163 after the PR #43/b0fc297 collision audit) -->

### Filter-free prose-residue guard test (live self-hosted claims, wiki-writer, absent-tool claims)
- id: `rm-164` | track: reliability | priority: 48.0 | status: implemented 2026-09-24 (cycle-10 B2 + review extension: six stale self-hosted sites era-qualified/dropped + the filter-free repo-wide guard with curated allowlist, negative-verified, 19/19; TERM_PATTERN extended by review to cover self_hosted/selfhosted/wikiwriter/'wiki writer' variants; the acceptance's third class — absence-claims naming retired tools (e.g. 'no pipx installed') — deferred, extension point is TERM_PATTERN; LANDED on main at 539c632 — test/prose-residue-guard.test.ts live with TERM_PATTERN at :90, verified 2026-09-24 run 41c7d471 roadmap)- signals: stale-prose sweeps have failed by SCOPE twice already — the extension-filter lesson (the extension-less Dockerfile hid COPY wiki-writer/package.json lines) and the 2026-09-23 path-scope lesson (an rm-147 comment sweep scoped to .github/ + AGENTS.md left three stale self-hosted sites: vitest.config.ts:10, web/vitest.config.ts:13, test/dockerfile-context.test.ts:12) — and run 3538ec96 assess F6 re-verified all three live at origin/main 82312f7 plus the runbook-comment class (codeql.yaml:42, main.yaml:132, release.yaml:438, AGENTS.md:47 — rm-148's rewrite batch covers those four but is pending landing); no standing test guards the class, while rm-131/rm-132/rm-154 are the repo's precedent for converting recurring sweep lessons into structural guard tests that run in Main
- acceptance: one vitest guard asserts zero LIVE claims about this repository's own CI/runners/tooling — self-hosted-runner claims, wiki-writer references, and absence-claims naming tools the repo no longer runs — sweeping the tree with NO file-extension filter and NO path-scope filter; the only exclusions an explicit allowlist of known-historical or external-deployment trees (docs/solutions/, docs/prioritization/, docs/ideation/, .agents/, node_modules/, web/dist/, .git/, .conductor/), each with a one-line justification comment, and prose that merely describes external deployments (the gateway runbook's topology) explicitly allowlisted rather than swept; the term list lives in one exported constant so future lessons extend one array; negative-verified by injecting a violation and watching the guard redden; green in pnpm test and Main
- evidence: the guard test in the suite with its negative-verification note; after landing alongside rm-148's rewrites, the three cycle-7 sites and the runbook quartet grep clean with only allowlisted hits remaining

### Conductor phase-worktrees base at origin/main tip (stale-frame cost)
- id: `rm-165` | track: reliability | priority: 26.0 | status: candidate (added 2026-09-24, run 3538ec96 assess F8 + research idea 8; fleet-convention item recorded here because the cost lands in repo work)
- signals: this run's assess worktree based at canonical HEAD 7809df6 while origin/main sat 18 commits ahead at 82312f7 — three candidate findings were already fixed at main and burned per-item currency archaeology (git show 82312f7: re-verification for every finding) before scoring; the cycle-9 compound hit the same class harder (its roadmap phase authored rm-148..rm-153 in a worktree whose base went stale mid-cycle; four of six items had already landed and the ids never reconciled into this file) (recurrence 2026-09-24, run 41c7d471: this run's own assess phase was dispatched at 7809df6 while origin/main sat at 539c632 — ~20 commits behind, so every fresh finding needed main-side re-verification before scoring; exactly this item's cost case) (second recurrence 2026-09-24, run 5b6e8e801952: this run's assess worktree based at canonical HEAD 7809df6 sat 20 commits behind origin/main 539c632 — same currency-archaeology cost, same cure (pin phases to the origin/main tip at dispatch) — 2026-09-25 update (run 4e7c674 roadmap): recurred AGAIN 2026-09-25 (run 4e7c674): assess worktree cut at 7809df6 (side-branch tip from 2026-09-22) while origin/main was 480c92c — 11 commits behind at dispatch (worktree reflog 16:55:49Z vs origin push 14:54:03Z), and this cycle's roadmap phase had to target 480c92c content via patch rather than edit the stale tree (third recurrence 2026-09-25, run 71991cb3 assess 8e16b753: worktree dispatched at 7809df6 while origin/main sat 45 commits ahead at 20ade64 (merge-base == HEAD; ahead 0) — 11 already-landed workstreams (supply-chain gates, shutdown wiring, rate budgets, rm-168 matcher, …) required per-finding delta archaeology before scoring; the item's required evidence datum is recorded NEGATIVE at .conductor/progress/8e16b75314fb4297b3c0b5c024407c62.ndjson step 4; the same run's research phase re-measured the identical 45-behind frame fresh); cure applied END-TO-END this run (compound 2026-09-25, run 71991cb3): every downstream phase authored against origin/main content (roadmap built from pristine 20ade64; stewardship + prioritize re-grounded every surface at 20ade64) and implement re-based the worktree 7809df6→20ade64 BEFORE touching files — zero stale-base findings survived to the batch; protocol codified in docs/solutions/workflow-issues/stale-dispatch-base-and-parallel-lineage-mint-collision-2026-09-25.md
- acceptance: campaign dispatch pins each phase worktree at the origin/main tip at dispatch time (re-fetch immediately before creating the worktree), or a phase-0 freshness check aborts and re-creates the worktree when HEAD trails origin/main; the convention recorded in the conductor campaign config or its AGENTS note citing the cycle-9 rm-148..rm-153 reconciliation as the cost case
- evidence: the next cycle's assess breadcrumb records worktree HEAD == origin/main tip at dispatch; a mid-cycle tip advance is handled by the freshness check rather than per-finding currency archaeology


<!-- cycle-1 extension #2 (2026-09-24, conductor run 41c7d4711ef349fba6c47a2bb73f93b0 roadmap attempt 737837ea5bac4082a7ab465558e1685a; items rm-166..rm-172 sourced from this run's assess (c5bf00b9, measured against origin/main 539c632) + research (5cf830f2); the sibling-report findings (run d6455cdc, reported 2026-09-22 and confirmed live at 539c632 but never tracked) enter the id space here; dated signals appended to rm-102/rm-103/rm-112/rm-114/rm-123/rm-157/rm-159/rm-164/rm-165; rm-108 needed no refresh — its 2026-09-24 measure from run 3538ec96 is current) -->

### Repair the base-drift digest readback (broken sentinel) + digest-parse guard test
- id: `rm-166` | track: reliability | priority: 88.0 | status: implemented 2026-09-24 (cycle 11 B1, run 41c7d471 implement f043c0ad: readback switched to `docker buildx imagetools inspect node:24-slim | awk '/^Digest:/{print $2; exit}'` + explicit `[ -z "$LIVE_DIGEST" ]` READBACK-ERROR guard with its own message at .github/workflows/base-drift.yaml; rider workflow-statics guard test test/base-drift-digest-readback.test.ts scans all workflows comment-stripped for the manifest-inspect|/^Digest:/ class AND the silently-ignored buildx --format template class (#3317); local actionlint 0, focused 267/267, and the remote Check Workflows gate green twice via the engine's ephemeral PRs #110/#111; the acceptance's pushed-sha re-dispatch evidence rides the landing — TIME-BOXED: first cron fire 2026-09-28T04:13Z, land before then; pending landing; added 2026-09-24, run 41c7d471 assess F1 + research C9; CONVERGENCE 2026-09-24 at integrate 217a18866568: rm-178 (run 6fe26972, cycle-12) tracked and implemented the same repair in parallel — both candidates landed the identical workflow content, so this item and rm-178 close together as one fix with two lineage ids)
- signals: .github/workflows/base-drift.yaml:39 reads the live digest via `docker manifest inspect node:24-slim | awk '/^Digest:/{print $2; exit}'`, but plain manifest-inspect output on ubuntu-latest carries no `Digest:` line — dispatch run 35970747110 (2026-09-24T07:38Z) logged `live node:24-slim digest: ` EMPTY and exited 1; same-hour control: Release run 35974819368 on the identical runner image parsed the buildx form fine (`docker buildx imagetools inspect` prints `Digest: sha256:...`), and the workflow is byte-identical 82312f7->539c632 — the sentinel is structurally broken, so the first scheduled fire is a guaranteed false red that also masks real drift (the registry's true state is unverifiable from a no-docker host; the 2026-09-22 token-authed probe found parity)
- acceptance: the live-digest readback switches to the proven form (`docker buildx imagetools inspect node:24-slim | awk '/^Digest:/{print $2; exit}'`) with an explicit empty-guard that fails with its own message distinguishing read-failure from drift; a re-dispatch run at the pushed sha logs BOTH digests and exits 0 at parity; rider (research C9): a workflow-statics guard test (pattern: test/release-trigger-paths.test.ts) fails any `manifest inspect` piped into a `/^Digest:/` awk across .github/workflows/ with a pointer to the buildx form — actionlint cannot catch this class
- evidence: dispatch run log shows pinned + live digests and a parity verdict; the guard test reddens on an injected manifest-inspect snippet and greens after; the 2026-09-28 scheduled fire reports green or genuine drift

### should-release rule 2 must consult lockfile changes (devDep-only skip can ship a changed runtime image)
- id: `rm-167` | track: reliability | priority: 56.0 | status: implemented 2026-09-24 (cycle 11 B2, run 41c7d471 implement f043c0ad: rule 2 in scripts/should-release.ts consults hasLockChange before the devDeps-only skip — skip only when devDeps-only AND lockfile unchanged, lockfile presence fails open to release; hard-release paths still win; test/should-release.test.ts flipped the lock+devDeps skip pin to the release verdict and added the narrowed no-lock skip regression; focused 267/267 + full remote suites green twice via ephemeral PRs #110/#111; pending landing; added 2026-09-24, run 41c7d471 assess F2)
- signals: scripts/should-release.ts rule 2 returns the devDeps-only skip BEFORE hasLockChange is consulted (lines 103-127), while release.yaml's `paths:` includes both package.json and pnpm-lock.yaml; a devDep-only package.json diff whose lockfile regen also moved an in-range runtime resolution (hono ^4.7.11, @hono/node-server ^2.0.0, transitives) merges with zero release candidates — the shipped image's runtime deps silently changed with no GHCR publish and no release note
- acceptance: rule 2 skips ONLY when the diff is devDeps-only AND pnpm-lock.yaml is unchanged; a lockfile change in a devDep-only PR forces the release-decision path; a table-driven test covers the four quadrants (devDep/runtime x lockfile-unchanged/changed) including an in-range runtime-resolution move inside a devDep-only diff
- evidence: should-release tests cover the quadrant matrix; the test log shows the devDep-only+lockfile-changed case producing a release verdict

### Aggregator: match the generic FORBIDDEN message in the vulnerability-alerts permission-error path
- id: `rm-168` | track: reliability | priority: 48.0 | status: implemented 2026-09-24 (cycle 11 B3, run 41c7d471 implement f043c0ad: isVulnerabilityAlertsPermissionError also matches the generic `resource not accessible by integration` message shape, anchored at the alerts call site (no global 403 swallow); test/aggregator.test.ts feeds a bare-message 403 and asserts the graceful no-alerts retry, not STALE; focused 267/267 + full remote green twice via ephemeral PRs #110/#111; pending landing; added 2026-09-24, run 41c7d471 assess F3; first reported by sibling run d6455cdc 2026-09-22, re-verified live at 539c632; feeds rm-112's degradation surface)
- signals: src/github/aggregator.ts:492-506 — isVulnerabilityAlertsPermissionError matches specific 403 shapes but not GitHub's generic `Resource not accessible by integration` message, which the function's own comment at :497-499 lists among the shapes that arrive; on a repo where the App lacks vulnerability_alerts:read, the whole repo degrades to STALE every 60s refresh instead of the designed graceful path (openAlerts=null / permission-denied marker)
- acceptance: the matcher covers the generic message while staying narrow (anchored to the alerts call site, never a global 403 swallow); a fixture test feeds the generic-message 403 and asserts the repo renders the permission-denied degradation rather than stale; a companion test confirms genuine failures still fail visible (rm-112 semantics)
- evidence: aggregator test with the generic-message fixture green; log or seeded-fixture evidence of one permission-denied repo rendering the marker instead of STALE

### Listener replay upsert un-acks notifications
- id: `rm-169` | track: reliability | priority: 44.0 | status: implemented 2026-09-24 (cycle 11 B4, run 41c7d471 implement f043c0ad: the replay upsert in src/listener/store.ts sets read_at = messages.read_at (identity fields separated from mutable state — acked replays stay read, unread replays stay unread); test/listener-store.test.ts flipped the reset-to-unread pin to stays-read and added unread-replay + ack-preservation cases; focused 267/267 + full remote green twice via ephemeral PRs #110/#111; the replay-vs-redelivery distinction noted at the site; pending landing; added 2026-09-24, run 41c7d471 assess F8; first reported by sibling run d6455cdc, re-verified live at 539c632)
- signals: src/listener/store.ts:91-95 — the dedupe-replay upsert rewrites the notification row with read_at = NULL, so a replayed webhook for an already-acked notification silently un-acks it (the operator re-sees it as unread); ingest-auth carries no replay nonce distinguishing a genuine second delivery from a replay
- acceptance: a replayed delivery preserves read_at and any operator-set mutable state — the upsert separates identity fields from mutable state; a test replays an acked webhook and asserts read_at survives; the replay-vs-redelivery distinction documented at the site
- evidence: listener store test covering replay-after-ack green; grep shows the upsert no longer resets read_at

### Mint fallback scopes transient errors out of the permission-subset degradation
- id: `rm-170` | track: security | priority: 40.0 | status: implemented 2026-09-25 (cycle 13 B3, run 11d3a522 implement a14443cc + targeted 036fe196: the full->core fallback in src/github/installations.ts:167-190 now fires ONLY on 403-shaped (permission) mint failures; status-less, 5xx, and 429 errors rethrow after exactly one mint attempt with a distinct transient log marker, so a blip can never cache a scope-less token for the TTL — 6 new tests pin both error classes incl. single-mint assertions; focused+full validation green via ephemeral PRs #131/#132; pending landing; added 2026-09-24, run 41c7d471 assess F4; first reported by sibling run d6455cdc, re-verified live at 539c632)
- signals: src/github/installations.ts:167-190 — the full->core permission fallback fires on ANY mint error including transient network failures, then the reduced-scope token is cached for its TTL (~55min), so one transient blip blanks alert panels for most of an hour with no telemetry distinguishing scope-loss from error
- acceptance: the fallback triggers only on permission-shaped errors (403 with the App-permissions shape); transient errors retry or fail the refresh visibly without caching a scope-less token; tests cover both error classes; a log marker distinguishes fallback-by-permission from mint-failure
- evidence: installations tests for the two error classes; a forced-transient scenario in tests never produces a cached core-scope token

### Graceful shutdown: handle SIGTERM/SIGINT
- id: `rm-171` | track: reliability | priority: 36.0 | status: implemented 2026-09-25 (cycle 13 B4, run 11d3a522 implement a14443cc + targeted 036fe196: NEW src/shutdown.ts — SIGTERM/SIGINT handlers (once-only, idempotent) drain aggregator -> listener store -> server with a 9s bounded force-exit deadline (10s at implement; lowered to 9s by review fix F4 so the deadline log lands before docker's 10s SIGKILL grace) and a settled guard so a late deadline is a no-op; injectable seams for tests; wired into serve() so the previously unreachable 'close' stop-path actually fires; 8 new tests in test/shutdown.test.ts cover registration, drain order, second-signal force-exit, close-error path, and deadline races; the runtime docker-stop drain-log evidence remains post-landing; added 2026-09-24, run 41c7d471 assess F5; first reported by sibling run d6455cdc, re-verified live at 539c632)
- signals: grep SIGTERM/SIGINT src/ -> 0 hits; the Dockerfile runs Node as PID 1 (no init wrapper), so `docker stop` (SIGTERM + grace period) kills in-flight requests and SSE streams abruptly — no drain, no listener flush, no shutdown log line
- acceptance: the server installs SIGTERM/SIGINT handlers that stop accepting, drain in-flight work (bounded), close the listener store, and log the shutdown reason; a container stop during a streaming request logs the drain instead of dying mid-frame; the drain path is tested or evidenced by a documented procedure
- evidence: handler present in the server entry; a local docker stop shows drain logs; suite green

### Stop re-reading the kill-switch file per request
- id: `rm-172` | track: reliability | priority: 30.0 | status: implemented 2026-09-24 (cycle 11 B5, run 41c7d471 implement f043c0ad: the '/' push-enabled handler in src/server.ts reads web/dist/index.html async-once and caches behind SPA_SHELL_CACHE_TTL_MS = 5s — no synchronous file I/O on the request path, a rebuilt shell is picked up within the bound, missing shell still 404s; test/operator-ui.test.ts gains TestAppOpts webDistRoot for a hermetic temp-dir flip test + missing-shell case; focused 267/267 + full remote green twice via ephemeral PRs #110/#111; pending landing; added 2026-09-24, run 41c7d471 assess F6; first reported by sibling run d6455cdc, re-verified live at 539c632)
- signals: src/server.ts:829 — the push-enabled '/' handler performs readFileSync (+ stat) on every authenticated page load in the pushKillswitch middleware path — synchronous disk I/O on the hot path for a flag that changes never-to-rarely
- acceptance: the kill-switch state is read once at startup or cached behind a short mtime/stat guard; the '/' path performs no synchronous file I/O; a test covers the cached semantics (a flip honored within the documented bound or at restart)
- evidence: grep shows no readFileSync in the request path; suite green

- id: `rm-182` | track: reliability | priority: 57.0 | status: implemented 2026-09-24 (renumbered from rm-177 at the integrate merge — cycle-12 owns the landed rm-177 meanings; cycle:1 batch B1 in-tree, pending landing; CONVERGENCE: rm-166 (cycle-11) + rm-178 (cycle-12) landed this same repair's buildx primary — the merged workflow keeps that primary and carries this item's registry token/HEAD fallback, one fix with three lineage ids; extraction guard + registry token/HEAD fallback + distinct NOT-drift red; validated — engine ephemeral-PR CI 9/9 green PRs #108/#109; live proof arrives at first weekly cron 2026-09-28 04:13 UTC)
- signals: base-drift.yaml:30 extracts the live digest and only guards the PINNED side against emptiness — dispatch run 35970747110 (2026-09-24T07:38Z) failed `pin 0e0ff40 vs live ` with the live side EMPTY while the registry truth was pin == live (auth.docker.io token + registry-1.docker.io HEAD, this run): `docker manifest inspect` on hosted runners can emit no Digest line yet exit 0 (anonymous manifest auth/rate fragility), so every pin "drifts" against the empty string and the weekly detector reds on nothing
- acceptance: (1) empty LIVE digest fails the job with a DISTINCT message naming extraction failure (never a drift verdict); (2) a curl-based fallback (Docker Hub token auth + registry HEAD, or `crane digest`) runs when the primary extraction yields nothing — recipe verified working from this environment this run; (3) a drift verdict requires BOTH digests non-empty in the run log; (4) actionlint clean
- evidence: re-dispatch base-drift with an intentionally empty live side — the job fails with the extraction-failure message, not a drift line; the 2026-09-28 scheduled run green with both digests logged (pin 0e0ff40 == live at 2026-09-24)

- id: `rm-183` | track: reliability | priority: 38.0 | status: candidate (renumbered from rm-178 at the integrate merge — cycle-12 owns the landed rm-178 meanings; added 2026-09-24, run 5b6e8e801952 assess F2 + research C3 — the rm-129 follow-on)
- signals: the landed limiter re-keys per XFF first-hop ONLY when RATE_LIMIT_TRUSTED_PROXY is enabled (safe default), but in the default proxy topology every client behind the gateway still collapses into ONE bucket per path class (60/min public covering /, healthz, /auth/*) — a scanner can starve the operator's SPA load/login, and nothing the server emits signals the collapse; upstream documents the same proxy-identity class (31564c4)
- acceptance: (1) with trusted-proxy off, the limiter emits exactly ONE warning per process lifetime once it observes multiple distinct XFF values against a single direct address (the detector itself rate-limited); (2) docs/runbooks/gateway-access.md gains a verification step (two-source curl through the proxy, confirm distinct buckets); (3) suite covers the log-once behavior
- evidence: the collapse warning greps out of server logs in a proxied dev setup; unit test asserts single emission; runbook diff

- id: `rm-184` | track: security | priority: 29.0 | status: implemented 2026-09-24 (renumbered from rm-179 at the integrate merge — cycle-12 owns the landed rm-179 meanings; cycle:1 batch B2 in-tree, pending landing; strict-false gate + privateCoercedCount warning, +5 fixtures; ephemeral-PR CI PRs #108/#109; prevention doc docs/solutions/security-issues/metadata-private-flag-fail-closed-coercion-2026-09-24.md)
- signals: src/github/metadata.ts:286 gates the redaction denylist on strict equality `private === true` — a data-branch entry `private: "true"` (string) passes the type check as a PUBLIC entry and its repo name flows toward installation enumeration, bypassing the denylist-before-query invariant; the boundary should fail closed on any non-false private value and skip-and-count anything structurally ambiguous
- acceptance: metadata validation treats every non-false, non-absent private value as private (fail-closed), routes such entries into redactedNodeIds BEFORE any GraphQL query, counts malformed private values in the existing skip counter, and tests cover string/number/null variants
- evidence: unit tests with `private: 'true'`, `private: 1`, `private: null` fixtures; grep shows no strict-equality private gate remains

- id: `rm-185` | track: reliability | priority: 27.0 | status: implemented 2026-09-24 (renumbered from rm-180 at the integrate merge — cycle-12 owns the landed rm-180 meanings; cycle:1 batch B3 in-tree, pending landing; MintedToken {token, expiresAt} seam honored with 55-min cap, +2 fake-timer tests; ephemeral-PR CI PRs #108/#109; PR #82 (cycle-11) touches the same two files — reconcile at landing)
- signals: src/github/app-client.ts:84-95 receives expiresAt from the createAppAuth auth() result but discards it, so the installation-token cache (installations.ts:147-149) guesses a 55-minute TTL — a token GitHub minted shorter gets served expired (401 mid-cycle) and a longer-lived one gets churned early; the Result<T,E> seam already carries the value across the boundary
- acceptance: the cached token entry stores the API-provided expiry (now+55min cap only as fallback when absent), refresh triggers on real expiry with a small safety margin, and a test proves the cache honors a short expiresAt fixture rather than the 55-minute guess
- evidence: unit test with a 5-minute expiresAt fixture asserting refresh at the right boundary; no literal 55-minute TTL remains the primary source of truth

- id: `rm-181` | track: docs | priority: 23.0 | status: candidate (added 2026-09-24, run 5b6e8e801952 research C7; sibling of rm-159's origin-side residue sweep — different class: completed validations' PRs left open forever)
- signals: ten conductor validation drafts (#18-#67) sit open on codeo1io/dashboard with zero non-CI checks and no landing path — they are historical validation evidence, not work queues, and they pollute every `gh pr list` surface the operator and agents scan (20 open PRs, half dead drafts) (fresh census 2026-09-25, run 71991cb3 research 77dda854: 23 open — 12 validation drafts (#18-#67 era) + 7 LIVE batch-landing PRs (#43 cycle-1, #45 cycle-9, #113 cycle-11, #136 cycle-13, #155 cycle-14, #157 cycle-15, #166 cycle-1-parallel; each head verified NOT an ancestor of origin/main 20ade64) + 4 pre-conductor-era; the class now strands FULL BATCHES, not just drafts — PR #166 (run fc4bb6a491a2, same repository-maintenance cycle:1 label) carries monitoring-gate + bounded-refresh implementations on a 45-behind base with 104-file conflict mass vs current main; main moves via direct integrate merges while conductor landing PRs strand open by default)
- acceptance: a documented disposition — close-with-comment (pointing at the recorded validation evidence) for drafts older than N days, or a small GITHUB_TOKEN-only workflow that auto-closes stale conductor-draft PRs past N days with a standard comment; the choice recorded in docs/runbooks; `gh pr list` open count reflects real work only
- evidence: gh pr list before/after; runbook or workflow diff; zero conductor drafts older than N days remain open

### Release build context ships git state and local secrets to the daemon (.dockerignore absent)
- id: `rm-186` | track: security | priority: 54.0 | status: implemented 2026-09-25 (status-string correction by run 71991cb3 stewardship a24c9316 + implement 73932b46: the item LANDED at 5c9c5d2 'chore: land cycle-13 batch (environmental determinism)', integrated 4d8ec12 — .dockerignore present on main with the full entry set and test/dockerfile-context.test.ts:117-131 already asserts existence + entries; the candidate string had gone stale in the landing window; original: renumbered from rm-184 at the integrate merge into d00d095 — the parallel cycle:1 batch (run 5b6e8e801952) landed first and owns the landed rm-184/rm-185 meanings; added 2026-09-25, run f1ae52a3f04b assess 9ca979e9 + research 9ca3c1b6)
- signals: the repo root has no .dockerignore (verified absent on origin/main 57a182b and in both open-lineage PR trees #113/#114; correction 2026-09-25, prioritize attempt 4a726441: the PR sweep missed PR #97 — its branch conductor/run-c2ae28ab9962 strands a complete 24-line .dockerignore with a measured ~340 MB -> ~3 MB context cut that never landed, so this item is implemented as a content-landing reconcile of that artifact, not a fresh derivation); .gitignore ignores .env, but `docker build` transfers the whole repo-root context — .git, node_modules, web/node_modules, test artifacts, and any locally-present .env — to the daemon on every Release build; the Dockerfile's selective COPY lines keep the image itself clean, but the context transfer has already exposed the bytes (secret-scanning exposure class on shared builders, plus context-size waste on every run) (2026-09-25 compound, run f1ae52a3f04b implement 21c37a08 + targeted_tests d7f87dba + full_tests e4483ab8: IMPLEMENTED in the cycle-13 batch and validated at full scope — .dockerignore landed by content from PR #97's 24-line artifact with an updated provenance header, the guard extended in test/dockerfile-context.test.ts (existence + minimal entry set + never-excludes-a-COPY-source cross-check; red-to-green and non-vacuity both proven), and the measured context cut re-verified at 403 MB -> 8.2 MB (~49x); zero executable-surface drift during validation (porcelain fingerprint e1f560efcb63c067 stable across targeted and full gates); ephemeral full gate 10/10 green at PR #118 (commit d56d9d6) and PR #120 (commit bcba21c) — awaiting landing, so the item stays candidate until main shows the file)
- acceptance:
  - a repo-root .dockerignore excludes at minimum .git, node_modules, web/node_modules, .env and siblings, test fixtures/artifacts, and web/dist — reviewed against the Dockerfile COPY set so nothing the image needs is excluded
  - the structural guard family (test/dockerfile-context.test.ts) asserts .dockerignore presence plus the minimal entry set, negative-verified against its absence
  - a Release build stays green with the ignore in place (proving the COPY set unaffected) and the measured context-size reduction is recorded in the cycle batch doc
- evidence: `ls .dockerignore` + `cat .dockerignore`; guard test green in `pnpm test`; a green Release run at the bumped commit

### One corrupt links cell bricks the listener messages endpoint
- id: `rm-187` | track: reliability | priority: 47.0 | status: candidate (renumbered from rm-185 at the integrate merge into d00d095 — the parallel cycle:1 landing owns the landed rm-185 meanings; added 2026-09-25, run f1ae52a3f04b assess 9ca979e9)
- signals: (sequence-blocked 2026-09-25, prioritize attempt 4a726441: PRs #23 and #113 both carry unlanded src/listener/store.ts deltas for the dedupe-un-ack replay bug — first candidate once either lands, per the rm-141/PR-#45 precedent) src/listener/store.ts:51 parses `JSON.parse(row.links)` unguarded inside the row mapper for GET /api/listener/messages — a single row whose links column holds non-JSON (schema drift, truncated write, manual edit) throws and 500s the whole endpoint, hiding every message behind one bad row; PR #113's store.ts change targets the dedupe-un-ack replay bug and leaves its line-53 twin parse unguarded, so no open lineage covers this half; no existing test seeds a corrupt links value (2026-09-25 compound, run f1ae52a3f04b prioritize 4a726441 + full_tests e4483ab8: gated OUT of the cycle-13 batch with evidence and re-verified live at cycle close — double-locked by PRs #23 and #113, both still unlanded; the full-gate green at PR #120 confirms main still ships the unguarded parse, so this remains the first candidate the moment either lock lands and this item's acceptance block stays the authoritative spec)
- acceptance:
  - the mapper treats a corrupt links cell as degraded data (empty list, other row fields intact) and never throws; the endpoint returns 200 with the healthy rows
  - degradation is observable (counter or log line) so silent data loss cannot recur
  - a test seeds a corrupt-links row (plus null/empty variants) and asserts the endpoint 200s with remaining rows — negative-verified before the fix
- evidence: targeted store/messages tests green in `pnpm test`; seeded-corruption transcript recorded in the cycle batch doc

### ubuntu-latest alias-flip guard for non-containerized jobs
- id: `rm-188` | track: reliability | priority: 33.0 | status: candidate (renumbered from rm-186 at the integrate merge into d00d095 — shifted with the cycle-13 block to keep ids contiguous above the landed cycle:1 items; added 2026-09-25, run f1ae52a3f04b research 9ca3c1b6)
- signals: 16 `runs-on: ubuntu-latest` sites across 9 workflow files (main.yaml x6, release.yaml x3, base-drift/canary/codeql/dependency-review/fro-bot/scorecard/visual x1) ride the rolling alias; actions/runner-images maps ubuntu-latest to 24.04 while ubuntu-26.04 images are already published (README fetched 2026-09-25) — a future alias flip silently swaps system deps and the image's bundled node under every non-containerized job; only the visual gate is insulated (containerized playwright pin); the mixed-provenance baseline fragility already on record makes surprise image flips the highest-cost failure class (2026-09-25 compound, run f1ae52a3f04b implement 21c37a08 + full_tests e4483ab8: IMPLEMENTED as the pin route — all 16 sites across the 9 workflows now pin runs-on: ubuntu-24.04 with a dated per-file re-eval comment aligned to rm-139's Node-26 window, zero ubuntu-latest remains outside those comments, actionlint clean via the container form, and the full gate (including Check Workflows on the real pinned image) is 10/10 green at PR #120 — awaiting landing) (dated 2026-09-25, run 70ba5270 research 761db5e1: ubuntu-26.04 images went GA 2026-09-18 (actions/runner-images issue #14747, amd64 + Arm64) — the alias-flip risk this pin opted out of now has a live upstream clock; the pinned 24.04 fleet is unaffected until image retirement, and when the 26.04 pre-flight acceptance clause is exercised the visual baselines must be regenerated inside the pinned playwright container matched to the new base)
- acceptance:
  - workflows pin `ubuntu-24.04` explicitly (opting out of the alias) OR a dated 26.04 pre-flight proves the suite green there first; the decision is recorded next to the pins with a re-evaluation date aligned to rm-139's Node-26 gate
  - actionlint and the full suite green at the change; no `ubuntu-latest` remains without a recorded reason
- evidence: `git grep -n 'runs-on:' .github` showing pins or the pre-flight job's green run; actionlint 0 via the container form
### Tool/security pin refresh (codeql-action digest, pnpm, agent pin)
- id: `rm-191` | track: reliability | priority: 78.0 | status: implemented 2026-09-25 (renumbered from rm-181 at the integrate merge into 8bbff5c6 — main's landed rm-181 (docs: conductor validation drafts) owns that id; pin edits landed run 4e7c674 implement f9529920 — codeql-action 1c5b675→2892aa5 across codeql/scorecard/release workflows, pnpm 11.27.0→11.27.1 in packageManager + Dockerfile, fro-bot/agent v0.114.1→v0.115.0 riding rm-157's contract; the reserved CI-green evidence arrived 2026-09-25 via run 4e7c674 full_tests a93fa5ca: ephemeral Check Workflows + Lint + Types + Test + CodeQL + Dependency Review all green at snapshots 8c58add (PR #125) and f5d2721 (PR #128); updated 2026-09-25 run 4e7c674 compound 9d398097)
- signals: autonomy-upstream/main is 7 dependency commits ahead of merge-base f65f8a43 (decoded 2026-09-25): codeql-action digest 1c5b675→2892aa5 (eb8ca95, touches codeql.yaml:48/54, scorecard.yaml:42, release.yaml:328 at the 57a182b implement base — fork still on 1c5b675), pnpm 11.27.0→11.27.1 (f35e281), eslint 10.10.0→10.11.0 (b8c7982; already on origin/main), fro-bot/agent→v0.115.0 (owned by rm-157); two further upstream bumps are N/A in the fork (renovate removed, rm-136): the renovate-action bump (9908bcf) and the bfra-me renovate.yaml digest pin (e51b480) — neither file exists in the fork
- acceptance: all codeql-action refs across .github/workflows equal 2892aa5; package.json packageManager pnpm@11.27.1 with `pnpm install --frozen-lockfile` clean; adopted by cherry-picking upstream commit content with fork exclusions preserved (never a wholesale merge — invariants #3206/#3210), no renovate/wiki-writer paths reintroduced (fork-exclusion-guard test green)
- evidence: `grep -rn 'codeql-action' .github/workflows | grep -v 2892aa5` empty; actionlint container form 0 findings; `git ls-files .github | grep renovate` empty; ephemeral CI Check Workflows green at the pushed sha

### failingChecks drill-down: workflow title + attempt via additive GraphQL fields
- id: `rm-192` | track: operator-experience | priority: 68.0 | status: candidate (renumbered from rm-182 at the integrate merge into 8bbff5c6 — main's landed rm-182 (base-drift readback repair) owns that id; added 2026-09-25, run 4e7c674 research S2; extends the rm-177 exported-query seam; staged as the LEAD for the next maintenance cycle by run 4e7c674 prioritize 1be0e527 — excluded from cycle-1 only as the staged next-cycle lead)
- signals: the shipped REPO_STATUS_QUERY selects counts only (checkRuns totalCount; aggregator.ts:135 documents the count-only ceiling) so a red repo tells the operator "how many checks failed" but never which workflow or attempt; GitHub GraphQL changelog 2026-06-05 added WorkflowRun.displayTitle + WorkflowRun.runAttempt (additive; verified 2026-09-25 that no removals touched statusCheckRollup/checkSuites/checkRuns in the 38 changelog entries since 2026-05-01) (convergence watch 2026-09-25, run 71991cb3 research 77dda854: the cycle implementing this staged lead must first convergence-check PR #166 (run fc4bb6a491a2 — SAME repository-maintenance cycle:1 label): its unlanded tree carries aggregator/server work (DASHBOARD_MONITORING_REFRESH gate, bounded refresh) minted at the same 7809df6-era stale base and tracked as implemented-but-unlanded on rm-181's census; re-baseline onto the origin/main tip BEFORE minting implementations, per rm-165)
- acceptance: query adds checkSuites.nodes.workflowRun { displayTitle runAttempt } and checkRuns node name/detailsUrl; query remains strictly read-only (zero mutations); exported-query guard + weekly canary (rm-177/179) updated to cover the new selection and green against the live API; RepoCiStatus DTO gains the fields with mapper tests; the SPA renders workflow title/attempts for red repos with a pinned snapshot test
- evidence: `node scripts/graphql-canary.ts` exit 0 with the exact exported query text (sha256-logged); test/query-shape-guard.test.ts negative-verified against field removal; focused vitest suites green; GraphQL changelog 2026-06-05 entry cited in the PR body

### Runtime parsers for operator-client approval/run responses
- id: `rm-193` | track: reliability | priority: 52.0 | status: implemented 2026-09-25 (renumbered from rm-183 at the integrate merge into 8bbff5c6 — main's landed rm-183 (rate-limit collapse warning) owns that id; run 4e7c674 implement f9529920 — src/gateway/operator-contract/run-responses.ts added with permissive structural parsers for launchRun/getRunSnapshot/listRunApprovals/decideRunApproval responses; all five operator-client fetch sites now parse via the contract seam and log GatewayProtocolError on malformed payloads; 30 fail-closed focused tests green; check-types + lint green; full-suite CI green at snapshots 8c58add (PR #125) + f5d2721 (PR #128) per run 4e7c674 full_tests a93fa5ca; updated 2026-09-25 run 4e7c674 compound 9d398097; SUPERSEDES 2026-09-25 at the a4a97e30 integrate: the batch's prune proposal (theirs rm-165 B2 — remove launchRun/getRunSnapshot/connectRunStream as zero-non-test-caller surfaces) did NOT land — main's landed decision keeps and hardens those surfaces via this item; the batch's header-truth half DID land: the operator-client module header now states the live server-side use (getCurrentSession in src/server.ts gateway-auth) and the zero-non-test-caller status truthfully instead of the stale 'no live /operator/* calls' claim)
- signals: src/gateway/operator-client.ts:560/580/634 cast launchRun/getRunSnapshot/listRunApprovals responses straight to DTOs with no runtime parse, while sibling session/csrf/repos/vapid/push paths all parse via operator-contract — a gateway shape drift surfaces as undefined-field bugs in the approvals UI instead of a fail-closed error (still true at 57a182b — main did not touch these call sites)
- acceptance: operator-contract gains parsers for the four responses with malformed-input fail-closed tests (missing/typed-wrong fields → Result error, never undefined deref); boundary casts stay `as unknown as X` at the fetch seam only; check-types + lint green
- evidence: new focused suite covering malformed/partial fixtures red-before-green; no behavior change on well-formed fixtures (golden responses)

### Approvals aging + attention ordering on the operator surface
- id: `rm-194` | track: operator-experience | priority: 50.0 | status: candidate (renumbered from rm-184 at the integrate merge into 8bbff5c6 — main's landed rm-184 (metadata private fail-closed) owns that id; added 2026-09-25, run 4e7c674 research S4)
- signals: review-queue/aging UX is the dominant feature cluster in the competing-tools survey (gh search 2026-09-25: snowshoe 41★, pr-desk, review-dash all lead with review queues); this repo already carries the raw material — operator-client approvals DTO with createdAt and the store read-flag precedent — but renders approvals without age emphasis or unread-first ordering
- acceptance: createdAt bucketing (fresh / >24h / >72h) with visual emphasis; unread-first then age-descending ordering pinned by a test; SSE live appends preserve the ordering invariant
- evidence: focused vitest suite pins the sort under ties/empty/single-element; a manual operator-surface check recorded in the PR body

### Snapshot trend sparkline (failingChecks/openPr history)
- id: `rm-195` | track: operator-experience | priority: 40.0 | status: candidate (renumbered from rm-185 at the integrate merge into 8bbff5c6 — main's landed rm-185 (installation-token expiry) owns that id; added 2026-09-25, run 4e7c674 research S5; sequenced AFTER the rm-107 monitoring-consumer decision)
- signals: snapshots are retained across cycles (aggregator history) but the SPA renders only the current state — the single operator gets no trajectory (is that red repo getting better or worse?); informs rm-107's undecided consumer question with a concrete capability
- acceptance: history source + retention window defined and tested (single-point, empty, and window-boundary cases); small SVG/CSS sparkline (no new chart dependency) in the monitoring surface rm-107 selects; DTO documented
- evidence: focused suite for boundary cases; no new runtime deps (lockfile unchanged)

### In-range lockfile refresh (hono, @hono/node-server, vite patch)
- id: `rm-196` | track: reliability | priority: 56.0 | status: in-progress (renumbered from rm-186 at the integrate merge into 8bbff5c6 — main's landed rm-186 (.dockerignore seal) owns that id; partial 2026-09-25, run 4e7c674 implement f9529920 — manifest floors raised hono ^4.7.11→^4.13.8 and @hono/node-server ^2.0.0→^2.1.1 (lockfile already resolved there), packageManager synced to pnpm@11.27.1, frozen-lockfile install clean; full-suite CI green at snapshots 8c58add (PR #125) + f5d2721 (PR #128) per run 4e7c674 full_tests a93fa5ca; hono 4.13.9 + vite 8.3.1 were BLOCKED by pnpm-workspace.yaml minimumReleaseAge 1440 (published 2026-09-24T01:32Z / 2026-09-24T12:26Z) — 2026-09-25 update (run 4e7c674 compound 9d398097): BOTH maturity timestamps have now passed (01:32Z / 12:26Z on 2026-09-25), so `pnpm update hono vite` + the vite manifest pin 8.3.0→8.3.1 are READY as a next-cycle rider; see docs/solutions/workflow-issues/supply-chain-release-age-gate-time-gates-fresh-versions-2026-09-25.md)
- signals: package.json ranges already admit newer resolved versions — hono ^4.7.11→4.13.9 (release 2026-09-24), @hono/node-server ^2.0.0→2.1.1, vite 8.3.0→8.3.1 (exact pin, patch) — a plain `pnpm update` inside ranges, zero code change; the 2026-09-25 attempt surfaced that minimumReleaseAge 1440 gates BOTH hono 4.13.9 and vite 8.3.1 on publish age, so the refresh is time-gated, not blocked on code
- acceptance: `pnpm update hono @hono/node-server vite` within declared ranges only — majors excluded (those are rm-108/rm-133 territory); manifest edits limited to same-major floor-raises and the vite exact-pin patch 8.3.0→8.3.1 (amended 2026-09-25 review fix c8b926e6: the implement batch did legitimately raise floors hono ^4.7.11→^4.13.8 and @hono/node-server ^2.0.0→^2.1.1, which the original wording forbade); full suite + `pnpm build:web` green; `pnpm outdated` shows those three at in-range latest
- evidence: lockfile diff touches only those packages; CI Main green at the pushed sha

<!-- cycle-1 extension (2026-09-24, conductor run a4a97e30 roadmap attempt f8230f54, based on origin/main 763c1fa; items rm-164..rm-167 sourced from this run's assess 036d520b + research 66ae4073; id space audited repo-wide and across open PRs — PR #43 carries rm-160..rm-163, so these start at rm-164)
     compound revision 2026-09-24 (run a4a97e30 compound attempt b114e7db, same run, pre-review evidence only): rm-164..rm-167 flipped candidate -> implemented (cycle-10 batch B2/B3/B1 in-tree at base 82312f7, pending landing) with dated validation signals — local full suite 3098/3098, cloud gate green on every real check, the sole red being the documented rm-131 snapshot artifact (byte-identity proven on snapshots 7db188a6/fe5c9930); statuses move to Completed only after landing verification on origin/main. -->
<!-- renumbered at the 2026-09-25 a4a97e30 integrate into main's cycle-14 lineage (8ef806f): the batch's rm-164/rm-165 content converged with / was superseded by main's landed rm-112 (cycle-13 B1) and rm-193 (operator-contract parsers) — see those items' integrate notes; rm-166→rm-203 and rm-167→rm-204 below are the surviving content with statuses updated to landing-at-this-merge -->

### Docs-truth micro-batch 3: README stack list, vite precache comments, ideation exclusion set
- id: `rm-203` | track: docs | priority: 30.0 | status: implemented 2026-09-24 in-tree, landing via this integrate (renumbered from rm-166 — main's landed rm-166 (base-drift readback repair) owns that id; cycle-10 B3, run a4a97e30 implement f14dfbc0 at base 82312f7, wording corrected in review-fix 145e0e3c per review 584624d6 finding 3; validated by the batch compound: repo-wide sweep clean, lint green, local suite 3098/3098 with the sole red the documented rm-131 artifact — status moves to Completed after landing verification on origin/main per the batch's own convention)
- signals: README.md:29 still names vite-plugin-pwa + Workbox in the stack list though the eight workbox runtime deps were removed at f334fb2 (rm-128 — zero workbox-* in package.json since); web/vite.config.ts:32,45-46 carries precache-era globPatterns/manifestTransforms wiring plus a cross-reference comment describing caching behavior web/src/sw.ts:43-45 explicitly discards (__WB_MANIFEST thrown away by the kill-switch); docs/ideation/2026-09-20-repository-extensions-research{,-2}.md carry wiki-writer prose references outside the canonical sweep's exclusion set (docs/solutions and docs/prioritization only), so every fresh agent session greps into prose that reads like a live exclusion target
- implemented 2026-09-24 (cycle-10 B3, run a4a97e30 implement f14dfbc0, base 82312f7; wording corrected in review-fix 145e0e3c per review 584624d6 finding 3): README stack list states the kill-switch truth (zero Workbox mentions; web/src/sw.ts purges caches and unregisters itself), vite.config precache-era comments rewritten with era-qualified historical wording, the ideation wiki-writer residue was left VERBATIM per the acceptance's second branch — era-qualified dated upstream history, untouched — and the canonical residue sweep was widened in the binding-docs doc in the same change: docs/ideation added to the documented exclusion set, the sweep command fixed to basename-form --exclude-dir values that actually match, and never-rewrite-the-dated-research-logs codified
- acceptance: README stack list describes the kill-switch service worker truthfully (binding-docs consistency invariant); vite.config comments state the kill-switch reality explicitly (independent of rm-138's architecture decision — comment truth does not wait on it); the ideation residue either reworded to read-only/exclusion wording or the ideation tree added to the documented exclusion set with the canonical grep form updated in the same change; pnpm lint green
- evidence: grep Workbox README.md returns zero; the vite.config.ts comment block quoting the discard; the canonical sweep (no extension or path filters) returning only intended historical hits

### Extend the weekly drift watch from base digests to dependency and action pins
- id: `rm-204` | track: reliability | priority: 44.0 | status: implemented 2026-09-24 in-tree, landing via this integrate (renumbered from rm-167 — main's landed rm-167 (should-release rule 2) owns that id; cycle-10 B2, run a4a97e30 implement f14dfbc0 at base 82312f7; validated by the batch compound: yaml parse, embedded-script syntax checks, actionlint container form, lint, plus a live network smoke run — 2 pins genuinely behind, 5 by-design majors correctly silenced; re-validated actionlint-clean at this integrate after adaptation to main's rm-188 ubuntu-24.04 pin and rm-204 id references — status moves to Completed after landing verification on origin/main per the batch's own convention)
- signals: base-drift.yaml (landed via rm-135/cycle-7) compares ONLY Dockerfile HEAD refs to the live node:24-slim digest — while the measured upstream drift since merge-base 0c7489d is 4 commits, ALL dependency-pin moves (bfra-me/.github 4.32 + 4.33, fro-bot/agent v0.115.0, pnpm 11.27.1 against the fork's 11.27.0) that nothing watches: dependabot's window opens ~2026-10-03 (rm-102), npm-latest minors (hono 4.13.9 vs lockfile 4.13.8) ride weekly at best, and upstream bump velocity measured 2026-09-24 is 115 commits/30d at ~85% dependency bumps — the drift class the watch covers (base digests) had already self-resolved to parity while the class it does not cover IS the entire live delta
- implemented 2026-09-24 (cycle-10 B2, run a4a97e30 implement f14dfbc0, base 82312f7): a report-only pin-drift job in base-drift.yaml covering packageManager fork-vs-upstream, npm deps vs registry (range output normalized, v-prefix normalized), and workflow action/image pins vs latest releases; always exits 0 — step-summary plus warnings only, because chronic npm-latest drift must not redden a weekly watch — dependabot stays the sole pin-mover, and majors deliberately pinned by design (rm-133: vitest 4 to 5, typescript 6 to 7) are classified not-drift rather than firing weekly; review-fix 2026-09-24 (145e0e3c, review 584624d6 finding 1): the uses-scan now captures the trailing '# vX.Y.Z' comment and treats it as authoritative for sha pins; live re-run against all 12 real pins: 10 current, exactly 2 accurate warnings (fro-bot/agent v0.115.0, pnpm/action-setup v6.1.0); at this integrate the job was adapted to main's rm-188 `ubuntu-24.04` runner pin (theirs authored ubuntu-latest) and its id references updated to rm-204; first scheduled fire 2026-09-28 emits the report-only summary and warning annotations
- acceptance: the weekly workflow also diffs the fork's packageManager pin, workflow action pins, and key npm pins against upstream's values and npm latest, reporting divergence in the same single-tracking-issue discipline; strictly read-only — it never bumps (dependabot owns bumps, rm-102/rm-146); actionlint container gate green; first run logs the full drift table (digests + pins) for review (issues are disabled on this fork — the single-tracking-issue clause is recorded N/A-on-fork per review 584624d6 finding 4; the shipped discipline is the weekly report-only summary + warning annotations with dependabot the sole pin-mover; the clause revives only if issues are ever enabled)
- evidence: workflow diff passing actionlint; first scheduled or dry-run output showing both digest and pin rows (N/A tracking issue on this fork — issues disabled)

<!-- cycle-1 extension (landing note 2026-09-25, integrate case 636b69c7 — candidate 399447b, run b4aac9ba cycle-11 batch, PR #82): the batch minted rm-171..rm-176 in-flight; main's cycles 11–14 landed meanwhile and content-rescued three of its items by content (see the INTEGRATE-MERGE comment above), landed meanings won rm-171/rm-172, and the batch's ONE surviving new item is renumbered rm-205 above the all-lineage max rm-204 (the d00d095/8bbff5c6 renumber convention). -->

### Triage the standing container critical: zlib1g CVE-2023-45853 (no fix ships)
- id: `rm-205` | track: security | priority: 55.0 | status: candidate (added 2026-09-24, run b4aac9ba research; renumbered from the batch's rm-174 at the 636b69c7 integrate 2026-09-25)
- signals: code-scanning alert #67 is the single OPEN CRITICAL — CVE-2023-45853 against zlib1g 1:1.2.13.dfsg-1 inside the built container image, Fixed Version EMPTY (Debian bookworm zlib; the issue is NVD-disputed minizip-contrib code, Debian carries no-dsa); open since 2026-09-16 with no action possible in-tree; per the repo's triage convention a finding with NO fixed version is document-and-watch, not chase; base-drift.yaml has landed on origin/main (weekly cron Mondays 04:13 UTC, first scheduled fire 2026-09-28) and is the natural re-check rider (dated refresh 2026-09-25, run 71991cb3 research 77dda854: the critical surface is FOUR open criticals (45853/42496/13221/8376; #51 corrected 2026-09-25 review b310d051), not one — CVE-2023-45853 (zlib1g) joined by CVE-2026-42496 and CVE-2026-13221 (both image-level, created 2026-09-16); node:24-slim registry HEAD digest == the pinned sha256:0e0ff40… (fresh negative: NO base drift — a rebuilt base is NOT the available fix), so per the triage convention remediation is in-image apt-patching of the affected bookworm packages plus a solution doc; scope of this item extends from the single zlib alert to the critical+high image-level census recorded on rm-117; live check 2026-09-25, run 71991cb3 implement 73932b46: docker run node:24-slim@sha256:0e0ff40 + apt-cache policy shows EVERY census package at Installed == Candidate (perl-base 5.36.0-7+deb12u3 — bookworm-security only offers the OLDER u2; zlib1g 1:1.2.13.dfsg-1; util-linux/libmount1 2.38.1-5+deb12u3; libacl1 2.3.1-3; libtinfo6 6.4-4; gzip 1.12-1; tar 1.34+dfsg-1.2+deb12u1; libsystemd0 252.39-1~deb12u2) — ZERO fixed versions published, so in-image patching has nothing to patch to (the #3308 precondition fails for every alert family); census table + re-check procedure recorded in docs/solutions/best-practices/container-cve-census-2026-09-25-no-fixed-versions.md; the watch stays wired via base-drift weekly digest checks (a rebuilt base absorbing fixes moves the digest) + Release-time trivy/codeql scans); compound 2026-09-25 (run 71991cb3 compound d730c0da): the actionable scope of this item is satisfied as document-and-watch — nothing-to-patch proven at the pinned digest (every census package Installed == Candidate), census table + re-check procedure landed with the batch; the NEXT action is mechanical: on the first base-digest move (base-drift weekly) or first Debian fixed-version publication, re-run the census doc's re-check procedure and only then consider in-image patching
- acceptance: alert #67 annotated with the disputed/no-fix rationale and links; a docs/solutions entry records the triage decision and the re-check trigger; the next base-digest bump re-scans and either closes the alert (if Debian ships a fixed zlib1g) or re-documents it — dismissal-without-rebuild is explicitly out of scope
- evidence: `gh api repos/codeo1io/dashboard/code-scanning/alerts/67` state plus the annotation; the solution doc; base-drift run log after the next digest bump

### Cookie-key canonical-format enforcement
- id: `rm-206` | track: security | priority: 55.0 | status: implemented 2026-09-25 (landed at the c2ae28ab integrate — authored in-batch as rm-165, renumbered because landed meanings own ids; conflict case 6a7122e8)
- signals: `loadCookieKey`/`decodeKey` previously accepted lenient decodings — stripped-padding base64, non-canonical hex/base64 lengths, and raw UTF-8 passphrases all silently loaded via Node's lenient decoders, so a misprovisioned cookie key booted and signed instead of failing at startup; a 44-char all-hex input fell through to a base64 mis-decode (the "44 A's" exploit path yielding 33 null bytes)
- acceptance: the encoded key form must be the exact canonical encoding of its decoded bytes — hex at 2 chars/byte or padded base64 at 4 chars per 3 bytes, decoding to ≥32 bytes; non-canonical encodings, degenerate (single-repeated-byte) keys, and raw passphrases throw fail-fast with specific messages at load; documented in README.md's Configuration section
- evidence: src/session.ts `decodeKey`/`canonicalFormatError`/`assertNonDegenerate`; test/session.test.ts `canonical format enforcement (rm-206)` suite (raw passphrase rejected, stripped padding rejected, 44-A hex-then-base64 fallthrough rejected, canonical forms accepted)

### Visual gate never fires on the operator runtime (paths filter gap)
- id: `rm-207` | track: reliability | priority: 36.0 | status: implemented 2026-09-25 (cycle-17 batch, run 71991cb3 implement 73932b46: paths gained web/index.html + web/vite.config.ts + public/** and test/visual-gate-paths.test.ts pins the coverage set; original: added 2026-09-25, run 71991cb3 assess F2 + research A1; verified absent from every prior id)
- signals: `.github/workflows/visual.yaml` pull_request paths on origin/main are only `web/src/**`, `tests/visual/**`, `playwright.config.ts`, and the workflow file itself (visual.yaml:10-13) — `public/**` (the ~3k-line operator runtime: operator-stream.js et al., served to browsers at `/operator/*`) and the SPA entry `web/index.html` never trigger the gate; the stale worktree's filter even included `web/vite.config.ts`, which the 45-commit window REMOVED — the filter narrowed as the runtime grew; tests/visual/dashboard.spec.ts runs full-page baselines + axe critical/serious against the fixture server that serves `public/`, so operator-runtime regressions are visually material yet unguarded; compound 2026-09-25 (run 71991cb3 compound d730c0da): coverage is now pinned in-tree — test/visual-gate-paths.test.ts parses the workflow and asserts all seven paths including public/**, so a future filter narrowing fails CI; the batch rode two engine full-gate greens (ephemeral PRs #170 and #174, 10/10 checks each at base 20ade64 + batch, visual included)
- acceptance: pull_request paths gain `public/**`, `web/index.html`, and `web/vite.config.ts`; a guard test (or lint rule) asserts the paths filter covers every non-test surface the fixture server serves, so a new public/ file cannot silently escape the gate; a PR touching only `public/operator-stream.js` shows the visual check as required — demonstrator OUTSTANDING at landing (review 2026-09-25 b310d051: no pr/ci tool ran it this cycle; backstop = the unfiltered push trigger on main fires the gate on every landing push; verify on this batch's landing PR before closing)
- evidence: `git show origin/main:.github/workflows/visual.yaml` diff; the guard test green in Main CI; the demonstrator PR's checks panel

### Unread badge silently freezes on auth expiry or network failure
- id: `rm-208` | track: operator-experience | priority: 32.0 | status: candidate (added 2026-09-25, run 71991cb3 assess F3 + research A2)
- signals: `web/src/App.tsx:45-56` pollUnreadCount calls fetchListenerMessages every 30s but only acts on success (`if (res.ok) setUnreadCount(...)`, no else branch) — on 401 or network failure the count freezes at its last good value with the interval continuing forever; the operator runtime it feeds (public/operator-*) DOES surface auth-required states, so the SPA is the only surface that hides expiry; no stale/auth indicator exists anywhere in the badge path; compound 2026-09-25 (run 71991cb3 compound d730c0da): deliberately NOT in the cycle-17 batch (surface-collision discipline — this run owned workflow/census surfaces only) and carried as the LEADING operator-experience candidate for the next cycle: auth-expired badge state + stale indicator with last-good timestamp + poll-loop test mocks, per this item's acceptance
- acceptance: a 401 stops the poll loop and renders an auth-expired state (badge cleared, re-auth affordance consistent with existing session UX); a network-failure streak of ≥2 marks the badge stale (aria + title with last-good timestamp) instead of freezing silently; focused tests in web/src/App.test.tsx cover ok/401/network-resolved mocks for the poll loop
- evidence: new App.test.tsx cases green (`pnpm test`); manual 30s-expiry check noted in the PR body or a docs/solutions entry

### CodeQL PinnedDependencies alert on the base-drift unpinned download
- id: `rm-209` | track: security | priority: 42.0 | status: implemented 2026-09-25 (cycle-17 batch, run 71991cb3 implement 73932b46: the curl|node pipe replaced with an in-node fetch() — bytes parsed as JSON, never executed; alert dismissal deferred to final_validation once the fix reaches the scanned default branch; original: added 2026-09-25, run 71991cb3 research A3 — a brand-new code-scanning alert created 2026-09-25T04:01:10Z at the 20ade64 scan)
- signals: code-scanning alert (rule PinnedDependenciesID, medium, security score 9) at `.github/workflows/base-drift.yaml:172` — the step pipes `curl -fsSL https://raw.githubusercontent.com/.../scripts/read-pnpm-version.js | node` and CodeQL flags the downloadThenRun as not pinned by hash; the alert is the only workflow-path alert among the 61 open (rest are image-level, see rm-117; correction 2026-09-25, run 71991cb3 implement 73932b46: the flagged pipe is .../fro-bot/dashboard/main/package.json | node -p at base-drift.yaml:172 — scripts/read-pnpm-version.js does not exist in this repo, the research-phase description had conflated the rule's example text with the step content)
- acceptance: the step fetches pinned content — either download + sha256-verify against a digest constant recorded in the workflow, or an actions/github-script step reading the file via the GitHub API (no unpiped remote execute; landed form 2026-09-25: in-node fetch() + JSON.parse — bytes are parsed, never executed, so no digest constant is required); actionlint container-form gate stays green; the alert is dismissed-with-link once the fix lands; compound 2026-09-25 (run 71991cb3 compound d730c0da): the in-node fetch() replacement rode both engine full-gate greens (ephemeral PRs #170/#174, CodeQL Analyze SUCCESS each time — the pattern is clean at CI on the batch tree); the open alert object itself only updates on the default-branch scan AFTER landing, so the dismissal-with-link step stays queued for final_validation/landing
- evidence: `gh api repos/codeo1io/dashboard/code-scanning/alerts?state=open` no longer lists the PinnedDependencies alert (or lists it dismissed-with-reason); workflow diff; actionlint run log

## Completed items

### Merge-residue guard test: assert the wiki-writer and renovate exclusions
- id: `rm-131` | track: reliability | priority: 88.0 | status: completed (landed at 2f4d118 2026-09-21 — test/fork-exclusion-guard.test.ts present at origin/main 92688a2, verified via cat-file)
- signals: the 2026-09-20 upstream merge 57c9c6b silently erased every fork exclusion — Dockerfile and README.md now sit at zero diff versus `autonomy-upstream/main` (`git diff HEAD autonomy-upstream/main -- Dockerfile README.md` is empty), Dockerfile:10,31 COPY wiki-writer/package.json while `wiki-writer/` does not exist in the fork, vitest.config.ts gained a `wiki-writer` include, `.github/workflows/renovate.yaml` returned, README/AGENTS regressed to upstream's relaxed wiki-write wording, and 18 engine-internal `.conductor/progress/` ndjson files landed tracked; the run-93069e23 guard test never landed (`git log --all` shows no test/wiki-writer-guard.test.ts), so nothing failed before Release run 35562249072 went red on push; upstream tip da6809d still carries every hazard, so the next absorb re-risks the same class (2026-09-21 compound, run 87e3c32f implement c6a1806d: IMPLEMENTED as test/fork-exclusion-guard.test.ts — 9 invariants matching acceptance exactly, incl. the org-flip and clonedeps-manifest checks and git ls-files .conductor empty; negative-verified: re-adding the upstream COPY line turns the guard red; runs in Main via the existing test include; suite 2033+1070 green; 2026-09-24 compound, run 04c7d9c2 full_tests d4b515a6: this guard is the SOLE cloud red in cycle-9 validation and it is an artifact of the validation route, not a regression — the engine's ephemeral-PR clone starts at main 763c1fa which still TRACKS .conductor/progress/70f231743f…ndjson (blob ec22d0cf), and the clone's delta patch pathspec ':(exclude).conductor/**' drops the batch's staged deletion of exactly that file, so the deletion can never reach the clone; green locally through the staged deletion (git ls-files .conductor empty) and self-cures on main once the untrack lands; mechanism documented in docs/solutions/workflow-issues/validation-clone-exclude-pathspec-drops-staged-breadcrumb-deletion-2026-09-24.md)
- acceptance was: a vitest test with no file-extension filters (the Dockerfile is extension-less) asserts zero occurrences of `wiki-writer` in Dockerfile, README.md, AGENTS.md, .github/copilot-instructions.md, vitest.config.ts, pnpm-workspace.yaml; asserts package-manager pnpm pinned 11.27.0 (upstream carries 11.8.0); asserts `.github/workflows/renovate.yaml` absent; asserts no `.conductor/` path is tracked; the test runs in Main so the class fails at PR time, not on push
- completion evidence: `git log origin/main -1 -- test/fork-exclusion-guard.test.ts` → 2f4d118 (fix: restore fork exclusion invariants erased by upstream merge 57c9c6b); file present at tip 92688a2; local gates at 92688a2 green (lint 0, types 0, suite 3115/3115, run 2ae7d10a assess); Main green on the landing lineage (run 35597011430 at f8ff412)

### PR-side Dockerfile validity gate: parse COPY sources before merge
- id: `rm-132` | track: reliability | priority: 84.0 | status: completed (landed at 2f4d118 2026-09-21 — test/dockerfile-context.test.ts present at origin/main 92688a2, verified via cat-file)
- signals: Main gates Lint/Design/Types/Test/Check Workflows/Scripts Load but never builds the image, and Release is main-push-only, so PR #7 merged green on 2026-09-20 while the same push broke Release at builder step 5/8 (COPY wiki-writer/package.json: not found, run 35562249072) — the third consecutive Release red (7ba9c82, b0ed12d, d0d17fc), all invisible at PR time; GHCR :latest stale since 4b1b406 (2026-09-20T21:53Z) (2026-09-21 compound, run 87e3c32f implement c6a1806d: IMPLEMENTED as test/dockerfile-context.test.ts — parses COPY/ADD in both stages, skips --from sources, fails on any missing tree path; negative-verified on the exact upstream hazard line; adds ~0.3s to the suite)
- acceptance was: a fast Node test parses Dockerfile COPY/ADD sources across both stages and fails when a referenced path does not exist in the tree — catching missing-context breakage in seconds without a docker build; runs in Main so the class fails at PR time; complements rm-131 (content exclusion) with structural validity
- completion evidence: `git log origin/main -1 -- test/dockerfile-context.test.ts` → 2f4d118; file present at tip 92688a2; suite green locally at 92688a2 (3115/3115); Release green at 493b82f (run 35582769317) on the landing lineage

### Pin dependabot major versions until migrations are planned
- id: `rm-133` | track: reliability | priority: 74.0 | status: completed (landed at 2f4d118 2026-09-21 — dependabot.yml ignore block present at origin/main 92688a2: npm ignore for vitest and typescript majors, dated re-evaluation comment 2026-10-21)
- signals: .github/dependabot.yml (landed as rm-102) carries no ignore rules while majors are already out — vitest 5.0.1 versus repo 4.1.11 (vitest 4 removed the `basic` reporter and its CLI rejects jest-style flags: documented constraints), TypeScript 7.0.2 versus repo 6.0.3 on the erasableSyntaxOnly pipeline, pnpm 12.5.1 versus the Dockerfile-pinned 11.27.0; the first grouped major PR from the 2026-09-19 window is expected around 2026-10-03 and will red the suite unbidden; npm view probes 2026-09-21: hono 4.13.8, playwright 1.63.0, tailwind 4.3.3, vite 8.3.0 all current within the majors in use — no minor drift (2026-09-21 compound, run 87e3c32f implement c6a1806d: IMPLEMENTED in .github/dependabot.yml — npm ignore for vitest+typescript majors, re-evaluation comment dated 2026-10-21; pnpm majors are NOT a dependabot surface (no ecosystem covers the corepack/Dockerfile pin) so that pin is guarded by rm-131's 11.27.0 assertions instead)
- acceptance was: dependabot.yml ignore rules for major-version updates of vitest, typescript and pnpm with a dated re-evaluation comment naming the migration owner; a docs decision note recording which majors are deliberately deferred and why; minor and patch updates stay open
- completion evidence: `git log origin/main -1 -- .github/dependabot.yml` → 2f4d118; ignore block verified present at tip 92688a2 (run 2ae7d10a assess); the pnpm half of the original acceptance intentionally covered by rm-131's guard instead (no dependabot ecosystem covers the pin), pnpm split to rm-140 (watch note 2026-09-24, run 6fe26972 research: a third major joins the set — jsdom 30.1.1 versus repo 29.1.1; the TS 7.0.2 evaluation must start with an @bfra.me toolchain-compat check (eslint-config 0.52.2 and tsconfig 0.13.2 are otherwise current at npm latest); node 24 base runway confirmed — support to 2026-10-20, EOL 2028-04-30 (endoflife.date), so the base pin faces no platform pressure)

### Absorb the upstream operator-push privacy policy (#495/#496)
- id: `rm-125` | track: reliability | priority: 74.0 | status: completed (landed 2026-09-21 in the absorb lineage — web/src/privacy/ module, the `/privacy` server route, and the AppShell link all present at origin/main 92688a2; fork divergence pinned at 7ba9c82)
- signals: upstream drift grew 6 to 8 on 2026-09-20 and the two new commits are one feature — 30839c6 plans the public operator push privacy policy (upstream #495) and 3f2fbe9 publishes it (upstream #496: `web/privacy.html` +87, `web/src/privacy/claims.ts` +239, 258 lines of claims tests, a `/privacy` server route, an AppShell link); the fork had no first-party privacy surface (grep, 2026-09-20), and this feature is the stated precondition for rm-106's listener digest push
- acceptance was: the policy feature hand-ported with fork layout — port intent, not a line merge (same discipline as rm-122): a `/privacy` route serves the static policy page in fork chrome, the claims module and its tests ported and green, the AppShell link lands, and the upstream drift accounting reflects #495/#496 absorbed while #493 stays skipped per rm-103
- completion evidence: `git log origin/main -1 -- web/src/privacy/` → 7ba9c82 (test(privacy): pin issues URL to the codeo1io fork — the fork-divergence commit); `web/src/privacy/` and `web/privacy.html` present at tip 92688a2; upstream delta 0 at 2026-09-21T21:50Z (run 2ae7d10a research) — #495/#496 absorbed; local suite green at 92688a2 (3115/3115)

### Server and CI hygiene batch 3
- id: `rm-130` | track: reliability | priority: 24.0 | status: completed (all three sub-items verified present on origin/main 92688a2, run 2ae7d10a assess 2026-09-21)
- signals: three verified small defects — the Main test job built the web client twice (`.github/workflows/main.yaml` Build web step plus the pretest hook at package.json:17 — redundant on the serialized single runner); `src/github/metadata.ts:331-344` silently skipped structurally-invalid PUBLIC entries while `skippedMalformedCount` (metadata.ts:281) counted only non-objects, so malformed-input volume stayed invisible to monitoring; the documented CSRF-400 retry (src/gateway/operator-client.ts:675-681) re-sent the identical request with the same token although the client exposed a `refreshCsrf` seam (operator-client.ts:522,805) — the stale-token cause the retry named could never be fixed by the retry as written
- acceptance was: one web build per test job (pretest the single build path); metadata skip counting extended to object-shape failures with a test; the CSRF-400 retry either re-fetches a fresh token via the existing seam before retrying or does not retry token rejections, with a test pinning the chosen behavior; all gates green
- completion evidence: main.yaml test job builds web once via pretest (comment at lines 108-113 names the single path); src/github/metadata.ts counts object-shape skips — "counted, never silent" (lines 329-346); src/gateway/operator-client.ts CSRF-400 retry refreshes a fresh token via the csrf refresh route before re-sending, with an rm-130-naming divergence comment; local gates green at 92688a2 (3115/3115)

### Drop the eight unused workbox runtime dependencies
- id: `rm-128` | track: reliability | priority: 34.0 | status: completed (landed at f334fb2 2026-09-20 — zero `workbox-*` entries in package.json at origin/main 92688a2, verified grep 2026-09-21 run 2ae7d10a research; f334fb2 is an ancestor of origin/main, merge-base verified)
- signals: package.json declared eight `workbox-*` packages (`workbox-window` included — the research-time count of seven missed it) alongside `vite-plugin-pwa` while zero `workbox-*` imports existed anywhere in the repo (repo-wide runtime-import scan, 2026-09-20) — the service worker is a cacheless kill-switch (`web/src/sw.ts` performs no precaching), so the workbox runtime surface was dead weight; any workbox code the PWA plugin itself needs arrives as its own transitive dependencies, not via these direct devDependencies
- acceptance was: the eight direct `workbox-*` devDependencies removed (`vite-plugin-pwa` retained only if the vite config still consumes it — verified: it does); frozen-lockfile install clean; `pnpm lint`, `pnpm check-types`, and `pnpm test` green; the repo-wide grep shows zero workbox imports
- completion evidence: `grep -c workbox package.json` → 0 at tip 92688a2; vite-plugin-pwa retained and still consumed by vite.config.ts; the vestigial precache wiring it leaves behind is tracked as rm-138

### Port upstream security fix #481 — global redaction chokepoint
- id: `rm-122` | track: security | priority: 76.0 | status: completed (landed at 7de0de3 2026-09-20 — commit names rm-122 onError port; Main/CodeQL/Release/Scorecard all green at 7de0de3 06:14Z, verified run-f69cd740 prioritize)
- signals: upstream fro-bot/dashboard merged 876a02a (PR #481, 2026-09-19) — buildDashboardApp registers `app.onError` routing unhandled request errors through `logger.error` with `sanitizeErrorMessage` and a generic 500, plus 62 lines of tests; grep-verified 2026-09-20 (run 779e7271 research): no `app.onError` anywhere in fork `src/` — an unhandled throw reached Hono's default raw `console.error`, bypassing the logger.ts single-chokepoint redaction discipline (AGENTS.md invariant 2); the fork already imports `sanitizeErrorMessage` (src/server.ts:50), but its server.ts has diverged (fixture harness, ingest routes, rate limiter), so this is a hand port of intent, not a line-level merge
- acceptance was: fork's buildDashboardApp registers `app.onError` (sanitized log line + generic 500 body, upstream parity); upstream's test intent ported — a secret-shaped throw never reaches the response body nor the log raw; existing handler behavior unchanged; full suite green
- completion evidence: `pnpm test` green including the 2 ported redaction tests; grep shows onError registered in src/server.ts; cross-ref `git show 876a02a` and upstream PR #481
- implementation note (2026-09-20, run 779e7271 implement): `app.onError` registered in src/server.ts after the middleware chain, upstream shape (logger.error with sanitizeErrorMessage(err) + generic 500 text); 2 tests added to test/dashboard.test.ts porting upstream's intent (secret-shaped throw: never in the response body, never raw in the log); dashboard.test.ts 45/45 green locally. Landed at 7de0de3 2026-09-20 with Main/CodeQL/Release/Scorecard green 06:14Z (run-f69cd740 prioritize verification)

### Binding-docs metadata-source sync
- id: `rm-121` | track: reliability | priority: 63.0 | status: completed (landed at 7de0de3 2026-09-20 — commit names rm-121 binding-docs sync; all four binding surfaces + test-narration residue; CI green at 7de0de3 06:14Z, verified run-f69cd740 prioritize)
- signals: AGENTS.md, README.md, .github/copilot-instructions.md, and the src/github/metadata.ts:4 doc comment all said the redaction source repo is `fro-bot/.github` while src/server.ts has read `codeo1io/.github` since commit 4aa5d07 (2026-08-10) — the docs described a stricter configuration than what actually runs; the deferred IR-5 residue (test/server.test.ts:210/248/254/288 titles and comments narrating fro-bot/.github) still stood at aa4ff9f
- acceptance was: all binding docs name `codeo1io/.github` as the metadata denylist source (docs updated, not code — the code is correct); a grep over binding docs for the fro-bot/.github metadata claim returns nothing; metadata.ts doc comments and tests aligned in the same change
- completion evidence: grep output post-change across AGENTS.md, README.md, .github/copilot-instructions.md, src/github/metadata.ts; pnpm lint exit 0; pnpm test green
- implementation note (2026-09-20, run 779e7271 implement): AGENTS.md:18, README.md:65, metadata.ts:4, and .github/copilot-instructions.md:17 now read `codeo1io/.github`; test/server.test.ts metadata-topology titles/comments + fixture accounts aligned (4 sites, injection-based, assertions unchanged); grep for the metadata claim across binding docs and tests returns nothing (the two `fro-bot/.github#3525` hits in test/aggregator.test.ts are upstream issue-tracker references — different semantics, left). CAUTION: the run-779e7271 stewardship grep that called copilot-instructions.md "clean" was a pathspec miss (bare filename vs the .github/ path). Landed at 7de0de3 2026-09-20 with Main/CodeQL/Release/Scorecard green 06:14Z (run-f69cd740 prioritize verification)

### Server and docs hygiene batch
- id: `rm-124` | track: reliability | priority: 26.0 | status: completed (landed at 7de0de3 2026-09-20 — commit names rm-124 hygiene; 4 of 5 B5 items, fifth moot at aa4ff9f; CI green at 7de0de3 06:14Z, verified run-f69cd740 prioritize)
- signals: five verified small defects from the 779e7271 assess — `devAutoLogin` Guard A rejected only `production` though its comment promised an explicit development/test allowlist (src/server.ts — the fixture-harness guard implements the strict form, so the divergence was between two guards in the same file); `src/github/metadata.ts` silently skipped malformed entries while the promised `log count at end` was never implemented; OAuth state compared with plain `!==` (src/routes/auth.ts) where session verification uses `timingSafeEqual` — two guards, two disciplines; the trivy best-practices doc claimed `Renovate already keeps this pin current` — false on this fork (PR #1 deleted renovate.yaml; pins move by manual absorb + weekly dependabot); `Dockerfile:41` said dependabot.yml was `staged 2026-09-19` (moot: the sentence no longer exists at aa4ff9f)
- acceptance was: devAutoLogin allowlist matches its comment (unset NODE_ENV no longer passes, mirroring the fixture guard); the malformed-entry count is logged as promised; the state comparison is timing-safe; the stale doc sentence corrected; `pnpm lint` / `pnpm check-types` / `pnpm test` all green
- completion evidence: new tests for the guard allowlist and the skip-count log; cross-ref to the session.ts timingSafeEqual pattern; git diff of the doc line; full local suite green
- implementation note (2026-09-20, run 779e7271 implement): Guard A now requires NODE_ENV=development|test explicitly (+2 pinning tests, and Guard B's message aligned); metadata.ts counts skipped malformed entries and warns with skippedCount when >0 (+1 test asserting the warning and its count); the OAuth state compare is timingSafeEqual with a length guard (+3 tests: equal-length mismatch, wrong-length mismatch → 403 not 500, happy path); the trivy doc sentence replaced with the fork-accurate drift-is-expected wording; the Dockerfile item verified moot at aa4ff9f. Landed at 7de0de3 2026-09-20 with Main/CodeQL/Release/Scorecard green 06:14Z (run-f69cd740 prioritize verification)
### Upstream absorb batch 2026-09-20 (node 0e0ff40, hono 4.13.8, retire in-image patch)
- id: `rm-111` | track: reliability | priority: 92.0 | status: completed (2026-09-20; landed at 916783f, Trivy proof at aa4ff9f)
- acceptance was: upstream content absorbed with fork exclusions preserved (no write code path; stricter read-only doc wording); all three Dockerfile pins at 0e0ff40; in-image libpcre2 patch removed; frozen-lockfile install clean; Release Trivy zero HIGH at the pushed sha
- completion evidence: pins at 0e0ff40 + patch retired in 916783f (content absorbed as direct edits, upstream history intentionally still ahead — see rm-116's double-apply note); Release run 35491419600 completed success at aa4ff9f with '🚦 Enforce fixed HIGH/CRITICAL vulnerabilities' conclusion=success (image ghcr.io/codeo1io/dashboard@sha256:9e6cbb41…, exit-code:1 fail-on-findings mode, zero enforceable findings); suite 3018 green at landing; absorb recipe at `docs/solutions/workflow-issues/upstream-lockfile-content-merge-2026-09-20.md`

### Restore required gates green at HEAD on main
- id: `rm-100` | track: reliability | priority: 100.0 | status: completed (2026-09-20; cycle-2 arc closed at aa4ff9f)
- acceptance was: Lint, Check Workflows (fro-bot.yaml job-env gate), and CodeQL Analyze green at HEAD on main via the self-hosted posture
- completion evidence: Main green at aa4ff9f (run 35490785353 — Lint + Check Workflows + full suite); CodeQL green (run 35490785207, SEMMLE_TYPESCRIPT_HOME round 2 landed aa4ff9f); the gates fixes landed at 916783f/aa4ff9f (runs 270220e7); root-cause solution doc `docs/solutions/workflow-issues/codeql-semmle-typescript-home-2026-09-20.md`

### Aggregate complete check-suite counts
- id: `rm-110` | track: reliability | priority: 55.0 | status: completed (2026-09-20; landed at 916783f)
- acceptance was: checkSuites raised past 10 with a fixture proving full counting
- completion evidence: `checkSuites(first: 100)` in both query variants at src/github/aggregator.ts:137/:174 with a 12-suite regression test in test/aggregator.test.ts; suite count 3018 at the landing tree; Main green at aa4ff9f (35490785353); page-ceiling comment rider (f4622d7e IR-6) applied at both query sites in the run-779e7271 batch-2 tree

### CI pin hygiene micro-batch
- id: `rm-113` | track: reliability | priority: 40.0 | status: completed (2026-09-20; landed at 916783f)
- acceptance was: setup-node at v7.0.0; single checkout pin repo-wide; container actionlint exit 0; Main green at the pushed sha
- completion evidence: checkout unified at v7.0.1 and setup-node at v7.0.0 incl. the setup composite (916783f); Main green at aa4ff9f (run 35490785353)

### Document the Fro Bot workflow disable state
- id: `rm-109` | track: reliability | priority: 50.0 | status: completed (2026-09-19; landed on origin/main at 3075f4a)
- acceptance was: an ops note in binding docs records the disabled state, the reason (missing secret), and the re-enable prerequisites
- completion evidence: `AGENTS.md` Fro Bot ops-conventions block (line 68) cites `disabled_manually`, the missing `FRO_BOT_PAT`, and the re-enable precondition; present at origin/main HEAD

### Shared test fixtures must be field-complete for required payload fields
- id: `rm-190` | track: reliability | priority: 35.0 | status: candidate (added 2026-09-25, cycle-13 compound, run 11d3a522 — mint-time provenance: ids were minted above the then-all-lineage max verified rm-186 (origin/conductor/run-f1ae52a3f04b, PR #130) with main's own ceiling at rm-180; renumbered rm-187→rm-190 at the integrate merge of run 11d3a522 — the landed f1ae52a3 cycle-13 batch owns rm-187 (links-parse degradation, landed via the d00d095 renumber) and open-lineage PR #136 tops at rm-189, so rm-190 mints above the re-verified all-lineage ceiling)
- signals: cycle-13's corrective targeted gate (036fe196) found 15 failing tests the implement phase's own evidence reported green: (a) five `fakeEnumerate` fixtures in test/server.test.ts were built with `vi.fn().mockResolvedValue(...)` — any-typed at the call site — so they lacked the newly REQUIRED `failedInstallationIds` field; the resulting TypeError was swallowed by the new fail-visible catch and surfaced only as wrong snapshot assertions; (b) old-contract mint stubs still drove the core-scope fallback with plain `Error`s after the rm-170 contract narrowed it to 403-shaped failures. `pnpm check-types` cannot catch either class. Full lesson recorded in docs/solutions/best-practices/cross-phase-test-evidence-reverify-fixture-drift-2026-09-25.md
- acceptance: shared fixture factories (makeEnumerateResult and kin) carry every required field with a comment tying fields to the payload type, or construct from a typed literal so adding a required field breaks the fixture at check-types time; a repo convention line (AGENTS.md or the solutions doc) states that a phase's claimed test counts are not evidence for the next phase — the gate re-runs the authoritative command
- evidence: a fixture-completeness assertion or typed-factory refactor lands; the corrective incident is citable from the run 11d3a522 breadcrumbs (.conductor/progress/036fe196af1a4b5c970e63fc9caaa178.ndjson steps 3-7)

### Aggregator cold-start in-flight degradation contract
- id: `rm-197` | track: reliability | priority: 59.0 | status: implemented 2026-09-25 in-tree, validated pre-review (cycle-14 batch B1, run ec07634c implement c3bbc0c8; minted above rm-196 after the 8bbff5c6 renumber took the original rm-191 allocation; validation run ec07634c targeted_tests 8b11a67e + full_tests 1cab3836: local focused 248/248, then authoritative ephemeral-PR gates ALL 9 checks SUCCESS twice — PR #146 @ f2a23bd3 and PR #147 @ e55dcb1 — pending landing; review fix 07afb0e5 closed independent-review F1: the two server.ts transports the unit-test fakes hid are now wired — see evidence)
- signals: between start() and the first refresh completing, getSnapshot() returned {repos: [], staleBanner: false} — authoritative-looking fresh-empty (aggregator.ts:929 at 6fd7d04); start() gated interval installation behind await refresh() with no re-entry guard; zero timeout/AbortSignal wiring across the Octokit constructions (app-client.ts, installations.ts), so a stalled serial first walk yields minutes of false-fresh empty; the FAILED first-refresh path DID banner (markSnapshotStale, :665) — only the in-flight window was uncovered. 6fd7d04 (PR #140) closed the warm half of rm-112, making this the last false-fresh gap
- acceptance: (1) cold getSnapshot serves bannered empty (explicit not-fresh) while any refresh may be in flight; (2) every GitHub transport call time-bounded (request.timeout via the AppClientOptions seam, default 30s); (3) interval installed independently of the first refresh completing; (4) stalled transport trips the existing FAILED-refresh banner path
- evidence: implemented: cold branch returns staleBanner:true (aggregator.ts getSnapshot), AppClientOptions.requestTimeoutMs + exported GITHUB_REQUEST_TIMEOUT_MS=30_000 wired into the ThrottledOctokit ctor (app-client.ts) and the installation-token Octokit (installations.ts) — metadata.ts constructs no Octokit (rides the app client); start() installs the interval then awaits the bounded first walk; both EMPTY_SNAPSHOT literals (server.ts, routes/api.ts) aligned bannered; no-provider defaults follow. Tests: cold-window banner, interval-installed-before-first-refresh-resolves (deferred enumerate), double-start no-op, double-stop single-clear (test/aggregator.test.ts); focused suites 248/248 green at 6fd7d04. Review fix (independent review 6f4e6190 F1, P1): acceptance (2) was unmet at implement time — the two transports constructed in server.ts escaped the batch's three-module fence and the unit fakes hid them; now ALL FIVE transport constructions carry the 30s bound (app-client ThrottledOctokit, installations Octokit, server.ts metadata `new Octokit` + server.ts per-repo `graphql.defaults`, auth/oauth.ts via AbortSignal.timeout); source-contract gate test/transport-timeout-contract.test.ts pins the wiring (census 4 matching construction sites, floor ≥4); the duplicated EMPTY_SNAPSHOT literals were consolidated to the exported COLD_START_SNAPSHOT (review F3)

### Boot-time last-known snapshot bridge
- id: `rm-198` | track: reliability | priority: 34.0 | status: candidate (added 2026-09-25, cycle-14 batch, run ec07634c; decision-gated — supersedes the rm-112 TTL-removal rationale restart-only, never intra-cycle; gated on rm-197 landing first)
- signals: cold start serves bannered empty (rm-197) until the first walk completes even when a prior process held a good snapshot minutes earlier; the rm-112 TTL-removal rationale recorded freshness invariance, not a prohibition on a restart-only bridge
- acceptance: persist the last good snapshot (node:sqlite mirroring the listener-store pattern) and serve it at boot bannered-stale until the first refresh completes; decision frame: restart-only semantics with no intra-cycle TTL, no API-budget motive, explicit supersession of the rm-112 rationale recorded in the implementing batch doc
- evidence: gated on rm-197's in-flight contract landing first; a future cycle decides the bridge against the decision frame recorded in docs/prioritization/2026-09-25-cycle-14-batch.md §Parked

### Degradation delivery (triggers for stale/degraded snapshots)
- id: `rm-199` | track: operator-experience | priority: 31.0 | status: candidate (added 2026-09-25, cycle-14 batch, run ec07634c; decision-gated — three delivery forms recorded, none chosen this cycle)
- signals: staleBanner and degradation counts exist in the snapshot contract but no code path triggers an operator notification on degradation; the /api/monitoring route is consumed by neither SPA nor operator tooling (rm-107 half); the invisible-failure lineage shows the operator does not poll; push-SEND lives gateway-side (vendored operator contract is GET-only with opaque subscription hashes) (decision evidence 2026-09-25, run 70ba5270 research 761db5e1: two of the three recorded forms are structurally unavailable on this fork — web/sw.ts is a self-unregistering kill-switch (no service worker survives to hold a push subscription), killing both fork-owned VAPID send and SW-local notification; the realistic forms are the rm-107 SPA surface riding the existing 30s polls, or the gateway-contract extension (rm-157 pairing) — the decision narrows to view-now vs ask-upstream)
- acceptance: a chosen delivery form fires on snapshot degradation: fork-owned VAPID send, client-side SW local notification, or a gateway-contract extension ask (blocked-external, pairs with rm-157); the choice recorded with its trade-offs before implementation
- evidence: decision frame + the three forms recorded in docs/prioritization/2026-09-25-cycle-14-batch.md §Parked; a future roadmap cycle decides

### Redaction single-char token boundary
- id: `rm-200` | track: security | priority: 41.0 | status: implemented 2026-09-25 in-tree, validated pre-review (cycle-14 batch B2, run ec07634c implement c3bbc0c8; validation run ec07634c targeted_tests 8b11a67e + full_tests 1cab3836: local focused 248/248 incl. the boundary suites, authoritative ephemeral-PR gates ALL 9 checks SUCCESS twice — PR #146 @ f2a23bd3 and PR #147 @ e55dcb1 — pending landing; review fix 07afb0e5 closed independent-review F2: the full pair token is now replaced unanchored — see evidence)
- signals: redactRepoIdentityFromText filtered its token list with token.length > 1, dropping GitHub-legal 1-char owner/repo tokens from log scrubbing entirely; the property suite's nameArb enforced min length 2, so the class was structurally untestable
- acceptance: boundary-anchored replacement covers tokens of length 1+ without mangling adjacent prose; nameArb widened to min length 1; regression test with 1-char owner and name proving standalone tokens scrub while embedded occurrences survive
- evidence: implemented: escapeRegExp + boundary-anchored (no word, dot or dash character adjacent) longest-first replacement in aggregator.ts; nameArb widened {1,30}→{0,30}; the identity property's assertion refined to boundary-anchored non-survival (not.toContain was too strong for tokens lexically embedded in scaffold words, e.g. 'one' inside 'alone' — embedding is not identity survival); explicit unit suite: standalone 1-char + full pair redacted, unrelated words ('cat','bravo') intact, dotted/dashed names ('a.txt','b-c') untouched; focused suites 248/248 green. Review fix (independent review 6f4e6190 F2, P3): the anchoring let a complete `owner/name` pair survive adjacent to '-'/'.' ('a/b-42' → `[REDACTED_REPO]/b-42`, name leaked) — the full pair token is now replaced UNANCHORED (a complete pair in prose is always an identity reference) while bare owner/name tokens keep boundary anchoring; residual (bare name adjacent to '.' in non-pair prose) accepted and documented in the code comment; unit regressions added for the hyphen-suffix pair cases

### Aggregator/shutdown lifecycle re-entry hardening
- id: `rm-201` | track: reliability | priority: 29.0 | status: implemented 2026-09-25 in-tree, validated pre-review (cycle-14 batch B3, run ec07634c implement c3bbc0c8; shutdown half verified ALREADY TRUE at 6fd7d04 — installShutdownHandlers carries the shutdownStarted latch + second-signal exit(1) — so the original doc-claim gap had closed by the renumber landing; implemented scope = the aggregator half; validation run ec07634c targeted_tests 8b11a67e + full_tests 1cab3836: local focused 248/248 incl. double-start/double-stop asserts, authoritative ephemeral-PR gates ALL 9 checks SUCCESS twice — PR #146 @ f2a23bd3 and PR #147 @ e55dcb1 — pending landing)
- signals: start() had no re-entry guard — double-start overwrote intervalHandle and leaked the first interval (stop clears only the live handle); shutdown.ts header claimed 'Idempotent at the process level' (claim-true at 6fd7d04, verified live this cycle)
- acceptance: start() re-entry is a documented no-op returning the existing lifecycle; stop() idempotent; shutdown claim matches behavior
- evidence: implemented: guard at start() head (logger.debug + return) with the leak rationale in a comment; duplicate-start asserts setIntervalFn called exactly once; double-stop asserts clearIntervalFn exactly once (test/aggregator.test.ts); shutdown.ts untouched (verified accurate as-is)

### AGENTS.md check-types gate description
- id: `rm-202` | track: docs | priority: 18.0 | status: implemented 2026-09-25 in-tree, validated pre-review (cycle-14 batch B4, run ec07634c implement c3bbc0c8; validation run ec07634c targeted_tests 8b11a67e + full_tests 1cab3836: authoritative ephemeral-PR gates ALL 9 checks SUCCESS twice — PR #146 @ f2a23bd3 and PR #147 @ e55dcb1, Lint covers the markdown surface — pending landing)
- signals: the gates line said check-types covers "server + web/" but the script also type-checks the .opencode project (true since 187d72a); agents following the doc mis-scoped the gate
- acceptance: the description lists all three surfaces; pnpm check-types itself unmodified (description-only)
- evidence: implemented: AGENTS.md gates line now reads "(server + `web/` + `.opencode`)"; package.json check-types script cited (tsc --noEmit && -p web/tsconfig.json && -p .opencode/tsconfig.json)

### Fork-exclusion guard misses the re-injectable .github/renovate.json5
- id: `rm-213` | track: reliability | priority: 40.0 | status: implemented 2026-09-25 (run 70ba5270 implement cc91a3ab, pre-review: both acceptance bullets landed — assertion + inline rm-136 rationale in test/fork-exclusion-guard.test.ts; planted-file negative verified; targeted 17/17 + full remote CI green on ephemeral PR #175; pending commit/landing)
- signals: test/fork-exclusion-guard.test.ts:52-53 asserts absence only of .github/workflows/renovate.yaml — .github/renovate.json5 (the other Renovate surface, rm-136 keep-deleted) is asserted by no test at main (`git grep -rn 'renovate.json5' origin/main -- test/` returns empty), and the re-injection the assertion would catch is empirically real: the file was absent at b185335 (2026-09-23), PRESENT again at 7809df6 (re-introduced by an upstream merge), and removed only at 0616de2 (2026-09-25 04:30) — a two-day window in which the guard stayed green; rm-136's own absence verification is one-off `git cat-file -e` probes, not a durable test; same structural-guard class as rm-131/rm-164 (guard what recurs, not what is currently true)
- acceptance:
  - fork-exclusion-guard asserts `.github/renovate.json5` absence alongside renovate.yaml; negative-verified (the test goes red when the file is planted at a scratch commit, green at main)
  - the guard's exclusion set carries the rm-136 keep-deleted rationale inline so a future maintainer knows deleting the assertion is a policy change, not cleanup
- evidence: guard test diff; `git grep -rn 'renovate.json5' test/` shows the assertion; the planted-file negative check recorded in the batch doc

### Document the operator environment variables (README table + structural coverage guard)
- id: `rm-214` | track: docs | priority: 22.0 | status: implemented 2026-09-25 (run 70ba5270 implement cc91a3ab, pre-review: README Configuration carries the full env table — 23 genuine env reads, the 4 same-named code constants footnoted, RATE_LIMIT_TRUSTED_PROXY semantics included; test/env-docs-guard.test.ts asserts src-tokens == README-tokens; both drift directions negative-verified; full remote CI green on ephemeral PR #175; pending commit/landing)
- signals: 27 distinct DASHBOARD_*/RATE_LIMIT_* tokens are read in src/ at 20ade64 (`git grep -oh 'DASHBOARD_[A-Z_]*\|RATE_LIMIT_[A-Z_]*' origin/main -- src/ | sort -u | wc -l` → 27) while README.md's Configuration section documents only DASHBOARD_COOKIE_KEY (the rm-206 landing added exactly that paragraph); RATE_LIMIT_TRUSTED_PROXY — the knob that re-keys rate limiting to XFF first hops (rm-129's opt-in) and silently changes WHO is throttled when the proxy chain mis-sets the header — appears in no operator-facing README/docs page (`git grep RATE_LIMIT_TRUSTED_PROXY origin/main -- docs/ README.md` → empty; AGENTS.md:30-33 already carries rm-129's agent-facing convention note — the README table generalizes it; only the listener key contract doc mentions its neighbor keys); PR #166 documents DASHBOARD_MONITORING_REFRESH in README's env section, establishing the pattern to generalize
- acceptance:
  - README Configuration carries a table covering every DASHBOARD_*/RATE_LIMIT_* var read in src/ with type, default, and where it is read
  - a structural guard asserts coverage: the token set matched in src/ equals the token set documented in README (same style as rm-164's prose guard) so a new env var fails the suite until documented; negative-verified by un-documenting one var
- evidence: README diff + guard test diff; `pnpm test` green with the guard; negative check recorded

### Listener per-message parse failures vanish against the unread count
- id: `rm-215` | track: reliability | priority: 26.0 | status: candidate (added 2026-09-25, run 70ba5270 — carried forward from the orphaned prior assess 13bc5355 so it is not lost, then re-verified live at origin/main 20ade64; NOT covered by the in-flight client-truth batch (spool proposal d0d342d7 rm-209) whose five sites are pollUnreadCount/session-expiry/AbortController/aggregator-comment/dependency-review; cycle-15 prioritize 2e2910cf DEFERRED to next-cycle lead: head-on collision with PR #155's listener rewrite — land post-#155 only)
- signals: web/src/api/listener.ts:35 parseMessage returns null on any per-message contract mismatch and the :106-109 loop silently skips nulls — envelope drift fails visibly ('contract-drift' at :101) but an individual malformed message disappears from the list while data.unreadCount still counts it server-side, producing an operator-invisible messages-vs-count mismatch; the class is exactly rm-112's invisible-failure lineage (fixed on the aggregation side) appearing on the listener view side
- acceptance:
  - per-message parse drops are counted and surfaced (drop count on the listener fetch Result, rendered in the view when non-zero) — the operator can distinguish "N new messages" from "N counted, M dropped"
  - a malformed-message test asserts the count increments and well-formed messages still render
  - no per-frame logging spam (count only)
- evidence: parse-guard diff + test; view shows the drop badge under a seeded malformed message; docs updated where the listener view contract is described

<!-- cycle-1 extension #4 (2026-09-25, conductor run 70ba5270f24940f5812c3f5db9dbf082 roadmap attempt 265348bc0bfa4cf991eaf75c19c208dd, repository-maintenance cycle:1; built on origin/main 20ade64 fetched fresh this phase — the dispatch frame 7809df6 sits 45 behind with no unique content per this run's assess): added rm-213 (fork-exclusion guard misses re-injectable .github/renovate.json5 — assess F1), rm-214 (operator env-var documentation + structural coverage guard — assess F3 + research C8), rm-215 (listener per-message parse drops vs unread count — carried from orphaned prior assess 13bc5355, re-verified at 20ade64). Id-space: minted above the ALL-LINEAGE IN-FLIGHT max rm-212 (spool proposal 40c5bb4d, run 040f1ae0's roadmap, claims rm-207..212; parallel spool proposal d0d342d7, run bf5d7753, claims colliding rm-207..209); landed max at 20ade64 is rm-206; if either in-flight proposal lands first this block renumbers at integrate per the d00d095/8bbff5c6 convention — in-flight claims renumber, landed meanings win. Deliberately NOT minted (dedupe): gateway session-validation cost/memoization (this run's assess F2 + research C5; in-flight as bf5d7753 rm-207 — reconcile by content on landing); pollUnreadCount in-flight guard + session-expiry-vs-network indistinguishability + dead AbortController (assess carried findings; in-flight as bf5d7753 rm-209 a-c); dependency-majors refresh (rm-108's 2026-09-25 signal already records the identical vitest 5.0.1/TypeScript 7.0.2/jsdom 30.1.1/pnpm 12.6.0 measures); stale-PR stewardship (rm-159's fresh signal in the 40c5bb4d proposal carries the same 23-open measurement); absorb riders (rm-157 signals in both in-flight proposals cover the v0.115.x set); ubuntu-26.04 GA (extends landed rm-188 — dated signal appended there, no id); the composed-status panel (rm-107 is its standing home — fifth-recurrence evidence appended there); web-push digest rerouting for rm-106 (structurally unavailable: sw.ts kill-switch — recorded instead as rm-199 decision evidence). Dated signals appended: rm-107, rm-179, rm-188, rm-199. Completed and superseded sections untouched. -->

<!-- cycle-1 compound #5 (2026-09-25, conductor run 70ba5270f24940f5812c3f5db9dbf082 compound attempt 944069953a3848c0bbcc0eb0dbe1b71f, pre-review evidence only): status flips rm-213/rm-214 candidate -> implemented 2026-09-25 (pre-review: acceptance landed, planted-file + both-drift-directions negatives verified, targeted 44/44 focused then full remote CI 9/9 green on ephemeral PR #173 commit cdc57bb and PR #175 commit cab0658; commit/landing still pending — review happens after this step); rm-215 deferral recorded (cycle-15 prioritize 2e2910cf: head-on collision with open PR #155's listener rewrite — next-cycle lead, land post-#155 only). Prevention learning minted as docs/solutions/workflow-issues/prioritization-doc-atx-heading-lint-trap-2026-09-25.md (bare #NNN PR references at column 0 in any markdown doc fail the remote Lint job on markdown/no-missing-atx-heading-space; caught on ephemeral PR #173 run 36101910325 job 107966020653). Env-count correction recorded: 27 grep identifiers = 23 genuine env reads + 4 same-named code constants (RATE_LIMIT_CLASSES/MAX/MAX_PER_CLASS/WINDOW_MS at server.ts:88-117) — the README table + test/env-docs-guard.test.ts extract from actual read sites, not grep. Next-cycle context lives in docs/prioritization/2026-09-25-cycle-15-batch-run-70ba5270.md compound addendum (deferral map, merge-conflict surface, watchlist). -->
## Superseded items

### Add test coverage for 21 untested module(s)
- id: `rm-002` | track: reliability | priority: 100.0 | status: superseded (2026-09-19, run cf5527c1; resurrection by the 2026-09-20 fleet render reverted 2026-09-20, run 270220e7)
- original acceptance: every flagged module has a corresponding test file with at least one passing test; original evidence cited "CI: pytest collects the new test files"
- supersession rationale: all 21 flagged paths are under `.agents/skills/impeccable/scripts/**`, designated vendored by `eslint.config.ts` — work there is discarded on reinstall; the cited pytest evidence cannot exist in this Node 24/pnpm/Vitest repository. First-party coverage is not flagged as gapped by the assess cycles (3017 tests passing at 2f3a884; listener/aggregator paths covered directly or indirectly). Replaced by the vendored-path exclusion in `rm-104`; first-party coverage signals should regenerate from the fixed render.

### Refactor 21 high-complexity function(s)
- id: `rm-001` | track: reliability | priority: 90.0 | status: superseded (2026-09-19, run cf5527c1; resurrection by the 2026-09-20 fleet render reverted 2026-09-20, run 270220e7)
- original acceptance: each flagged function decomposed below the branch threshold with behavior locked by characterization tests; original evidence cited an "ast-based branch-count check in CI"
- supersession rationale: every flagged site is in vendored `.agents/skills/impeccable/scripts/live-browser.js` and siblings (same discard-on-reinstall problem); no ast-based CI check exists in this repository's stack. Replaced by `rm-104`; first-party complexity signals should regenerate from the fixed render.

<!-- managed by hermes-roadmap render; do not edit by hand -->
